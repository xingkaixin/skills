// Generated by scripts/generate-web-skills.ts. Do not edit manually.
export const skillContent: Record<string, string> = {
  "agent-friendly-cli-design": "<h2>面向 Agent 的 CLI 设计清单</h2>\n<p>Agent-friendly 不等于 machine-readable。machine-readable 只解决程序串联，agent-friendly 还要解决两件事：<strong>语义理解</strong>（LLM 能读懂输出）和 <strong>context 成本</strong>（无关字段会浪费 token 并污染推理）。设计时始终假设使用者是一个不能按方向键、context 很贵、会不断重试的 agent。</p>\n<h3>一、结构：用两层解决「命令爆炸」</h3>\n<p>能力面一大，若每个能力都做成独立 command，<code>--help</code> 会爆炸，agent 永远要猜「这个操作有没有对应命令」。用两层拆分：</p>\n<h4>Resource 层 —— 承接长尾 CRUD</h4>\n<p>把 RESTful resource model 迁移到 CLI：<strong>路径定位资源，动词表达动作</strong>。这样 API 文档几乎不用翻译就能当 CLI 使用说明，agent 学会一个资源就学会所有资源。</p>\n<ul>\n<li>动词收敛到最小闭集：<code>list / get / create / update / delete</code></li>\n<li>参数分层：<code>key=value</code> 是资源参数，<code>--flag</code> 是 CLI 行为控制</li>\n<li>用 <code>/</code> 前缀显式标记资源路径，避免 <code>issues list</code> 里 issues 到底是 command 还是 resource 的歧义</li>\n</ul>\n<pre><code class=\"language-bash\">cli /issues list state=open author=me\ncli /issues create title=&quot;Fix login&quot; body=&quot;...&quot;\ncli /issues/42 update state=closed\ncli /issues/42 delete\ncli /issues/42/comments create body=&quot;LGTM&quot;\ncli /issues list state=open --json --jq &#39;.[].title&#39;\n</code></pre>\n<p>若基于已有 REST API，直接提供一个透传入口（如 <code>gh api</code>），让 <code>DELETE /repos/{owner}/{repo}/issues/comments/{id}</code> 一一对应到 CLI 调用，无需为每个长尾能力手写 command。</p>\n<h4>Command 层 —— 承接无法 resource 化的意图</h4>\n<p>有些意图不是对资源做 CRUD，强行 resource 化很别扭。这些留在 command 层（无 <code>/</code> 前缀）：</p>\n<ul>\n<li><code>login</code> —— 不是对某资源的操作</li>\n<li><code>clone</code> —— 同时涉及远程 repo、本地文件系统、git 状态</li>\n<li><code>checkout</code> —— 切换本地工作区状态</li>\n<li><code>merge</code> —— 用户表达的是「把改动合进去」，而非「改字段再删分支」</li>\n</ul>\n<pre><code class=\"language-bash\">cli login\ncli clone acme/demo-app\ncli checkout 353\ncli merge 353 --squash\ncli status\n</code></pre>\n<p>Command 的本质是承接无法自然 resource 化的用户意图，「多步骤编排」只是它的常见来源之一。两层是互补关系：resource 层负责覆盖面，command 层负责高层意图。</p>\n<h3>二、输出：默认语义化，需要时结构化</h3>\n<p>在 agent workflow 里输出不是越多越好。无关字段进入 context 会浪费 token 并干扰后续推理。</p>\n<ul>\n<li><strong>默认输出走自然语言 / 语义化 key-value</strong>，这是 LLM 最擅长的表征，比一整坨 JSON 更好懂</li>\n<li><strong>JSON 的定位是串联和精确抽取，不是默认认知界面</strong></li>\n<li>提供 <code>--json</code> 选字段 + <code>--jq</code> 做裁剪，让裁剪发生在进入 LLM context <strong>之前</strong></li>\n<li>成功时返回可用的结构化数据（ID、URL），而不是只输出 emoji</li>\n</ul>\n<pre><code class=\"language-bash\"># 默认：语义化，适合直接理解\ncli /issues/42 get\n# Issue #42: Fix login bug\n# State: open  Author: alice  Labels: bug, auth\n\n# 需要串联时：结构化 + 裁剪，只让必要信息进 context\ncli /issues/42 get --json --jq &#39;.title&#39;\n</code></pre>\n<h3>三、执行层：让 agent 能稳定使用的基建</h3>\n<table>\n<thead>\n<tr>\n<th>原则</th>\n<th>做法</th>\n<th>反例</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>非交互优先</td>\n<td>所有输入都能通过 flag 传入；交互模式仅作缺参时的 fallback；支持 <code>PROMPT_DISABLED=1</code> 类环境变量</td>\n<td>执行中途弹 <code>? Which environment? (arrow keys)</code> 卡死 agent</td>\n</tr>\n<tr>\n<td>渐进式文档</td>\n<td>让 agent 跑 <code>cli</code>→看子命令→跑 <code>cli deploy --help</code> 按需发现，不一次性倾倒全部文档</td>\n<td>首屏塞满用不到的命令，浪费 context</td>\n</tr>\n<tr>\n<td><code>--help</code> 带示例</td>\n<td>每个子命令都有 <code>--help</code>，且<strong>包含具体示例</strong>；agent 靠模式匹配 <code>cli deploy --env staging --tag v1.2.3</code> 比读描述快</td>\n<td>只有干巴巴的选项描述</td>\n</tr>\n<tr>\n<td>接受 stdin + flag</td>\n<td>agent 用管道思维串联命令</td>\n<td>要求奇怪顺序的位置参数，或缺值就回退交互</td>\n</tr>\n<tr>\n<td>快速失败 + 可操作错误</td>\n<td>缺参立即报错并给出正确调用方式和获取值的命令，agent 擅长自我修正</td>\n<td>挂起或只说「参数错误」</td>\n</tr>\n<tr>\n<td>幂等</td>\n<td>重复执行 <code>deploy</code> 返回「already deployed, no-op」，不产生重复副作用</td>\n<td>重试导致重复部署</td>\n</tr>\n<tr>\n<td><code>--dry-run</code></td>\n<td>破坏性操作前可预览计划，先验证再实跑</td>\n<td>直接执行不可逆操作</td>\n</tr>\n<tr>\n<td><code>--yes</code> / <code>--force</code></td>\n<td>安全路径设为默认，但允许绕过确认</td>\n<td>无法跳过确认，agent 卡在 <code>are you sure?</code></td>\n</tr>\n<tr>\n<td>可预测结构</td>\n<td>选定一种模式（resource + verb / 统一 flag 名）并处处复用，学会一个能推断其余</td>\n<td>每个命令自成体系，无法泛化</td>\n</tr>\n<tr>\n<td>flag 一致复用</td>\n<td><code>--repo</code>、<code>--json</code>、<code>--jq</code> 等在不同命令里语义一致</td>\n<td>同一概念在不同命令用不同 flag 名</td>\n</tr>\n<tr>\n<td><code>--web</code> 作 fallback</td>\n<td>不假装覆盖所有交互，有些事 Web 更合适</td>\n<td>强行在 CLI 里实现所有交互</td>\n</tr>\n</tbody></table>\n<p>错误信息示例（快速失败 + 指路）：</p>\n<pre><code class=\"language-text\">Error: No image tag specified.\n  cli deploy --env staging --tag &lt;image-tag&gt;\n  Available tags: cli build list --output tags\n</code></pre>\n<h3>设计自检清单</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 命令是否分成了 resource 层（<code>/path</code> + 统一动词）和 command 层（高层意图）？</li>\n<li><input disabled=\"\" type=\"checkbox\"> resource 动词是否收敛到 <code>list/get/create/update/delete</code>？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 默认输出是否语义化，<code>--json</code>/<code>--jq</code> 是否可裁剪字段？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 成功输出是否返回可用的 ID / URL 而非仅 emoji？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 是否全程非交互可用（flag + stdin + 环境变量）？</li>\n<li><input disabled=\"\" type=\"checkbox\"> <code>--help</code> 是否每个子命令都有且带示例？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 错误是否快速失败并给出修正调用？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 破坏性操作是否幂等 + 有 <code>--dry-run</code> + <code>--yes</code>？</li>\n<li><input disabled=\"\" type=\"checkbox\"> flag 命名是否跨命令一致、结构是否可推断？</li>\n</ul>\n",
  "ai-avoid": "<h2>AI Writing Tropes to Avoid</h2>\n<p>Add this file to your AI assistant&#39;s system prompt or context to help it avoid\ncommon AI writing patterns. Source: <a href=\"https://tropes.fyi\">tropes.fyi</a> by <a href=\"https://ossama.is\">ossama.is</a></p>\n<hr>\n<h3>Word Choice</h3>\n<h4>&quot;Quietly&quot; and Other Magic Adverbs</h4>\n<p>Overuse of &quot;quietly&quot; and similar adverbs to convey subtle importance or understated power. AI reaches for these adverbs to make mundane descriptions feel significant. Also includes: &quot;deeply&quot;, &quot;fundamentally&quot;, &quot;remarkably&quot;, &quot;arguably&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;quietly orchestrating workflows, decisions, and interactions&quot;</li>\n<li>&quot;the one that quietly suffocates everything else&quot;</li>\n<li>&quot;a quiet intelligence behind it&quot;</li>\n</ul>\n<h4>&quot;Delve&quot; and Friends</h4>\n<p>Used to be the most infamous AI tell. &quot;Delve&quot; went from an uncommon English word to appearing in a staggering percentage of AI-generated text. Part of a family of overused AI vocabulary including &quot;certainly&quot;, &quot;utilize&quot;, &quot;leverage&quot; (as a verb), &quot;robust&quot;, &quot;streamline&quot;, and &quot;harness&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Let&#39;s delve into the details...&quot;</li>\n<li>&quot;Delving deeper into this topic...&quot;</li>\n<li>&quot;We certainly need to leverage these robust frameworks...&quot;</li>\n</ul>\n<h4>&quot;Tapestry&quot; and &quot;Landscape&quot;</h4>\n<p>Overuse of ornate or grandiose nouns where simpler words would do. &quot;Tapestry&quot; is used to describe anything interconnected. &quot;Landscape&quot; is used to describe any field or domain. Other offenders: &quot;paradigm&quot;, &quot;synergy&quot;, &quot;ecosystem&quot;, &quot;framework&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The rich tapestry of human experience...&quot;</li>\n<li>&quot;Navigating the complex landscape of modern AI...&quot;</li>\n<li>&quot;The ever-evolving landscape of technology...&quot;</li>\n</ul>\n<h4>The &quot;Serves As&quot; Dodge</h4>\n<p>Replacing simple &quot;is&quot; or &quot;are&quot; with pompous alternatives like &quot;serves as&quot;, &quot;stands as&quot;, &quot;marks&quot;, or &quot;represents&quot;. AI avoids basic copulas because its repetition penalty pushes it toward fancier constructions (I&#39;ve studied this!).</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The building serves as a reminder of the city&#39;s heritage.&quot;</li>\n<li>&quot;Gallery 825 serves as LAAA&#39;s exhibition space for contemporary art.&quot;</li>\n<li>&quot;The station marks a pivotal moment in the evolution of regional transit.&quot;</li>\n</ul>\n<hr>\n<h3>Sentence Structure</h3>\n<h4>Negative Parallelism</h4>\n<p>The &quot;It&#39;s not X -- it&#39;s Y&quot; pattern, often with an em dash. The single most commonly identified AI writing tell. Man I f*cking hate it. AI uses this to create false profundity by framing everything as a surprising reframe. One in a piece can be effective; ten in a blog post is a genuine insult to the reader. Before LLMs, people simply did not write like this at scale. Includes the causal variant &quot;not because X, but because Y&quot; where every explanation is framed as a surprise reveal.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;It&#39;s not bold. It&#39;s backwards.&quot;</li>\n<li>&quot;Feeding isn&#39;t nutrition. It&#39;s dialysis.&quot;</li>\n<li>&quot;Half the bugs you chase aren&#39;t in your code. They&#39;re in your head.&quot;</li>\n</ul>\n<h4>&quot;Not X. Not Y. Just Z.&quot;</h4>\n<p>The dramatic countdown pattern. AI builds tension by negating two or more things before revealing the actual point. Creates a false sense of narrowing down to the truth.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Not a bug. Not a feature. A fundamental design flaw.&quot;</li>\n<li>&quot;Not ten. Not fifty. Five hundred and twenty-three lint violations across 67 files.&quot;</li>\n<li>&quot;not recklessly, not completely, but enough&quot;</li>\n</ul>\n<h4>&quot;The X? A Y.&quot;</h4>\n<p>Self-posed rhetorical questions answered immediately in the next sentence or clause. The model asks a question nobody was asking, then answers it for dramatic effect. Thinks this is the epitome of great writing.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The result? Devastating.&quot;</li>\n<li>&quot;The worst part? Nobody saw it coming.&quot;</li>\n<li>&quot;The scary part? This attack vector is perfect for developers.&quot;</li>\n</ul>\n<h4>Anaphora Abuse</h4>\n<p>Repeating the same sentence opening multiple times in quick succession.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;They assume that users will pay... They assume that developers will build... They assume that ecosystems will emerge... They assume that...&quot;</li>\n<li>&quot;They could expose... They could offer... They could provide... They could create... They could let... They could unlock...&quot;</li>\n<li>&quot;They have built engines, but not vehicles. They have built power, but not leverage. They have built walls, but not doors.&quot;</li>\n</ul>\n<h4>Tricolon Abuse</h4>\n<p>Overuse of the rule-of-three pattern, often extended to four or five. A single tricolon is elegant; three back-to-back tricolons are a pattern recognition failure.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Products impress people; platforms empower them. Products solve problems; platforms create worlds. Products scale linearly; platforms scale exponentially.&quot;</li>\n<li>&quot;identity, payments, compute, distribution&quot;</li>\n<li>&quot;workflows, decisions, and interactions&quot;</li>\n</ul>\n<h4>&quot;It&#39;s Worth Noting&quot;</h4>\n<p>Filler transitions that signal nothing. AI uses these phrases to introduce new points without actually connecting them to the previous argument. Also includes: &quot;It bears mentioning&quot;, &quot;Importantly&quot;, &quot;Interestingly&quot;, &quot;Notably&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;It&#39;s worth noting that this approach has limitations.&quot;</li>\n<li>&quot;Importantly, we must consider the broader implications.&quot;</li>\n<li>&quot;Interestingly, this pattern repeats across industries.&quot;</li>\n</ul>\n<h4>Superficial Analyses</h4>\n<p>Tacking a present participle (&quot;-ing&quot;) phrase onto the end of a sentence to inject shallow analysis that says nothing. The model attaches significance, legacy, or broader meaning to mundane facts using phrases like &quot;highlighting its importance&quot;, &quot;reflecting broader trends&quot;, or &quot;contributing to the development of...&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;contributing to the region&#39;s rich cultural heritage&quot;</li>\n<li>&quot;This etymology highlights the enduring legacy of the community&#39;s resistance and the transformative power of unity in shaping its identity.&quot;</li>\n<li>&quot;underscoring its role as a dynamic hub of activity and culture&quot;</li>\n</ul>\n<h4>False Ranges</h4>\n<p>Using &quot;from X to Y&quot; constructions where X and Y aren&#39;t on any real scale. In legitimate use, &quot;from X to Y&quot; implies a spectrum with a meaningful middle. AI uses it as a fancy way to list two loosely related things. &quot;From innovation to cultural transformation&quot; -- what&#39;s in between???? Nothing!</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;From innovation to implementation to cultural transformation.&quot;</li>\n<li>&quot;From the singularity of the Big Bang to the grand cosmic web.&quot;</li>\n<li>&quot;From problem-solving and tool-making to scientific discovery, artistic expression, and technological innovation.&quot;</li>\n</ul>\n<h4>Gerund Fragment Litany</h4>\n<p>After making a claim, AI illustrates it with a stream of verbless gerund fragments — standalone sentences with no grammatical subject. &quot;Fixing small bugs. Writing straightforward features. Implementing well-defined tickets.&quot; The first sentence already said everything. The fragments add nothing except word count and that familiar AI cadence. Humans don&#39;t write first drafts this way. It&#39;s a pure structural tic.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Fixing small bugs. Writing straightforward features. Implementing well-defined tickets.&quot;</li>\n<li>&quot;Reviewing pull requests. Debugging edge cases. Attending architecture meetings.&quot;</li>\n<li>&quot;Shipping faster. Moving quicker. Delivering more.&quot;</li>\n</ul>\n<hr>\n<h3>Paragraph Structure</h3>\n<h4>Short Punchy Fragments</h4>\n<p>Excessive use of very short sentences or sentence fragments as standalone paragraphs for manufactured emphasis. RLHF training has pushed models toward &quot;writing for readability&quot; aimed at the lowest common denominator: one thought per sentence, no mental state-keeping required. It&#39;s an inhuman style. No real person writes first drafts this way because it doesn&#39;t match how humans think or speak.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;He published this. Openly. In a book. As a priest.&quot;</li>\n<li>&quot;These weren&#39;t just products. And the software side matched. Then it professionalised. But I adapted.&quot;</li>\n<li>&quot;Platforms do.&quot;</li>\n</ul>\n<h4>Listicle in a Trench Coat</h4>\n<p>Numbered or labeled points dressed up as continuous prose. The model writes what is essentially a listicle but wraps each point in a paragraph that starts with &quot;The first... The second... The third...&quot; to disguise the format. Perhaps you told it to stop generating lists and it decided to do this instead... still very common.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The first wall is the absence of a free, scoped API... The second wall is the lack of delegated access... The third wall is the absence of scoped permissions...&quot;</li>\n<li>&quot;The second takeaway is that... The third takeaway is that... The fourth takeaway is that...&quot;</li>\n</ul>\n<hr>\n<h3>Tone</h3>\n<h4>&quot;Here&#39;s the Kicker&quot;</h4>\n<p>False suspense transitions that promise a revelation but deliver a point that did NOT need the buildup. The model uses these phrases to manufacture drama before an otherwise unremarkable observation LOL. Also includes: &quot;Here&#39;s the thing&quot;, &quot;Here&#39;s where it gets interesting&quot;, &quot;Here&#39;s what most people miss&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Here&#39;s the kicker.&quot;</li>\n<li>&quot;Here&#39;s the thing about AI adoption.&quot;</li>\n<li>&quot;Here&#39;s where it gets interesting.&quot;</li>\n</ul>\n<h4>&quot;Think of It As...&quot;</h4>\n<p>The patronizing analogy. AI constantly reaches for &quot;Think of it as...&quot; or &quot;It&#39;s like a...&quot; to simplify concepts. The model defaults to teacher mode and assumes the reader needs a metaphor to understand anything. Often produces analogies that are less clear than the original concept.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Think of it like a highway system for data.&quot;</li>\n<li>&quot;Think of it as a Swiss Army knife for your workflow.&quot;</li>\n<li>&quot;It&#39;s like asking someone to buy a car they&#39;re only allowed to sit in while it&#39;s parked.&quot;</li>\n</ul>\n<h4>&quot;Imagine a World Where...&quot;</h4>\n<p>The classic AI invitation to futurism. To sell the argument usually begins with &quot;Imagine&quot; followed by a list of wonderful things that will happen if the reader agrees with the premise.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Imagine a world where every tool you use -- your calendar, your inbox, your documents, your CRM, your code editor -- has a quiet intelligence behind it...&quot;</li>\n<li>&quot;In that world, workflows stop being collections of manual steps and start becoming orchestrations.&quot;</li>\n</ul>\n<h4>False Vulnerability</h4>\n<p>Simulated self-awareness or honesty that reads as performative. The model pretends to break the fourth wall or admit a bias, creating a false sense of authenticity. Real vulnerability is specific and uncomfortable; AI vulnerability is polished and risk-free!!!!</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;And yes, I&#39;m openly in love with the platform model&quot;</li>\n<li>&quot;And yes, since we&#39;re being honest: I&#39;m looking at you, OpenAI, Google, Anthropic, Meta&quot;</li>\n<li>&quot;This is not a rant; it&#39;s a diagnosis&quot;</li>\n</ul>\n<h4>&quot;The Truth Is Simple&quot;</h4>\n<p>Asserting that something is obvious, clear or simple instead of actually proving it. If you have to tell the reader your point is clear, it very likely isn&#39;t.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The reality is simpler and less flattering&quot;</li>\n<li>&quot;History is unambiguous on this point&quot;</li>\n<li>&quot;History is clear, the metrics are clear, the examples are clear&quot;</li>\n</ul>\n<h4>Grandiose Stakes Inflation</h4>\n<p>Everything is the most important thing ever. AI inflates the stakes of every argument to world-historical significance. A blog post about API pricing becomes a meditation on the fate of civilization.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;This will fundamentally reshape how we think about everything.&quot;</li>\n<li>&quot;will define the next era of computing&quot;</li>\n<li>&quot;something entirely new&quot;</li>\n</ul>\n<h4>&quot;Let&#39;s Break This Down&quot;</h4>\n<p>The pedagogical voice that assumes the reader needs hand-holding. AI defaults to a teacher-student dynamic even when writing for expert audiences. Also includes: &quot;Let&#39;s unpack this&quot;, &quot;Let&#39;s explore&quot;, &quot;Let&#39;s dive in&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Let&#39;s break this down step by step.&quot;</li>\n<li>&quot;Let&#39;s unpack what this really means.&quot;</li>\n<li>&quot;Let&#39;s explore this idea further.&quot;</li>\n</ul>\n<h4>Vague Attributions</h4>\n<p>Attributing claims to unnamed authorities instead of being specific. AI loves to invoke &quot;experts&quot;, &quot;observers&quot;, &quot;industry reports&quot;, and &quot;several publications&quot; without naming anyone. It also inflates the quantity of sources -- presenting what one person said as a widely held view, or writing &quot;several publications have cited&quot; when it means two. If you can&#39;t name the expert, you don&#39;t have a source.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Experts argue that this approach has significant drawbacks.&quot;</li>\n<li>&quot;Industry reports suggest that adoption is accelerating.&quot;</li>\n<li>&quot;Observers have cited the initiative as a turning point.&quot;</li>\n</ul>\n<h4>Invented Concept Labels</h4>\n<p>AI clusters invented compound labels that sound analytical without being grounded. It appends abstract problem-nouns (paradox, trap, creep, divide, vacuum, inversion) to domain words — &quot;supervision paradox&quot;, &quot;acceleration trap&quot;, &quot;workload creep&quot; — and uses them as if they&#39;re established, rigorously defined terms. They function as rhetorical shorthand: name a thing, skip the argument. Multiple such labels in the same piece is a strong signal of AI slop.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;the supervision paradox&quot;</li>\n<li>&quot;the acceleration trap&quot;</li>\n<li>&quot;workload creep&quot;</li>\n</ul>\n<hr>\n<h3>Formatting</h3>\n<h4>Em-Dash Addiction</h4>\n<p>Compulsive overuse of em dashes for dramatic pauses, parenthetical asides and pivot points. A human writer might use 2-3 per piece (and naturally); AI will use 20+.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The problem -- and this is the part nobody talks about -- is systemic.&quot;</li>\n<li>&quot;The tinkerer spirit didn&#39;t die of natural causes -- it was bought out.&quot;</li>\n<li>&quot;Not recklessly, not completely -- but enough -- enough to matter.&quot;</li>\n</ul>\n<h4>Bold-First Bullets</h4>\n<p>Every bullet point or list item starts with a bolded phrase or sentence. Extremely common in Claude and ChatGPT markdown output. Almost nobody formats lists this way when writing by hand. It&#39;s a telltale sign of AI-generated documentation and blog posts AND README files (especially with emojis).</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Every single bullet point begins with a bold keyword.&quot;</li>\n<li>&quot;<strong>Security</strong>: Environment-based configuration with...&quot;</li>\n<li>&quot;<strong>Performance</strong>: Lazy loading of expensive resources...&quot;</li>\n</ul>\n<h4>Unicode Decoration</h4>\n<p>Use of unicode arrows (-&gt;), smart/curly quotes, and other special characters that can&#39;t be easily typed on a standard keyboard. Real writers typing in a text editor produce straight quotes and -&gt; or =&gt;. Claude in particular loves the -&gt; arrow.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Input → Processing → Output&quot;</li>\n<li>&quot;This leads to better outcomes → which means higher engagement&quot;</li>\n<li>&quot;“Smart quotes” instead of straight &quot;quotes&quot; that you’d actually type&quot;</li>\n</ul>\n<hr>\n<h3>Composition</h3>\n<h4>Fractal Summaries</h4>\n<p>&quot;What I&#39;m going to tell you; what I&#39;m telling you; what I just told you&quot; -- applied at every level of the document. Every subsection gets a summary. Every section gets a summary. The document itself gets a summary.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;In this section, we&#39;ll explore... [3000 words later] ...as we&#39;ve seen in this section.&quot;</li>\n<li>&quot;A conclusion that restates every point already made in the previous 3000 words&quot;</li>\n<li>&quot;And so we return to where we began.&quot;</li>\n</ul>\n<h4>The Dead Metaphor</h4>\n<p>Latching onto a single metaphor and beating it into the ground across the entire thing. A human writer would introduce a metaphor, use it then move on. AI will repeat the same metaphor 5-10 times.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The ecosystem needs ecosystems to build ecosystem value.&quot;</li>\n<li>&quot;Walls and doors used 30+ times in the same article&quot;</li>\n<li>&quot;Every paragraph finds a way to say &quot;primitives&quot; again&quot;</li>\n</ul>\n<h4>Historical Analogy Stacking</h4>\n<p>ESPECIALLY COMMON IN TECHNICAL WRITING: Rapid-fire listing of historical companies or tech revolutions to build false authority.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Apple didn&#39;t build Uber. Facebook didn&#39;t build Spotify. Stripe didn&#39;t build Shopify. AWS didn&#39;t build Airbnb.&quot;</li>\n<li>&quot;Every major technological shift -- the web, mobile, social, cloud -- followed the same pattern.&quot;</li>\n<li>&quot;Take Spotify... Or consider Uber... Airbnb followed a similar path... Shopify is another example... Even Discord...&quot;</li>\n</ul>\n<h4>One-Point Dilution</h4>\n<p>Making a single argument and restating it in 10 different ways across thousands of words. The model pads a simple thesis to feel &quot;comprehensive&quot; by rephrasing the same idea with different metaphors, examples, and framings. An 800-word argument becomes 4000 words of circular repetition.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The same point, restated eight ways across 4000 words.&quot;</li>\n<li>&quot;Each section rephrases the thesis with a different metaphor but adds nothing new&quot;</li>\n</ul>\n<h4>Content Duplication</h4>\n<p>Repeating entire sections or paragraphs verbatim within the same piece. This happens when the model loses track of what it has already written, especially in longer pieces. A dead giveaway of unedited AI output. Less common nowadays.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;The same section appeared twice, word-for-word identical.&quot;</li>\n<li>&quot;Paragraph 3 and paragraph 17 are the same sentence reworded&quot;</li>\n</ul>\n<h4>The Signposted Conclusion</h4>\n<p>Explicitly announcing the conclusion with &quot;In conclusion&quot;, &quot;To sum up&quot;, or &quot;In summary&quot;. Competent writing doesn&#39;t need to tell you it&#39;s concluding. The reader can feel it. AI signals its structural moves because it&#39;s following a template, not writing organically.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;In conclusion, the future of AI depends on...&quot;</li>\n<li>&quot;To sum up, we&#39;ve explored three key themes...&quot;</li>\n<li>&quot;In summary, the evidence suggests...&quot;</li>\n</ul>\n<h4>&quot;Despite Its Challenges...&quot;</h4>\n<p>The rigid formula where AI acknowledges problems only to immediately dismiss them. Always follows the same beat: &quot;Despite its [positive words], [subject] faces challenges...&quot; then ends with &quot;Despite these challenges, [optimistic conclusion].&quot;.</p>\n<p><strong>Avoid patterns like:</strong></p>\n<ul>\n<li>&quot;Despite these challenges, the initiative continues to thrive.&quot;</li>\n<li>&quot;Despite its industrial and residential prosperity, Korattur faces challenges typical of urban areas.&quot;</li>\n<li>&quot;Despite their promising applications, pyroelectric materials face several challenges that must be addressed for broader adoption.&quot;</li>\n</ul>\n<hr>\n<p>Remember: any of these patterns used once might be fine. The problem is when\nmultiple tropes appear together or when a single trope is used repeatedly.\nWrite like a human: varied, imperfect, specific.</p>\n",
  "article-illustrator": "<h2>Article Illustrator</h2>\n<p>Analyze articles, identify illustration positions, generate structured prompts for downstream image generation.</p>\n<h3>Two Dimensions</h3>\n<table>\n<thead>\n<tr>\n<th>Dimension</th>\n<th>Controls</th>\n<th>Examples</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Type</strong></td>\n<td>Information structure</td>\n<td>infographic, scene, flowchart, comparison, framework, timeline</td>\n</tr>\n<tr>\n<td><strong>Style</strong></td>\n<td>Visual aesthetics</td>\n<td>notion, warm, minimal, blueprint, watercolor, elegant</td>\n</tr>\n</tbody></table>\n<p>Combine freely: <code>--type infographic --style blueprint</code></p>\n<p>Or use presets: <code>--preset tech-explainer</code> → type + style in one flag. See <a href=\"references/style-presets.md\">Style Presets</a>.</p>\n<h3>Types</h3>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Best For</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>infographic</code></td>\n<td>Data, metrics, technical</td>\n</tr>\n<tr>\n<td><code>scene</code></td>\n<td>Narratives, emotional</td>\n</tr>\n<tr>\n<td><code>flowchart</code></td>\n<td>Processes, workflows</td>\n</tr>\n<tr>\n<td><code>comparison</code></td>\n<td>Side-by-side, options</td>\n</tr>\n<tr>\n<td><code>framework</code></td>\n<td>Models, architecture</td>\n</tr>\n<tr>\n<td><code>timeline</code></td>\n<td>History, evolution</td>\n</tr>\n</tbody></table>\n<h3>Styles</h3>\n<p>See <a href=\"references/styles.md\">references/styles.md</a> for Core Styles, full gallery, and Type × Style compatibility.</p>\n<h3>Workflow</h3>\n<pre><code>Article Illustrator Progress:\n- [ ] Step 0: Check EXTEND.md ⛔ BLOCKING\n- [ ] Step 1: Analyze content + save refs\n- [ ] Step 2: Confirm settings ⚠️\n- [ ] Step 3: Generate outline\n- [ ] Step 4: Create prompt files\n- [ ] Step 5: Completion report\n</code></pre>\n<h4>Step 0: Load EXTEND.md ⛔ BLOCKING</h4>\n<p>Check EXTEND.md in project root only:</p>\n<pre><code class=\"language-bash\">test -f EXTEND.md &amp;&amp; echo &quot;found&quot;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Result</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Found</td>\n<td>Read, parse, display summary → Continue</td>\n</tr>\n<tr>\n<td>Not found</td>\n<td>⛔ Run <a href=\"references/config/first-time-setup.md\">first-time-setup</a></td>\n</tr>\n</tbody></table>\n<p>Full procedures: <a href=\"references/workflow.md#step-0-load-extendmd\">references/workflow.md</a></p>\n<h4>Step 1: Analyze Content</h4>\n<table>\n<thead>\n<tr>\n<th>Analysis</th>\n<th>Output</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Content type</td>\n<td>Technical / Tutorial / Methodology / Narrative</td>\n</tr>\n<tr>\n<td>Purpose</td>\n<td>information / visualization / imagination</td>\n</tr>\n<tr>\n<td>Core arguments</td>\n<td>2-5 main points</td>\n</tr>\n<tr>\n<td>Positions</td>\n<td>Where illustrations add value</td>\n</tr>\n</tbody></table>\n<p><strong>CRITICAL</strong>: Metaphors → visualize underlying concept, NOT literal image.</p>\n<p>Full procedures: <a href=\"references/workflow.md#step-1-analyze-content\">references/workflow.md</a></p>\n<h4>Step 2: Confirm Settings ⚠️</h4>\n<p><strong>ONE AskUserQuestion, max 4 Qs. Q1-Q2 REQUIRED.</strong></p>\n<table>\n<thead>\n<tr>\n<th>Q</th>\n<th>Options</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Q1: Preset or Type</strong></td>\n<td>[Recommended preset], [alt preset], or manual: infographic, scene, flowchart, comparison, framework, timeline, mixed</td>\n</tr>\n<tr>\n<td><strong>Q2: Density</strong></td>\n<td>minimal (1-2), balanced (3-5), per-section (Recommended), rich (6+)</td>\n</tr>\n<tr>\n<td><strong>Q3: Style</strong></td>\n<td>[Recommended], minimal-flat, sci-fi, hand-drawn, editorial, scene, poster, Other — <strong>skip if preset chosen</strong></td>\n</tr>\n<tr>\n<td>Q4: Language</td>\n<td>When article language ≠ EXTEND.md setting</td>\n</tr>\n</tbody></table>\n<p>Full procedures: <a href=\"references/workflow.md#step-2-confirm-settings\">references/workflow.md</a></p>\n<h4>Step 3: Generate Outline</h4>\n<p>Save <code>outline.md</code> with frontmatter (type, density, style, image_count) and entries:</p>\n<pre><code class=\"language-yaml\">## Illustration 1\n**Position**: [section/paragraph]\n**Purpose**: [why]\n**Visual Content**: [what]\n**Filename**: illustration-01.md\n</code></pre>\n<p>Full template: <a href=\"references/workflow.md#step-3-generate-outline\">references/workflow.md</a></p>\n<h4>Step 4: Create Prompt Files</h4>\n<p><strong>Output</strong>: One <code>illustration-{NN}.md</code> file per illustration, in the same directory as the article.</p>\n<p>Each file contains:</p>\n<ul>\n<li>YAML frontmatter (type, style, references)</li>\n<li>Content Context (title, summary, keywords)</li>\n<li>Visual Design (type, style, aspect, language)</li>\n<li>Illustration Prompt (structured prompt)</li>\n<li>Rendering Notes (style characteristics)</li>\n<li>Reference Style (if reference images provided)</li>\n</ul>\n<p>Full template: <a href=\"references/prompt-construction.md\">references/prompt-construction.md</a></p>\n<h4>Step 5: Completion Report</h4>\n<pre><code>Article Illustration Prompts Ready!\n\nArticle: [path]\nType: [type] | Density: [level] | Style: [style]\n\nPrompt Files:\n✓ illustration-01.md\n✓ illustration-02.md\n\nNext Steps:\n1. Use illustration prompts with your preferred image generation tool\n2. Generated images will be saved alongside the prompts\n3. Reference in article with: ![description](illustration-{NN}.png)\n</code></pre>\n<h3>File Structure</h3>\n<p>Prompt files are saved in the same directory as the article:</p>\n<pre><code>{article-dir}/\n├── article.md\n├── outline.md\n├── illustration-01.md\n├── illustration-02.md\n└── refs/\n    └── ref-01-{slug}.png\n</code></pre>\n<p><strong>Filename conflict</strong>: If <code>illustration-{NN}.md</code> exists, append <code>-1</code>, <code>-2</code>, etc.</p>\n<h3>Modification</h3>\n<table>\n<thead>\n<tr>\n<th>Action</th>\n<th>Steps</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Edit</td>\n<td>Update prompt file → Regenerate image</td>\n</tr>\n<tr>\n<td>Add</td>\n<td>Position → Prompt → Insert reference</td>\n</tr>\n<tr>\n<td>Delete</td>\n<td>Delete files → Remove reference → Update outline</td>\n</tr>\n</tbody></table>\n<h3>References</h3>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Content</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"references/workflow.md\">references/workflow.md</a></td>\n<td>Detailed procedures</td>\n</tr>\n<tr>\n<td><a href=\"references/usage.md\">references/usage.md</a></td>\n<td>Command syntax</td>\n</tr>\n<tr>\n<td><a href=\"references/styles.md\">references/styles.md</a></td>\n<td>Style gallery</td>\n</tr>\n<tr>\n<td><a href=\"references/style-presets.md\">references/style-presets.md</a></td>\n<td>Preset shortcuts (type + style)</td>\n</tr>\n<tr>\n<td><a href=\"references/prompt-construction.md\">references/prompt-construction.md</a></td>\n<td>Prompt templates</td>\n</tr>\n<tr>\n<td><a href=\"references/config/first-time-setup.md\">references/config/first-time-setup.md</a></td>\n<td>First-time setup</td>\n</tr>\n</tbody></table>\n",
  "bilingual-release-changelog": "<h2>双语版本发布与 Changelog 维护</h2>\n<p>维护「中文为源、英文为译」的双语 changelog，并完成版本号更新与发布的完整流程。核心约定：<strong>中文永远是源语言，英文条目是中文的翻译</strong>，不独立撰写。</p>\n<h3>1. 确定变更范围</h3>\n<p>按场景选一种模式，逐 commit 看改动文件，不要只按 commit 标题猜：</p>\n<ul>\n<li><strong>发布导向（最常用）</strong>：上一个 tag 到 HEAD。<pre><code class=\"language-bash\">git tag --list | rg &#39;^v0\\.6\\.5$&#39;           # 确认基准 tag 存在\ngit log --oneline --decorate v0.6.5..HEAD\n# 逐 commit 查看涉及文件，判断用户可见性：\nfor c in $(git rev-list --reverse v0.6.5..HEAD); do\n  git show --no-patch --pretty=format:&#39;%h %s&#39; $c; echo\n  git show --name-only --pretty=format: $c | sed &#39;/^$/d&#39;; echo\ndone\n</code></pre>\n</li>\n<li><strong>Feature 分支导向</strong>：<code>git log main..HEAD --oneline</code> + <code>git diff main..HEAD --stat</code>。</li>\n<li><strong>按日期（少用）</strong>：<code>git log --since=&#39;... 00:00&#39; --until=&#39;... 23:59:59&#39; --oneline</code>。</li>\n</ul>\n<h3>2. 条目写作规范</h3>\n<ul>\n<li>Keep a Changelog 风格：<code>## [版本] - YYYY-MM-DD</code>，下分 <code>### 新增 / 变更 / 问题修复</code>（英文 <code>Added / Changed / Fixed</code>）。</li>\n<li>未发版内容先进 <code>## [未发布]</code> / <code>## [Unreleased]</code>；正式发版时落版本号+日期，发布日期与用户确认。</li>\n<li><strong>面向普通用户措辞</strong>：写功能点，不写代码更新说明；非技术人员要能读懂。</li>\n<li>对新功能的修复类 commit，<strong>按新功能介绍</strong>来写，不写成 fix。</li>\n<li>过滤纯内部重构、测试补充、依赖升级细节；docs/CI 提交是否收录属于边界项，可与用户确认。</li>\n</ul>\n<h3>3. 双语文件同步（最高频返工点）</h3>\n<ol>\n<li>先探测本项目的真源布局，<strong>不要假设路径</strong>：<pre><code class=\"language-bash\">rg --files -g &#39;CHANGELOG*&#39; -g &#39;changelog*&#39;\n</code></pre>\n常见布局：根 <code>CHANGELOG.md</code>(英) + <code>docs/zh/CHANGELOG.md</code>(中)；<code>CHANGELOG.md</code>(英) + <code>CHANGELOG_zh.md</code>(中)；文档站 <code>docs/zh/changelog/</code> + <code>docs/en/changelog/</code>；也可能只有中文单文件（内部项目）。注意历史文件名可能带 typo，沿用现状即可。</li>\n<li>列出<strong>全部</strong>目标文件清单（可能是 2-3 份），一次性同步更新，逐一确认。漏更 docs/en 或 docs/zh 是最常见的返工原因。</li>\n<li>先写中文条目，英文按中文翻译，遵循既有文件的格式与用词。</li>\n</ol>\n<h3>4. 版本号单一来源</h3>\n<p>按语言栈消除「改版本要动 N 处」的问题：</p>\n<ul>\n<li><strong>Python (uv + hatchling)</strong>：版本只写在 <code>src/&lt;pkg&gt;/__about__.py</code>；<code>__init__.py</code> 用 <code>from &lt;pkg&gt;.__about__ import __version__</code> 保留公开属性；<code>pyproject.toml</code> 用 <code>dynamic = [&quot;version&quot;]</code> + <code>[tool.hatch.version] path = &quot;src/&lt;pkg&gt;/__about__.py&quot;</code>。发布前校验 tag 版本 == <code>__about__</code> 版本。</li>\n<li><strong>Go</strong>：代码里 <code>var version = &quot;dev&quot;</code>，CI 发布时用 <code>-ldflags</code> 注入 Git tag；Git tag 是版本真源。</li>\n<li><strong>npm 多平台包</strong>：所有 <code>npm/*/package.json</code> 平时占位 <code>0.0.0-dev</code>，发布时脚本（如 <code>scripts/set-npm-version.mjs &lt;version&gt;</code>）一次性写入主包和全部平台包；平台包版本必须与主包严格一致，主包用 <code>optionalDependencies</code> 精确锁版本。</li>\n<li><strong>前端</strong>：<code>package.json</code> 为唯一源，构建时注入 UI 显示；版本号点击跳转对应语言的 changelog 页（新开标签）是惯用的产品化收尾。</li>\n</ul>\n<h3>5. 发布与 CI</h3>\n<ul>\n<li>发版前跑质量门禁（lint/test/覆盖率）。</li>\n<li><strong>PyPI</strong>：<code>vX.Y.Z</code> tag 触发，<code>uv build --no-sources</code> + <code>uv publish</code>（两步分离），发布 job 校验 tag 版本一致。workflow 命名 <code>pypi.yml</code> / <code>npm.yml</code>。</li>\n<li><strong>npm 跨平台原生分发</strong>：主包只放 JS 启动器，各平台二进制拆分为 <code>os</code>/<code>cpu</code> 匹配的平台包（<code>darwin-arm64</code>、<code>linux-x64</code>…）。上游不支持的平台<strong>不发假包</strong>，留扩展位即可。</li>\n<li><strong>Trusted Publishing (OIDC)</strong>：workflow 加 <code>id-token: write</code>；不要设置空的 <code>NODE_AUTH_TOKEN</code>（会导致 E401）。</li>\n<li><code>npm publish</code> 本地目录必须带 <code>./</code> 前缀（<code>npm publish ./npm/pkg-dir</code>），否则被解析为 package spec 推导成 Git 地址。</li>\n</ul>\n<h3>6. 发布失败恢复</h3>\n<ul>\n<li><strong>原则：tag 不可变，优先发新 patch 版本</strong>。rerun job 无效——Actions 按 tag 指向的旧 commit 取代码。</li>\n<li>坚持复用版本号时，删 tag 重打：<pre><code class=\"language-bash\">git tag -d v0.6.0\ngit push origin :refs/tags/v0.6.0\ngit tag v0.6.0 &lt;fixed-commit&gt;\ngit push origin v0.6.0\n</code></pre>\n前提是发布脚本具备幂等：先 <code>npm view &lt;name&gt;@&lt;version&gt;</code> 检查，已存在则 skip，支持安全重跑。</li>\n<li>排查半成功发布：用 <code>npm view &lt;pkg&gt;@&lt;ver&gt; version --json</code> 逐包探测实际发布状态，定位断点，不靠猜。</li>\n</ul>\n<h3>7. 收尾清单</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 所有 changelog 文件（中/英/文档站）已同步</li>\n<li><input disabled=\"\" type=\"checkbox\"> README.md / README_zh.md 按需更新（中文为源）；平台支持表、用法示例同步</li>\n<li><input disabled=\"\" type=\"checkbox\"> 版本号单一源已更新，其余位置为派生</li>\n<li><input disabled=\"\" type=\"checkbox\"> 项目内相关 SKILL/文档未落后于新能力</li>\n<li><input disabled=\"\" type=\"checkbox\"> lint/test 通过后再打 tag</li>\n</ul>\n",
  "claude-agent-sdk-typescript": "<h2>Claude Agent SDK TypeScript</h2>\n<p>Help users build AI agents with the Claude Agent SDK TypeScript version. This skill covers the <code>query()</code> function, message types, configuration options, subagents, hooks, MCP servers, custom tools, streaming, and structured outputs.</p>\n<hr>\n<h3>安装与设置</h3>\n<h4>安装 SDK</h4>\n<pre><code class=\"language-bash\">npm install @anthropic-ai/claude-agent-sdk\n</code></pre>\n<h4>环境变量</h4>\n<pre><code class=\"language-bash\"># 必需的 API Key\nexport ANTHROPIC_API_KEY=your-api-key\n\n# 可选：第三方 API 提供商\nexport CLAUDE_CODE_USE_BEDROCK=1    # Amazon Bedrock\nexport CLAUDE_CODE_USE_VERTEX=1     # Google Vertex AI\nexport CLAUDE_CODE_USE_FOUNDRY=1    # Microsoft Azure\n</code></pre>\n<h3>基础用法</h3>\n<h4>最简单的 Agent</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nfor await (const message of query({\n  prompt: &quot;What files are in this directory?&quot;,\n  options: { allowedTools: [&quot;Bash&quot;, &quot;Glob&quot;] }\n})) {\n  if (message.type === &quot;result&quot; &amp;&amp; message.subtype === &quot;success&quot;) {\n    console.log(message.result);\n  }\n}\n</code></pre>\n<h4>消息类型处理</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nfor await (const message of query({ prompt: &quot;Your task&quot;, options: {} })) {\n  switch (message.type) {\n    case &quot;system&quot;:\n      // 系统消息：session 初始化、compact_boundary\n      if (message.subtype === &quot;init&quot;) {\n        console.log(&quot;Session ID:&quot;, message.session_id);\n      }\n      break;\n\n    case &quot;assistant&quot;:\n      // Claude 的响应：包含文本和工具调用\n      for (const block of message.message.content) {\n        if (block.type === &quot;text&quot;) {\n          console.log(&quot;Claude:&quot;, block.text);\n        } else if (block.type === &quot;tool_use&quot;) {\n          console.log(&quot;Tool:&quot;, block.name);\n        }\n      }\n      break;\n\n    case &quot;user&quot;:\n      // 工具执行结果\n      console.log(&quot;Tool result received&quot;);\n      break;\n\n    case &quot;result&quot;:\n      // 最终结果\n      if (message.subtype === &quot;success&quot;) {\n        console.log(&quot;Done:&quot;, message.result);\n      } else {\n        console.log(&quot;Stopped:&quot;, message.subtype);\n      }\n      break;\n  }\n}\n</code></pre>\n<h3>配置选项</h3>\n<h4>核心选项</h4>\n<pre><code class=\"language-typescript\">const options = {\n  // 工具控制\n  allowedTools: [&quot;Read&quot;, &quot;Edit&quot;, &quot;Bash&quot;],      // 预批准的工具\n  disallowedTools: [&quot;Write&quot;],                   // 禁止的工具\n\n  // 权限模式\n  permissionMode: &quot;acceptEdits&quot;,                // &quot;default&quot; | &quot;acceptEdits&quot; | &quot;dontAsk&quot; | &quot;bypassPermissions&quot; | &quot;plan&quot;\n\n  // 限制\n  maxTurns: 30,                                 // 最大回合数\n  maxBudgetUsd: 5.0,                            // 最大预算（美元）\n\n  // 推理深度\n  effort: &quot;high&quot;,                               // &quot;low&quot; | &quot;medium&quot; | &quot;high&quot; | &quot;max&quot;\n\n  // 模型\n  model: &quot;claude-sonnet-4-6&quot;,                   // 指定模型\n\n  // 系统提示\n  systemPrompt: &quot;You are a senior TypeScript developer.&quot;,\n\n  // 设置源\n  settingSources: [&quot;project&quot;],                  // 加载 .claude/CLAUDE.md, skills\n};\n</code></pre>\n<h4>权限模式对比</h4>\n<table>\n<thead>\n<tr>\n<th>模式</th>\n<th>行为</th>\n<th>适用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>default</code></td>\n<td>需要 <code>canUseTool</code> 回调处理未批准的工具</td>\n<td>交互式应用</td>\n</tr>\n<tr>\n<td><code>acceptEdits</code></td>\n<td>自动批准文件编辑，其他工具需审批</td>\n<td>开发工作流</td>\n</tr>\n<tr>\n<td><code>dontAsk</code></td>\n<td>拒绝未在 <code>allowedTools</code> 中列出的工具</td>\n<td>受限的自动化</td>\n</tr>\n<tr>\n<td><code>bypassPermissions</code></td>\n<td>运行所有工具（谨慎使用！）</td>\n<td>CI/CD、沙箱环境</td>\n</tr>\n<tr>\n<td><code>plan</code></td>\n<td>只规划，不执行</td>\n<td>代码审查</td>\n</tr>\n</tbody></table>\n<h3>会话管理</h3>\n<h4>自动继续会话</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\n// 第一次查询\nfor await (const message of query({\n  prompt: &quot;Analyze the auth module&quot;,\n  options: { allowedTools: [&quot;Read&quot;, &quot;Glob&quot;] }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n\n// 继续同一会话\nfor await (const message of query({\n  prompt: &quot;Now refactor it to use JWT&quot;,\n  options: {\n    continue: true,  // 自动继续最近的会话\n    allowedTools: [&quot;Read&quot;, &quot;Edit&quot;, &quot;Write&quot;]\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>按 ID 恢复会话</h4>\n<pre><code class=\"language-typescript\">let sessionId: string | undefined;\n\n// 第一次查询，保存 session ID\nfor await (const message of query({\n  prompt: &quot;Analyze the auth module&quot;,\n  options: { allowedTools: [&quot;Read&quot;, &quot;Glob&quot;] }\n})) {\n  if (message.type === &quot;system&quot; &amp;&amp; message.subtype === &quot;init&quot;) {\n    sessionId = message.session_id;\n  }\n  if (message.type === &quot;result&quot;) {\n    console.log(message.result);\n  }\n}\n\n// 之后恢复特定会话\nfor await (const message of query({\n  prompt: &quot;Continue with the refactoring&quot;,\n  options: { resume: sessionId }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>分叉会话</h4>\n<pre><code class=\"language-typescript\">// 从现有会话创建分支\nlet forkedId: string | undefined;\n\nfor await (const message of query({\n  prompt: &quot;Try OAuth2 instead of JWT&quot;,\n  options: {\n    resume: sessionId,\n    forkSession: true  // 创建分支，原会话不受影响\n  }\n})) {\n  if (message.type === &quot;system&quot; &amp;&amp; message.subtype === &quot;init&quot;) {\n    forkedId = message.session_id;  // 新分支的 ID\n  }\n}\n</code></pre>\n<h3>子代理</h3>\n<h4>定义子代理</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nfor await (const message of query({\n  prompt: &quot;Review the authentication module&quot;,\n  options: {\n    allowedTools: [&quot;Read&quot;, &quot;Grep&quot;, &quot;Glob&quot;, &quot;Agent&quot;],  // Agent 工具必需\n    agents: {\n      &quot;code-reviewer&quot;: {\n        description: &quot;Expert code reviewer. Use for quality and security reviews.&quot;,\n        prompt: `You are a code review specialist with expertise in security, performance, and best practices.\n\nWhen reviewing code:\n- Identify security vulnerabilities\n- Check for performance issues\n- Verify adherence to coding standards\n- Suggest specific improvements\n\nBe thorough but concise in your feedback.`,\n        tools: [&quot;Read&quot;, &quot;Grep&quot;, &quot;Glob&quot;],  // 只读权限\n        model: &quot;sonnet&quot;  // 可选：覆盖默认模型\n      },\n      &quot;test-runner&quot;: {\n        description: &quot;Runs and analyzes test suites.&quot;,\n        prompt: &quot;You are a test execution specialist. Run tests and provide clear analysis of results.&quot;,\n        tools: [&quot;Bash&quot;, &quot;Read&quot;, &quot;Grep&quot;]\n      }\n    }\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>AgentDefinition 配置</h4>\n<table>\n<thead>\n<tr>\n<th>字段</th>\n<th>类型</th>\n<th>必需</th>\n<th>描述</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>description</code></td>\n<td><code>string</code></td>\n<td>是</td>\n<td>何时使用此代理的自然语言描述</td>\n</tr>\n<tr>\n<td><code>prompt</code></td>\n<td><code>string</code></td>\n<td>是</td>\n<td>定义代理角色和行为的系统提示</td>\n</tr>\n<tr>\n<td><code>tools</code></td>\n<td><code>string[]</code></td>\n<td>否</td>\n<td>允许的工具列表。省略则继承所有工具</td>\n</tr>\n<tr>\n<td><code>model</code></td>\n<td><code>&#39;sonnet&#39; | &#39;opus&#39; | &#39;haiku&#39; | &#39;inherit&#39;</code></td>\n<td>否</td>\n<td>模型覆盖</td>\n</tr>\n</tbody></table>\n<h3>MCP 服务器</h3>\n<h4>stdio 传输</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nfor await (const message of query({\n  prompt: &quot;List recent GitHub issues&quot;,\n  options: {\n    mcpServers: {\n      github: {\n        command: &quot;npx&quot;,\n        args: [&quot;-y&quot;, &quot;@modelcontextprotocol/server-github&quot;],\n        env: { GITHUB_TOKEN: process.env.GITHUB_TOKEN }\n      }\n    },\n    allowedTools: [&quot;mcp__github__list_issues&quot;, &quot;mcp__github__search_issues&quot;]\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>HTTP/SSE 传输</h4>\n<pre><code class=\"language-typescript\">for await (const message of query({\n  prompt: &quot;Search the docs&quot;,\n  options: {\n    mcpServers: {\n      &quot;docs-server&quot;: {\n        type: &quot;http&quot;,\n        url: &quot;https://code.claude.com/docs/mcp&quot;\n      }\n    },\n    allowedTools: [&quot;mcp__docs-server__*&quot;]  // 通配符允许所有工具\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>检查 MCP 连接状态</h4>\n<pre><code class=\"language-typescript\">for await (const message of query({ prompt: &quot;...&quot;, options })) {\n  if (message.type === &quot;system&quot; &amp;&amp; message.subtype === &quot;init&quot;) {\n    const failedServers = message.mcp_servers?.filter(\n      s =&gt; s.status !== &quot;connected&quot;\n    );\n    if (failedServers?.length &gt; 0) {\n      console.warn(&quot;Failed to connect:&quot;, failedServers);\n    }\n  }\n}\n</code></pre>\n<h3>Hooks</h3>\n<h4>PreToolUse Hook（阻止危险操作）</h4>\n<pre><code class=\"language-typescript\">import { query, HookCallback } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nconst protectEnvFiles: HookCallback = async (input, toolUseID, { signal }) =&gt; {\n  const preInput = input as any;\n  const toolInput = preInput.tool_input as Record&lt;string, any&gt;;\n  const filePath = toolInput?.file_path as string;\n  const fileName = filePath?.split(&quot;/&quot;).pop();\n\n  if (fileName === &quot;.env&quot;) {\n    return {\n      hookSpecificOutput: {\n        hookEventName: preInput.hook_event_name,\n        permissionDecision: &quot;deny&quot;,\n        permissionDecisionReason: &quot;Cannot modify .env files&quot;\n      }\n    };\n  }\n  return {};\n};\n\nfor await (const message of query({\n  prompt: &quot;Update the database configuration&quot;,\n  options: {\n    hooks: {\n      PreToolUse: [{ matcher: &quot;Write|Edit&quot;, hooks: [protectEnvFiles] }]\n    }\n  }\n})) {\n  console.log(message);\n}\n</code></pre>\n<h4>PostToolUse Hook（审计日志）</h4>\n<pre><code class=\"language-typescript\">import { query, HookCallback } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\nimport { appendFile } from &quot;fs/promises&quot;;\n\nconst logFileChange: HookCallback = async (input, toolUseID, { signal }) =&gt; {\n  const postInput = input as any;\n  const filePath = postInput.tool_input?.file_path ?? &quot;unknown&quot;;\n  await appendFile(&quot;./audit.log&quot;, `${new Date().toISOString()}: modified ${filePath}\\n`);\n  return {};\n};\n\nfor await (const message of query({\n  prompt: &quot;Refactor utils.ts&quot;,\n  options: {\n    permissionMode: &quot;acceptEdits&quot;,\n    hooks: {\n      PostToolUse: [{ matcher: &quot;Edit|Write&quot;, hooks: [logFileChange] }]\n    }\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h4>可用的 Hooks</h4>\n<table>\n<thead>\n<tr>\n<th>Hook</th>\n<th>Python</th>\n<th>TypeScript</th>\n<th>触发时机</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>PreToolUse</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>工具调用前（可阻止/修改）</td>\n</tr>\n<tr>\n<td><code>PostToolUse</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>工具执行后</td>\n</tr>\n<tr>\n<td><code>PostToolUseFailure</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>工具执行失败</td>\n</tr>\n<tr>\n<td><code>UserPromptSubmit</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>用户提交提示时</td>\n</tr>\n<tr>\n<td><code>Stop</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>Agent 停止时</td>\n</tr>\n<tr>\n<td><code>SubagentStart</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>子代理启动</td>\n</tr>\n<tr>\n<td><code>SubagentStop</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>子代理完成</td>\n</tr>\n<tr>\n<td><code>PreCompact</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>上下文压缩前</td>\n</tr>\n<tr>\n<td><code>PermissionRequest</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>显示权限对话框时</td>\n</tr>\n<tr>\n<td><code>SessionStart</code></td>\n<td>✗</td>\n<td>✓</td>\n<td>会话初始化</td>\n</tr>\n<tr>\n<td><code>SessionEnd</code></td>\n<td>✗</td>\n<td>✓</td>\n<td>会话终止</td>\n</tr>\n<tr>\n<td><code>Notification</code></td>\n<td>✓</td>\n<td>✓</td>\n<td>状态通知</td>\n</tr>\n</tbody></table>\n<h3>自定义工具</h3>\n<h4>创建自定义工具</h4>\n<pre><code class=\"language-typescript\">import { query, tool, createSdkMcpServer } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\nimport { z } from &quot;zod&quot;;\n\nconst weatherServer = createSdkMcpServer({\n  name: &quot;weather-tools&quot;,\n  version: &quot;1.0.0&quot;,\n  tools: [\n    tool(\n      &quot;get_weather&quot;,\n      &quot;Get current temperature for a location&quot;,\n      {\n        latitude: z.number().describe(&quot;Latitude coordinate&quot;),\n        longitude: z.number().describe(&quot;Longitude coordinate&quot;)\n      },\n      async (args) =&gt; {\n        const response = await fetch(\n          `https://api.open-meteo.com/v1/forecast?latitude=${args.latitude}&amp;longitude=${args.longitude}&amp;current=temperature_2m`\n        );\n        const data = await response.json();\n        return {\n          content: [{\n            type: &quot;text&quot;,\n            text: `Temperature: ${data.current.temperature_2m}°C`\n          }]\n        };\n      }\n    )\n  ]\n});\n\n// 使用流式输入\nasync function* generateMessages() {\n  yield {\n    type: &quot;user&quot; as const,\n    message: {\n      role: &quot;user&quot; as const,\n      content: &quot;What&#39;s the weather in San Francisco?&quot;\n    }\n  };\n}\n\nfor await (const message of query({\n  prompt: generateMessages(),\n  options: {\n    mcpServers: { &quot;weather-tools&quot;: weatherServer },\n    allowedTools: [&quot;mcp__weather-tools__get_weather&quot;]\n  }\n})) {\n  if (message.type === &quot;result&quot;) console.log(message.result);\n}\n</code></pre>\n<h3>流式响应</h3>\n<h4>启用流式输出</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nfor await (const message of query({\n  prompt: &quot;Explain how databases work&quot;,\n  options: {\n    includePartialMessages: true,\n    allowedTools: [&quot;Bash&quot;, &quot;Read&quot;]\n  }\n})) {\n  if (message.type === &quot;stream_event&quot;) {\n    const event = message.event;\n    if (event.type === &quot;content_block_delta&quot; &amp;&amp; event.delta.type === &quot;text_delta&quot;) {\n      process.stdout.write(event.delta.text);\n    }\n  }\n}\n</code></pre>\n<h4>流式事件类型</h4>\n<table>\n<thead>\n<tr>\n<th>事件类型</th>\n<th>描述</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>message_start</code></td>\n<td>新消息开始</td>\n</tr>\n<tr>\n<td><code>content_block_start</code></td>\n<td>内容块开始（文本或工具使用）</td>\n</tr>\n<tr>\n<td><code>content_block_delta</code></td>\n<td>增量内容更新</td>\n</tr>\n<tr>\n<td><code>content_block_stop</code></td>\n<td>内容块结束</td>\n</tr>\n<tr>\n<td><code>message_delta</code></td>\n<td>消息级更新（停止原因、用量）</td>\n</tr>\n<tr>\n<td><code>message_stop</code></td>\n<td>消息结束</td>\n</tr>\n</tbody></table>\n<h3>结构化输出</h3>\n<h4>JSON Schema 方式</h4>\n<pre><code class=\"language-typescript\">import { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nconst schema = {\n  type: &quot;object&quot;,\n  properties: {\n    company_name: { type: &quot;string&quot; },\n    founded_year: { type: &quot;number&quot; },\n    headquarters: { type: &quot;string&quot; }\n  },\n  required: [&quot;company_name&quot;]\n};\n\nfor await (const message of query({\n  prompt: &quot;Research Anthropic and provide key company information&quot;,\n  options: {\n    outputFormat: { type: &quot;json_schema&quot;, schema }\n  }\n})) {\n  if (message.type === &quot;result&quot; &amp;&amp; message.structured_output) {\n    console.log(message.structured_output);\n    // { company_name: &quot;Anthropic&quot;, founded_year: 2021, headquarters: &quot;San Francisco&quot; }\n  }\n}\n</code></pre>\n<h4>Zod 方式（推荐）</h4>\n<pre><code class=\"language-typescript\">import { z } from &quot;zod&quot;;\nimport { query } from &quot;@anthropic-ai/claude-agent-sdk&quot;;\n\nconst FeaturePlan = z.object({\n  feature_name: z.string(),\n  summary: z.string(),\n  steps: z.array(z.object({\n    step_number: z.number(),\n    description: z.string(),\n    estimated_complexity: z.enum([&quot;low&quot;, &quot;medium&quot;, &quot;high&quot;])\n  })),\n  risks: z.array(z.string())\n});\n\ntype FeaturePlan = z.infer&lt;typeof FeaturePlan&gt;;\n\nfor await (const message of query({\n  prompt: &quot;Plan how to add dark mode support to a React app&quot;,\n  options: {\n    outputFormat: {\n      type: &quot;json_schema&quot;,\n      schema: z.toJSONSchema(FeaturePlan)\n    }\n  }\n})) {\n  if (message.type === &quot;result&quot; &amp;&amp; message.structured_output) {\n    const parsed = FeaturePlan.safeParse(message.structured_output);\n    if (parsed.success) {\n      const plan: FeaturePlan = parsed.data;\n      console.log(`Feature: ${plan.feature_name}`);\n      plan.steps.forEach(step =&gt; {\n        console.log(`${step.step_number}. [${step.estimated_complexity}] ${step.description}`);\n      });\n    }\n  }\n}\n</code></pre>\n<h3>处理结果</h3>\n<h4>结果子类型</h4>\n<pre><code class=\"language-typescript\">for await (const message of query({ prompt: &quot;...&quot;, options: {} })) {\n  if (message.type === &quot;result&quot;) {\n    switch (message.subtype) {\n      case &quot;success&quot;:\n        console.log(&quot;Result:&quot;, message.result);\n        console.log(&quot;Cost:&quot;, message.total_cost_usd);\n        break;\n      case &quot;error_max_turns&quot;:\n        console.log(&quot;Hit turn limit. Resume session to continue.&quot;);\n        break;\n      case &quot;error_max_budget_usd&quot;:\n        console.log(&quot;Hit budget limit.&quot;);\n        break;\n      case &quot;error_during_execution&quot;:\n        console.log(&quot;Execution error occurred.&quot;);\n        break;\n      case &quot;error_max_structured_output_retries&quot;:\n        console.log(&quot;Failed to generate valid structured output.&quot;);\n        break;\n    }\n  }\n}\n</code></pre>\n<h3>参考文档</h3>\n<ul>\n<li><a href=\"./references/quickstart.md\">快速开始指南</a></li>\n<li><a href=\"./references/agent-loop.md\">Agent 循环详解</a></li>\n<li><a href=\"./references/sessions.md\">会话管理</a></li>\n<li><a href=\"./references/subagents.md\">子代理</a></li>\n<li><a href=\"./references/hooks.md\">Hooks 系统</a></li>\n<li><a href=\"./references/mcp.md\">MCP 服务器集成</a></li>\n<li><a href=\"./references/custom-tools.md\">自定义工具</a></li>\n<li><a href=\"./references/streaming.md\">流式响应</a></li>\n<li><a href=\"./references/structured-outputs.md\">结构化输出</a></li>\n<li><a href=\"./references/permissions.md\">权限配置</a></li>\n</ul>\n",
  "cloudflare-deploy-migration": "<h2>Cloudflare 部署与 Vercel 迁移</h2>\n<p>按站点复杂度选三条路径：纯静态 → Pages；Next.js → vinext + Workers；带后端 → Workers + D1/KV/DO。</p>\n<h3>1. 纯静态站 → Cloudflare Pages（Direct Upload）</h3>\n<pre><code class=\"language-bash\">npx wrangler login\nwrangler pages deploy dist --project-name=&lt;name&gt;          # 必须传构建产物目录，不要传 &quot;.&quot;\nwrangler pages deploy web --project-name=&lt;name&gt; --commit-dirty=true   # 工作区脏时\nwrangler pages deploy web --project-name=&lt;name&gt; --branch=preview      # 预览分支\n</code></pre>\n<ul>\n<li>无服务端逻辑用 Pages；需要边缘逻辑才用 Workers static assets。</li>\n<li><code>vercel.json</code> 的 SPA rewrite/headers 迁到 <code>public/_redirects</code>（<code>/* /index.html 200</code>）+ <code>public/_headers</code>，构建后自动进 dist。</li>\n<li>移除 <code>@vercel/analytics</code> 依赖与 <code>&lt;Analytics /&gt;</code> 组件。</li>\n<li><code>.wrangler/</code> 是本地缓存，进 <code>.gitignore</code>。</li>\n</ul>\n<h3>2. Next.js → vinext + CF Workers</h3>\n<p>vinext（Cloudflare 官方，next CLI 的 drop-in replacement）：<code>vinext check</code>（兼容评分）→ <code>vinext init</code> → 脚本 <code>next dev/build/start → vinext dev/build/start</code>。</p>\n<p><strong>关键经验：仓库已有自定义 <code>vite.config.ts</code>/<code>wrangler.jsonc</code> 时，放弃 <code>vinext deploy</code> 的自动配置，改手工链路</strong>——否则自动配置与手写配置打架，产出 assets-only Worker 导致全站 404：</p>\n<pre><code class=\"language-bash\">mv wrangler.jsonc wrangler.jsonc.bak   # 绕过 vinext build 的插件强检查\nbun run build                          # 产出 dist/client + dist/server/entry.js\nmv wrangler.jsonc.bak wrangler.jsonc\nrm -f .wrangler/deploy/config.json     # 清理过期重定向缓存\nbunx wrangler deploy --dry-run &amp;&amp; bunx wrangler deploy\n</code></pre>\n<p><code>worker/index.ts</code> 从真实产物 <code>import ... from &quot;../dist/server/entry.js&quot;</code>；<code>wrangler.jsonc</code> 手写 <code>main</code> + <code>assets.directory</code> + <code>compatibility_flags: [&quot;nodejs_compat&quot;]</code>。</p>\n<p>SSR 最大成本是 CJS/ESM 互操作：CJS 叶子依赖收敛进 <code>vite.config.ts</code> 的 <code>ssr.optimizeDeps</code>（<strong>不要放含 React 的包本体</strong>，会拉出第二份 React 触发 invalid hook call）；<code>_app</code>/<code>_document</code> 改函数式。</p>\n<h3>3. 带后端应用 → Workers + D1/KV/DO</h3>\n<p>单 Worker（Hono）+ <code>[assets]</code> 托管前端。基础配置（用新式 <code>[assets]</code>，不是旧 <code>[site]</code>）：</p>\n<pre><code class=\"language-toml\">name = &quot;app&quot;\ncompatibility_date = &quot;2026-03-17&quot;\nmain = &quot;dist/server.js&quot;\n[assets]\ndirectory = &quot;dist/client&quot;\nnot_found_handling = &quot;single-page-application&quot;\n</code></pre>\n<ul>\n<li><strong>配置文件拆分</strong>：<code>wrangler.toml</code>（dev 默认，本地 KV/D1）/ <code>wrangler.deploy.toml</code>（生产真实资源 ID）/ <code>wrangler.e2e.toml</code>（E2E）。<strong>彼此没有继承关系</strong>，wrangler 只读 <code>--config</code> 指定的那一个；代价是易漂移，需人为保持同步。</li>\n<li><strong>D1 上线</strong>：<code>wrangler d1 create &lt;db&gt;</code> 拿 id 回填 → <code>wrangler d1 migrations apply &lt;database_name&gt; --remote</code>。坑：位置参数是 <strong>database_name 不是 binding 名</strong>。先迁移再部署 Worker。</li>\n<li><strong>Durable Objects</strong> 无需 create：声明 <code>[[durable_objects.bindings]]</code> + <code>[[migrations]]</code>（<code>new_sqlite_classes</code>），随 deploy 注册；class 名上线后保持稳定。</li>\n<li><strong>bundle 守卫</strong>：单文件 server bundle 残留分包引用会运行时报 <code>No such module</code>——加 verify-server-bundle 脚本进 build 链。</li>\n</ul>\n<h3>4. 变量与 Secret 三分法</h3>\n<ul>\n<li>前端构建时变量 <code>VITE_*</code> → Vite <code>.env.production</code>（本就浏览器可见，绝不进 secret）。</li>\n<li>Worker 运行时非敏感 → <code>[vars]</code>（<strong>当 <code>.env</code> 写，值统一字符串</strong>；wrangler 自身字段才用原生类型）。</li>\n<li>Worker 运行时敏感 → <code>wrangler secret put &lt;key&gt;</code> / <code>--secrets-file &lt;file&gt;</code>（文件进 <code>.gitignore</code> 并附 <code>.example</code>）。本地开发用 <code>.dev.vars</code>。</li>\n<li>secret 与 var 运行时无差别，区别只是 Dashboard 不回显；同一 key 不要两边都配。</li>\n</ul>\n<h3>5. CI 与 E2E</h3>\n<ul>\n<li>test workflow：<code>build</code> 与 <code>test_e2e</code> 独立 job，artifact 传产物；<strong>上传/下载两端都必须 <code>path: dist</code></strong>（<code>dist/**</code> 会多嵌层、<code>.</code> 会丢层，与 <code>main = &quot;dist/server.js&quot;</code> 的契约对不上）。加 <code>concurrency</code> + <code>cancel-in-progress</code>。</li>\n<li><strong>E2E 在 wrangler 运行时上跑（HOME 隔离）</strong>：<pre><code class=\"language-bash\">HOME=&quot;$PWD/.wrangler-home&quot; wrangler dev --config wrangler.e2e.toml \\\n  --local --no-bundle --persist-to .wrangler/state/e2e \\\n  --ip 127.0.0.1 --port 3000 --inspector-port 0 --log-level error\n</code></pre>\nwrangler 默认写 <code>~/.config/.wrangler</code> 全局状态，CI 不可控；HOME 重定向进仓库内目录，<code>--local --no-bundle</code> 直接吃已构建产物。</li>\n</ul>\n<h3>6. Analytics 替换</h3>\n<p>只改静态入口 HTML 的 <code>&lt;head&gt;</code>，不引 SDK：</p>\n<ul>\n<li>CF Web Analytics：<code>&lt;script defer src=&#39;https://static.cloudflareinsights.com/beacon.min.js&#39; data-cf-beacon=&#39;{&quot;token&quot;: &quot;&lt;每站不同&gt;&quot;}&#39;&gt;&lt;/script&gt;</code></li>\n<li>umami：<code>&lt;script defer src=&quot;https://cloud.umami.is/script.js&quot; data-website-id=&quot;...&quot;&gt;</code></li>\n<li>两者不要并存（重复统计）。</li>\n</ul>\n<h3>7. 根因速查表</h3>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>根因</th>\n<th>解法</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>刷新页面中文乱码（SPA 路由正常）</td>\n<td>SSR 响应 <code>Content-Type: text/html</code> 缺 <code>charset=utf-8</code>，浏览器猜错编码</td>\n<td>Worker 里对 page render 响应补 charset（保留 Set-Cookie，不误伤 /api、图片、xml）；不要去改 React 树</td>\n</tr>\n<tr>\n<td>部署后全站 404，<code>wrangler tail</code> 报 assets-only</td>\n<td>产物配置只有 assets 没有 main</td>\n<td>手工 build + deploy 链路（见 §2）</td>\n</tr>\n<tr>\n<td><code>Couldn&#39;t find DB with name &#39;X&#39;</code></td>\n<td>D1 migrations 传了 binding 名</td>\n<td>传 database_name + <code>--remote</code></td>\n</tr>\n<tr>\n<td>CI 报 entry-point not found</td>\n<td>artifact 上传/下载路径嵌层不一致</td>\n<td>两端都 <code>path: dist</code></td>\n</tr>\n<tr>\n<td>运行时 <code>No such module &quot;X-*.js&quot;</code></td>\n<td>server bundle 残留分包引用</td>\n<td>收敛服务端打包 + bundle 守卫脚本</td>\n</tr>\n</tbody></table>\n<h3>8. 选型参考（免费额度）</h3>\n<p>CF Free：静态请求不限量、Workers/Functions 10 万次/天。Vercel Hobby：Function 100 万次/月、可配函数区域。纯静态内容站 → CF 更划算；重函数全栈或需函数区域可控 → Vercel。</p>\n",
  "code-review-checklist": "<h2>Code Review Checklist</h2>\n<p>做 code review 时按本清单执行。好的审查关注可维护性与可扩展性，不是纠语法、风格、缩进——那些交给 formatter。审查是迭代过程，不是一次性盖章。</p>\n<h3>分层审查顺序</h3>\n<p>按顺序走，先大局后细节。<strong>第一遍只看设计，不钻细节。</strong></p>\n<h4>第 1 层：大局 / 设计</h4>\n<p>问自己（这些更关乎系统设计，而非改动本身）：</p>\n<ul>\n<li>这段代码如何融入系统其余部分？与代码库其他部分怎么交互？</li>\n<li>它如何影响整体架构？会影响未来计划中的工作吗？</li>\n<li>这次改动解决了什么问题？可能引入什么未来问题？</li>\n<li>随着代码演进，有没有更好的抽象？</li>\n</ul>\n<p>原则：接受了糟糕的改动，系统会变脆。文档、测试、数据类型与代码本身同等重要。</p>\n<h4>第 2 层：调用点与「没被改的行」</h4>\n<ul>\n<li><strong>看那些<em>没被</em>改动的行</strong>——它们常常讲述真实的故事。改动者经常忘了同步更新代码库或文档的相关部分，导致 bug、破坏性变更或安全问题。</li>\n<li>彻底检查新代码的<strong>所有 call-sites</strong>：都正确更新了吗？</li>\n<li>测试还在测正确的东西吗？改动放对地方了吗？</li>\n</ul>\n<h4>第 3 层：命名（往往是最重要的一环）</h4>\n<p>名字封装概念，是代码的「构建块」。坏名字是暗示深层问题的 code smell，会把认知负担放大一个数量级。花时间为变量想最贴切的名字，即使感觉像吹毛求疵。</p>\n<p>坏（临时/随意名，看不出用途）：</p>\n<pre><code class=\"language-rust\">let usr = player.username.trim().to_lowercase();\nlet updated_score = player.score + bonus_points;\nlet l = if level_up { player.level + 1 } else { player.level };\nlet l2 = if l &gt; 100 { 100 } else { l };\n</code></pre>\n<p>好（名字描述每一步的含义，用 shadowing 表达意图，最终名字常与字段名对齐）：</p>\n<pre><code class=\"language-rust\">let username = player.username.trim().to_lowercase();\nlet score = player.score + bonus_points;\nlet level = if level_up { player.level + 1 } else { player.level };\nlet level = if level &gt; 100 { 100 } else { level };\n</code></pre>\n<p>命名在大型代码库中尤其关键——值的声明与使用相隔很远，多人需要对问题领域有共同理解。</p>\n<h4>第 4 层：亲自跑代码</h4>\n<p>长时间盯着代码容易漏掉微妙细节。有条件就 checkout 分支，运行代码、测试、linter；到处改改、试着弄坏它、理解它如何工作。UI 变更、错误消息这类面向用户的改动，跑起来并尝试破坏时最容易发现问题。之后 revert，把发现写进评论。</p>\n<h3>该跳过什么</h3>\n<ul>\n<li><strong>别吹毛求疵。</strong> 空白、格式交给 formatter。把精力留给真正重要的：逻辑、设计、可维护性、正确性。</li>\n<li>判断标准：<strong>这会影响功能吗？会让未来的开发者困惑吗？</strong> 若都不会，放手。</li>\n<li>避免不影响代码质量的主观偏好之争。</li>\n</ul>\n<h3>反馈怎么说（沟通 &gt; 挑错）</h3>\n<p>审查也是在团队内建立对代码的共同理解，不只是指出缺陷。</p>\n<ul>\n<li><strong>专注 why，而非 how。</strong> 解释改动背后的理由，比无理由地指出缺陷更可能被接受。别只写 &quot;Don&#39;t do this&quot;；给出替代方案、链接文档、说明为何这样会在未来出问题。</li>\n<li><strong>别当混蛋。</strong> 不说 &quot;this is wrong&quot;，说 &quot;I would do it this way&quot;。只留下你自己乐于收到的评论。</li>\n<li><strong>用苏格拉底式提问</strong>引导作者自己思考，往往能带来更好的设计：<ul>\n<li>&quot;Will this break existing workflows if we do it this way?&quot;</li>\n<li>&quot;Which alternatives have you considered?&quot;</li>\n<li>&quot;What happens if you call this function with an empty array?&quot;</li>\n<li>&quot;If I don&#39;t set this value, what error message does the user see?&quot;</li>\n</ul>\n</li>\n<li><strong>别怕问「蠢」问题。</strong> 不懂就问，你多半不是唯一看不懂的人——这可能暴露出缺失的文档或不自解释的系统。</li>\n<li>适当加正面评论（&quot;I like this&quot;、&quot;great idea&quot;），维持作者积极性。</li>\n<li>拒绝的是代码，不是人。</li>\n</ul>\n<h3>何时说「不」</h3>\n<ul>\n<li>与其合并一个不对、会埋下隐患的改动，不如直接拒绝。&quot;先合并以后再修&quot; 是滑坡，会累积技术债。</li>\n<li>拒绝时别粉饰、别硬装友好：客观、解释理由、给更好的替代方案，然后聚焦下一步。</li>\n<li>若你对同一件事反复说「不」，把它写成 style guide 或团队指南，用事实支撑直觉。</li>\n<li>宽容但果断——这只是代码。</li>\n</ul>\n<h3>流程习惯</h3>\n<ul>\n<li><strong>多轮迭代。</strong> 别指望一次过；第一轮看设计，之后才进细节。目标是接受高质量代码，不是尽快合并。</li>\n<li><strong>明确你的可用性。</strong> review 是开发瓶颈（有 human in the loop）。没时间审、或短期内审不完，主动告知作者，设清预期。</li>\n<li><strong>把 review 当学习。</strong> 每次审查争取学一样新东西——新技巧、模式、库，以及别人如何解决问题。</li>\n<li><strong>偶尔请作者反馈你的反馈</strong>：太苛刻/吹毛求疵/慢/马虎了吗？指出的点对吗？对他们有帮助吗？</li>\n</ul>\n<h3>一次审查的执行清单</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 第一遍先评估设计与架构契合度，未钻语法细节</li>\n<li><input disabled=\"\" type=\"checkbox\"> 检查了所有 call-sites 与未改动但相关的行、文档、测试</li>\n<li><input disabled=\"\" type=\"checkbox\"> 审查了命名，标出会增加认知负担的坏名字</li>\n<li><input disabled=\"\" type=\"checkbox\"> 有条件时 checkout 并实际运行了代码 / 测试 / linter</li>\n<li><input disabled=\"\" type=\"checkbox\"> 跳过了 formatter 能处理的格式/空白问题</li>\n<li><input disabled=\"\" type=\"checkbox\"> 反馈聚焦 why、给出替代方案、措辞是我自己乐于收到的</li>\n<li><input disabled=\"\" type=\"checkbox\"> 需要时用苏格拉底式提问代替断言</li>\n<li><input disabled=\"\" type=\"checkbox\"> 若改动不达标，明确而客观地说「不」并给替代方案</li>\n</ul>\n",
  "codebase-review-loop": "<h2>代码库审查与改进循环</h2>\n<p>以仓库内一套<strong>编号改进提案文档</strong>（Improvement Proposal，下称「提案」，存放于 <code>proposals/</code> 之类的专用目录，目录名与前缀可按项目惯例自定）为载体，形成闭环：<strong>多维度审查生成带证据的 findings → 只读核查 finding 是否仍成立（对齐真源）→ 一次修一条 → 验证门禁 → 回写状态 → 提 PR</strong>。</p>\n<h3>1. 文档格式</h3>\n<ul>\n<li>命名：<code>proposal-{N}-{english-slug}.md</code>，N 单调递增可跳号；一级标题与文件名一致；子专题放子目录（如 <code>proposal-6-evaluation/</code> 下分 architecture/performance/security 等维度文档）。</li>\n<li>Frontmatter 强制：<code>Author</code>（agent 或用户名，如 <code>&quot;@alice&quot;</code>）、<code>Created</code>、<code>Updated</code>、<code>Status</code>（Draft → Almost Complete → Complete）；完成态可加 <code>Reviewer</code>、<code>ClosedDate</code>、<code>Origin</code>（从父提案拆出时注明）。</li>\n<li>Codebase Review 类骨架：<code>背景 / 现状 / 目标 / 非目标 / 发现与方案(P0/P1/P2) / 建议落地顺序</code>。</li>\n</ul>\n<p><strong>Finding 写法</strong>（每条一个 <code>###</code>，带优先级前缀）：</p>\n<pre><code class=\"language-markdown\">### P0. 后端应用装配与领域逻辑未分层\n#### 证据            # 文件路径+行号/规模，可被直接核验\n#### 为什么不合理     # 影响论证\n#### 建议拆分边界     # 最小方案\n#### 暂不建议做的事   # 显式排除，防过度设计\n</code></pre>\n<p>修复后原地追加：<code>#### 实施结果（日期）</code>、<code>#### 已完成解决方案</code>、<code>#### 保持不变的边界</code>。跟踪区用 checklist：<code>- [x]</code> 完成、<code>- [~]</code> 移出，行内标注 <code>-&gt; 完成</code> / <code>-&gt; 不考虑</code> / <code>-&gt; 不存在这个问题</code>。</p>\n<p><strong>写作硬约束</strong>：每条现状断言必须引真实文件路径/函数（grounded in code），未验证的写「待确认」，绝不编造；已决定与未决定分段；每篇一个决策边界，scope creep 甩给未来提案；正文语言遵循项目惯例、标识符/路径保持原文；不写激励性废话。</p>\n<p>反模式：现状结论混入愿景、非目标写成模糊排除、提案里塞实现代码（类型/JSON 示例可以）、「待讨论」当垃圾桶、协议/存储变更忘记向后兼容。</p>\n<h3>2. 生成审查（写提案）</h3>\n<ul>\n<li>审查前先回看已有提案，<strong>不重复登记已修问题</strong>。</li>\n<li>只记录能被代码和已跑命令直接支撑的结论，不记猜测风险；性能审查只找「随数据量增长而放大」的问题。</li>\n<li>多维度并行：可 spawn 只读探索子代理分维度（架构/性能/安全/UI），各自输出带证据的 findings 后汇总。</li>\n<li>基调：倾向恢复单一职责、拆过界文件，而非引入统一抽象。</li>\n</ul>\n<h3>3. 真源先行：只读核查循环</h3>\n<p><strong>真源 = 当前仓库代码的实际状态</strong>，区别于文档旧声称、记忆、上轮结论。动手前先用只读方式对齐真源。</p>\n<ul>\n<li>核查与修复是<strong>两个独立动作</strong>：接到「只读核查」指令时绝不顺手改代码。</li>\n<li>核查产物三件套：<strong>三态结论</strong>（仍成立 / 部分成立 / 不成立）+ <strong>证据</strong>（文件+行号）+ <strong>最小修复顺序建议</strong>（区分「本轮小补丁」vs「只应登记的结构性问题」）。</li>\n<li>硬约束：不建议大重构。</li>\n</ul>\n<h3>4. 修复循环（一次一条）</h3>\n<ol>\n<li>用户以 <code>[proposal-x.md] #### N. 标题</code> 单条触发；不批量改。</li>\n<li>读项目约定 + 提案原文 → 只读排查确认真源 → 最小改动修复。</li>\n<li><strong>验证门禁</strong>：lint + format:check + test + build + <code>git diff --check</code>；性能类必须带 benchmark 前后毫秒数据；交互类做浏览器人工验证。</li>\n<li><strong>先回写提案状态，再提 PR</strong>（draft）。回写保留原始证据 + 已完成方案 + 保持不变的边界——否则文档与真源分裂。</li>\n<li>发现旧文档描述已不准确时，主动标注「此 finding 现已不成立/已收口」。</li>\n</ol>\n<h3>5. 并行子代理提升测试覆盖率</h3>\n<ol>\n<li><strong>先建真源</strong>：跑 <code>test:coverage</code> 拿真实报表 + 读覆盖率阈值配置，不拍脑袋选模块。</li>\n<li>只读探索子代理分区盘点低覆盖模块，各输出「未覆盖核心分支 + 可复用 mock + 优先级」。</li>\n<li><strong>锁目标</strong>：「完成覆盖率要求」在已过线时有歧义——先与用户确认是守阈值、补测还是提阈值，以及并行粒度。</li>\n<li>按领域大模块拆执行子代理，指令写死约束：<strong>写入边界仅测试文件</strong>、不改生产代码/对外接口、各子代理写入互不重叠、复用现有 mock 与测试风格、「你不是单独工作，别回退别人的改动」。</li>\n<li>主代理整合、全量回归、上调阈值（用 CI 可严格拦截的写法防回退）。</li>\n<li>惯例：纯入口薄封装做覆盖率<strong>排除</strong>而非硬凑；外部大模型 API 用 mock；「整体阈值 + 单文件阈值」双达标。</li>\n</ol>\n",
  "coding-agent-session-formats": "<h2>Coding Agent 本地会话格式与解析</h2>\n<p>各家 coding agent 本地会话存储的逆向知识。适用于解析、导出、可视化会话，或做 token/cost 统计。</p>\n<h3>1. 存储位置（env 优先 → home 兜底）</h3>\n<p>路径解析设计：官方环境变量优先 → 默认 home → 本地开发目录回退。</p>\n<table>\n<thead>\n<tr>\n<th>Agent</th>\n<th>根目录</th>\n<th>会话数据</th>\n<th>形态</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Codex</td>\n<td><code>CODEX_HOME</code> → <code>~/.codex</code></td>\n<td><code>sessions/YYYY/MM/DD/rollout-&lt;ISO&gt;-&lt;uuid&gt;.jsonl</code>；标题缓存 <code>.codex-global-state.json</code></td>\n<td>JSONL</td>\n</tr>\n<tr>\n<td>Claude Code</td>\n<td><code>CLAUDE_CONFIG_DIR</code> → <code>~/.claude</code></td>\n<td><code>projects/&lt;cwd转义&gt;/&lt;sessionId&gt;.jsonl</code>（cwd 的 <code>/</code> 转 <code>-</code>）</td>\n<td>JSONL</td>\n</tr>\n<tr>\n<td>Kimi</td>\n<td><code>KIMI_SHARE_DIR</code> → <code>~/.kimi</code></td>\n<td><code>sessions/&lt;hash&gt;/&lt;uuid&gt;/</code>：<code>context.jsonl</code>(主) + <code>wire.jsonl</code>(事件) + <code>metadata.json</code></td>\n<td>JSONL 目录</td>\n</tr>\n<tr>\n<td>OpenCode</td>\n<td><code>XDG_DATA_HOME/opencode</code> → <code>~/.local/share/opencode</code></td>\n<td><code>opencode.db</code>（表 session/message/part）</td>\n<td>SQLite</td>\n</tr>\n<tr>\n<td>zcode</td>\n<td><code>~/.zcode</code>（macOS）/ <code>%USERPROFILE%\\.zcode</code>（Win）；Linux 不存在</td>\n<td><code>cli/db/db.sqlite</code>，OpenCode 同范式</td>\n<td>SQLite</td>\n</tr>\n<tr>\n<td>Pi</td>\n<td><code>~/.pi</code></td>\n<td><code>agent/sessions/--&lt;path&gt;--/&lt;ts&gt;_&lt;uuid&gt;.jsonl</code>（树状）</td>\n<td>JSONL</td>\n</tr>\n</tbody></table>\n<p>resume 命令：<code>claude --resume {id}</code> / <code>codex resume {id}</code> / <code>kimi --r {id}</code> / <code>opencode -s {id}</code> / <code>pi --session {id}</code>；Cursor 无。</p>\n<h3>2. 各格式消息结构</h3>\n<h4>Codex（rollout jsonl）</h4>\n<p>每行 <code>{timestamp, type, payload}</code>：</p>\n<ul>\n<li><code>session_meta</code>：<code>{id, timestamp, cwd, ...}</code>。</li>\n<li><code>response_item</code>（正文）：<code>payload.type</code> = <code>message</code>（role: developer|user|assistant，content[].type = input_text/output_text）、<code>reasoning</code>（summary[].type=summary_text 即 thinking）、<code>function_call</code>/<code>function_call_output</code>（按 <code>call_id</code> 配对）、<code>custom_tool_call(_output)</code>（如 <code>apply_patch</code>）。</li>\n<li><code>event_msg</code>：UI 事件，<strong>与 response_item 内容重复，是主要去重来源</strong>。</li>\n</ul>\n<p>规则：时间取每行 <code>timestamp</code>；call/output 按 <code>call_id</code> 合并为一个 tool part；<strong>工具向上归属</strong>——text 之后连续的 tool 挂到该 text，直到下一条 text；工具名 <code>exec_command → bash</code>（function_call 几乎只有它，其余走 custom_tool_call）；<code>role=developer</code>、<code>&lt;INSTRUCTIONS&gt;</code>/<code>&lt;environment_context&gt;</code> 包装的 user 消息应过滤；bash 输出头部噪声（<code>Chunk ID / Wall time / Process exited / Original token count / Output:</code>）展示时剥离；<code>apply_patch</code> 结构化为 <code>write_file</code>（文件+完整内容）/<code>edit_file</code>（按文件 diff）。</p>\n<h4>Claude Code（jsonl）</h4>\n<p>每行顶层：<code>parentUuid, isSidechain, cwd, sessionId, version, gitBranch, type(assistant|user|summary), uuid, timestamp, message</code>。</p>\n<ul>\n<li><code>message.content[]</code>：<code>text</code>/<code>thinking</code>/<code>tool_use{id,name,input}</code>/<code>tool_result{tool_use_id,content}</code>；<code>message.usage{input_tokens,output_tokens,cache...}</code>。</li>\n<li><strong>tool_use 与 tool_result 是两条独立消息</strong>（result 在 <code>type:user</code> 里），用 <code>tool_use.id ↔ tool_use_id</code> 关联。</li>\n<li><code>isMeta: true</code> 是注入消息，过滤；<code>TodoWrite</code> 整体移除；普通工具按 id 把 output 回填进调用。</li>\n</ul>\n<h4>Kimi（context.jsonl 主，wire.jsonl 回退）</h4>\n<p><strong>必须以 context.jsonl 为主</strong>——只读 wire.jsonl 会把流式中间态扩散成独立消息（实测 184 vs 真实 113 条）。</p>\n<ul>\n<li>context 行 <code>role</code>：user/assistant/tool/<code>_checkpoint</code>/<code>_usage</code>（后两者忽略）。assistant：<code>content[].type = think|text</code> + <code>tool_calls[]</code>（<code>id</code> 如 <code>ReadFile:0</code>）；tool 行用 <code>tool_call_id</code> 关联回填。</li>\n<li>工具名映射（仅 7 个）：<code>ReadFile→read, Glob→glob, StrReplaceFile→edit, Grep→grep, WriteFile→write, Shell→bash</code>；<code>SetTodoList</code> 过滤。Kimi 无 tool status（默认成功）。</li>\n<li>token 缺失时从 wire.jsonl 事件的 <code>usage{input_tokens,output_tokens}</code> 补，仍缺则按模型价格估算并标记 estimated。</li>\n</ul>\n<h4>OpenCode / zcode（SQLite）</h4>\n<p><code>session</code>（id/directory/title/time_created 毫秒…）+ <code>message.data</code>(JSON: agent,role,time,cost,modelID) + <code>part.data</code>(JSON: type,callID,state,tool,tokens,text)；<code>part.type</code> = tool/text/reasoning/file。</p>\n<h4>Pi（树状 jsonl）</h4>\n<p><code>id</code>/<code>parentId</code> 组树可原地分支；content blocks：text/image/thinking/toolCall；<code>ToolResultMessage{toolCallId,toolName,isError}</code>。其 <code>Usage{input,output,cacheRead,cacheWrite,totalTokens,cost:{...分项}}</code> 自带分项成本，<strong>是 cost 建模最佳样板</strong>。</p>\n<h3>3. assistant 消息模式组（跨 agent 统一）</h3>\n<p>只有 4 种模式，必须严格保序：① thinking ② thinking+text+tool ③ text+tool ④ text。\n实现要点：先并 thinking、再并 text、再挂 tool；<strong>thinking-only 分组不能吸附 tool</strong>——需要「当前分组」与「最近可挂 tool 的 text 分组」两个独立锚点。</p>\n<h3>4. 统一导出 Schema</h3>\n<p>顶层：<code>id/title/directory/version/time_created/time_updated/stats/messages</code>；<code>messages[] = role + parts[]</code>（text/reasoning/tool）。\ntool part 统一结构：</p>\n<pre><code class=\"language-json\">{ &quot;type&quot;: &quot;tool&quot;, &quot;tool&quot;: &quot;read&quot;, &quot;callID&quot;: &quot;...&quot;, &quot;title&quot;: &quot;read&quot;,\n  &quot;state&quot;: { &quot;arguments&quot;: {}, &quot;output&quot;: [{&quot;type&quot;:&quot;text&quot;,&quot;text&quot;:&quot;...&quot;}] },\n  &quot;time_created&quot;: 0 }\n</code></pre>\n<p>tool output 回填到调用而非独立消息；无法关联的保留为 fallback <code>role=tool</code> 不丢数据。MCP 工具要把 namespace 拼进完整名。</p>\n<h3>5. Token / Cost 统计</h3>\n<ul>\n<li><strong>cache token 必须计入 cost</strong>：<code>cost = input×in价 + output×out价 + cache_write×cacheWrite价 + cache_read×cacheRead价</code>。漏算 cache 是「cost 明显过大/对不上」的常见根因。</li>\n<li>模型别名要归一（如 <code>anthropic--claude-4.6-opus → claude-opus-4-6</code>），价格表用 LiteLLM 快照做离线兜底。</li>\n<li>区分 <code>cost_source: &quot;recorded&quot; | &quot;estimated&quot;</code>：有原始 cost 用 recorded；缺失按 token×价格估算并透传标记到 UI。</li>\n<li>逐消息累加，缺失字段按 <code>?? 0</code> 容错。</li>\n</ul>\n<h3>6. 解析性能铁律（单文件可达 100-400MB）</h3>\n<ul>\n<li><strong>绝不整读大文件</strong>，流式逐行解析。</li>\n<li>Worker 流式解析时用 append-only buffer（引用稳定）+ 版本号信号驱动 UI，<strong>禁止每 batch spread 复制历史记录</strong>（O(n²)）。</li>\n<li>聚合/派生数据增量累积（O(delta)），不要每 batch 全量重扫；无过滤条件时统计直接复用 parser 内部 stats（O(1)）。</li>\n<li>缓存架构：前台只读缓存（SQLite），独立 worker 负责写入；不要做「源文件更新就直读原始」的分叉路径。</li>\n<li>用多规格基准文件（50/150/300MB + 高记录数小文件）做 benchmark 验证。</li>\n</ul>\n",
  "cover-image": "<h2>Cover Image Prompt Generator</h2>\n<p>Generate structured prompts for article cover images with 5-dimensional customization.</p>\n<h3>Usage</h3>\n<pre><code class=\"language-bash\"># Auto-select dimensions based on content\n/cover-image path/to/article.md\n\n# Quick mode: skip confirmation\n/cover-image article.md --quick\n\n# Specify dimensions\n/cover-image article.md --type conceptual --palette warm --rendering flat-vector\n\n# Style presets (shorthand for palette + rendering)\n/cover-image article.md --style blueprint\n\n# With reference images\n/cover-image article.md --ref style-ref.png\n\n# Direct content input\n/cover-image --palette mono --aspect 1:1 --quick\n[paste content]\n</code></pre>\n<h3>Options</h3>\n<table>\n<thead>\n<tr>\n<th>Option</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--type &lt;name&gt;</code></td>\n<td>hero, conceptual, typography, metaphor, scene, minimal</td>\n</tr>\n<tr>\n<td><code>--palette &lt;name&gt;</code></td>\n<td>warm, elegant, cool, dark, earth, vivid, pastel, mono, retro, duotone</td>\n</tr>\n<tr>\n<td><code>--rendering &lt;name&gt;</code></td>\n<td>flat-vector, hand-drawn, painterly, digital, pixel, chalk, screen-print</td>\n</tr>\n<tr>\n<td><code>--style &lt;name&gt;</code></td>\n<td>Preset shorthand (see <a href=\"references/style-presets.md\">Style Presets</a>)</td>\n</tr>\n<tr>\n<td><code>--text &lt;level&gt;</code></td>\n<td>none, title-only, title-subtitle, text-rich</td>\n</tr>\n<tr>\n<td><code>--mood &lt;level&gt;</code></td>\n<td>subtle, balanced, bold</td>\n</tr>\n<tr>\n<td><code>--font &lt;name&gt;</code></td>\n<td>clean, handwritten, serif, display</td>\n</tr>\n<tr>\n<td><code>--aspect &lt;ratio&gt;</code></td>\n<td>16:9 (default), 2.35:1, 4:3, 3:2, 1:1, 3:4</td>\n</tr>\n<tr>\n<td><code>--lang &lt;code&gt;</code></td>\n<td>Title language (en, zh, ja, etc.)</td>\n</tr>\n<tr>\n<td><code>--no-title</code></td>\n<td>Alias for <code>--text none</code></td>\n</tr>\n<tr>\n<td><code>--quick</code></td>\n<td>Skip confirmation, use auto-selection</td>\n</tr>\n<tr>\n<td><code>--ref &lt;files...&gt;</code></td>\n<td>Reference images for style/composition guidance</td>\n</tr>\n</tbody></table>\n<h3>Five Dimensions</h3>\n<table>\n<thead>\n<tr>\n<th>Dimension</th>\n<th>Values</th>\n<th>Default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Type</strong></td>\n<td>hero, conceptual, typography, metaphor, scene, minimal</td>\n<td>auto</td>\n</tr>\n<tr>\n<td><strong>Palette</strong></td>\n<td>warm, elegant, cool, dark, earth, vivid, pastel, mono, retro, duotone</td>\n<td>auto</td>\n</tr>\n<tr>\n<td><strong>Rendering</strong></td>\n<td>flat-vector, hand-drawn, painterly, digital, pixel, chalk, screen-print</td>\n<td>auto</td>\n</tr>\n<tr>\n<td><strong>Text</strong></td>\n<td>none, title-only, title-subtitle, text-rich</td>\n<td>title-only</td>\n</tr>\n<tr>\n<td><strong>Mood</strong></td>\n<td>subtle, balanced, bold</td>\n<td>balanced</td>\n</tr>\n<tr>\n<td><strong>Font</strong></td>\n<td>clean, handwritten, serif, display</td>\n<td>clean</td>\n</tr>\n</tbody></table>\n<p>Auto-selection rules: <a href=\"references/auto-selection.md\">references/auto-selection.md</a></p>\n<h3>Galleries</h3>\n<p><strong>Types</strong>: hero, conceptual, typography, metaphor, scene, minimal\n→ Details: <a href=\"references/types.md\">references/types.md</a></p>\n<p><strong>Palettes</strong>: warm, elegant, cool, dark, earth, vivid, pastel, mono, retro, duotone\n→ Details: <a href=\"references/palettes/\">references/palettes/</a></p>\n<p><strong>Renderings</strong>: flat-vector, hand-drawn, painterly, digital, pixel, chalk, screen-print\n→ Details: <a href=\"references/renderings/\">references/renderings/</a></p>\n<p><strong>Text Levels</strong>: none (pure visual) | title-only (default) | title-subtitle | text-rich (with tags)\n→ Details: <a href=\"references/dimensions/text.md\">references/dimensions/text.md</a></p>\n<p><strong>Mood Levels</strong>: subtle (low contrast) | balanced (default) | bold (high contrast)\n→ Details: <a href=\"references/dimensions/mood.md\">references/dimensions/mood.md</a></p>\n<p><strong>Fonts</strong>: clean (sans-serif) | handwritten | serif | display (bold decorative)\n→ Details: <a href=\"references/dimensions/font.md\">references/dimensions/font.md</a></p>\n<h3>File Structure</h3>\n<p>Cover prompt is generated in the same directory as the article. Do NOT copy articles to other directories.</p>\n<pre><code>{article-dir}/\n├── article.md             # Original article (not copied)\n├── cover.md               # Generated prompt\n├── cover-1.md             # Conflict variant (if cover.md exists)\n├── refs/                  # Reference images (if provided)\n│   ├── ref-01-{slug}.{ext}\n│   └── ref-01-{slug}.md   # Description file\n└── ...\n</code></pre>\n<p><strong>Cover filename</strong>: <code>cover.md</code>. If exists: <code>cover-1.md</code>, <code>cover-2.md</code>, etc.</p>\n<p>For direct content input (no article file): generate in current working directory.</p>\n<h3>Workflow</h3>\n<h4>Progress Checklist</h4>\n<pre><code>Cover Image Progress:\n- [ ] Step 0: Check preferences (EXTEND.md) ⛔ BLOCKING\n- [ ] Step 1: Analyze content + save refs\n- [ ] Step 2: Confirm options (6 dimensions) ⚠️ unless --quick\n- [ ] Step 3: Create prompt\n- [ ] Step 4: Completion report\n</code></pre>\n<h4>Flow</h4>\n<pre><code>Input → [Step 0: Preferences] ─┬─ Found → Continue\n                               └─ Not found → First-Time Setup ⛔ BLOCKING → Save EXTEND.md → Continue\n        ↓\nAnalyze + Save Refs → [Confirm: 6 Dimensions] → Prompt → Complete\n                                              ↓\n                                     (skip if --quick or all specified)\n</code></pre>\n<h4>Step 0: Load Preferences ⛔ BLOCKING</h4>\n<p>Check EXTEND.md in project root only:</p>\n<pre><code class=\"language-bash\">test -f EXTEND.md &amp;&amp; echo &quot;found&quot;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Result</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Found</td>\n<td>Load, display summary → Continue</td>\n</tr>\n<tr>\n<td>Not found</td>\n<td>Ask user for preferences → Generate EXTEND.md → Continue</td>\n</tr>\n</tbody></table>\n<p><strong>CRITICAL</strong>: If not found, complete setup BEFORE any other steps or questions. Do NOT check other directories.</p>\n<h4>Step 1: Analyze Content</h4>\n<ol>\n<li><strong>Save reference images</strong> (if provided) → <a href=\"references/workflow/reference-images.md\">references/workflow/reference-images.md</a></li>\n<li><strong>Analyze content</strong>: topic, tone, keywords, visual metaphors</li>\n<li><strong>Deep analyze references</strong> ⚠️: Extract specific, concrete elements (see reference-images.md)</li>\n<li><strong>Detect language</strong>: Compare source, user input, EXTEND.md preference</li>\n</ol>\n<p><strong>⚠️ People in Reference Images — MUST follow all 3 rules:</strong></p>\n<p>If reference images contain <strong>people</strong> who should appear in the cover:</p>\n<ol>\n<li><strong><code>usage: direct</code></strong> — MUST set in refs description file. NEVER use <code>style</code> or <code>palette</code> when people need to appear</li>\n<li><strong>Per-character description</strong> — MUST describe each person&#39;s distinctive features (hair, glasses, skin tone, clothing) in <code>refs/ref-NN-{slug}.md</code>. Vague descriptions like &quot;a man&quot; will fail</li>\n<li><strong>Prompt inclusion</strong> — MUST include reference image paths in prompt frontmatter <code>references</code> list so the downstream image generator can access them</li>\n</ol>\n<p>See <a href=\"references/workflow/reference-images.md\">reference-images.md § Character Analysis</a> for description format.</p>\n<h4>Step 2: Confirm Options ⚠️</h4>\n<p><strong>MUST use <code>AskUserQuestion</code> tool</strong> to present options as interactive selection — NOT plain text tables. Present up to 4 questions in a single <code>AskUserQuestion</code> call (Type, Palette, Rendering, Font + Settings). Each question shows the recommended option first with reason, followed by alternatives.</p>\n<p>Full confirmation flow and question format: <a href=\"references/workflow/confirm-options.md\">references/workflow/confirm-options.md</a></p>\n<table>\n<thead>\n<tr>\n<th>Condition</th>\n<th>Skipped</th>\n<th>Still Asked</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--quick</code> or <code>quick_mode: true</code></td>\n<td>6 dimensions</td>\n<td>Aspect ratio (unless <code>--aspect</code>)</td>\n</tr>\n<tr>\n<td>All 6 + <code>--aspect</code> specified</td>\n<td>All</td>\n<td>None</td>\n</tr>\n</tbody></table>\n<h4>Step 3: Create Prompt</h4>\n<p>Save to article directory as <code>cover.md</code>. Template: <a href=\"references/workflow/prompt-template.md\">references/workflow/prompt-template.md</a></p>\n<p><strong>Filename conflict</strong>: If <code>cover.md</code> exists, use <code>cover-1.md</code>, <code>cover-2.md</code>, etc. Check before writing.</p>\n<p><strong>CRITICAL - References in Frontmatter</strong>:</p>\n<ul>\n<li>Files saved to <code>refs/</code> → Add to frontmatter <code>references</code> list</li>\n<li>Style extracted verbally (no file) → Omit <code>references</code>, describe in body</li>\n<li>Before writing → Verify: <code>test -f refs/ref-NN-{slug}.{ext}</code></li>\n</ul>\n<p><strong>Reference elements in body</strong> MUST be detailed, prefixed with &quot;MUST&quot;/&quot;REQUIRED&quot;, with integration approach.</p>\n<h4>Step 4: Completion Report</h4>\n<pre><code>Prompt Ready!\n\nTopic: [topic]\nType: [type] | Palette: [palette] | Rendering: [rendering]\nText: [text] | Mood: [mood] | Font: [font] | Aspect: [ratio]\nTitle: [title or &quot;visual only&quot;]\nLanguage: [lang] | Watermark: [enabled/disabled]\nReferences: [N images or &quot;extracted style&quot; or &quot;none&quot;]\nLocation: [article directory path]\n\nFiles:\n✓ cover.md (or cover-N.md)\n</code></pre>\n<h3>Composition Principles</h3>\n<ul>\n<li><strong>Whitespace</strong>: 40-60% breathing room</li>\n<li><strong>Visual anchor</strong>: Main element centered or offset left</li>\n<li><strong>Characters</strong>: Simplified silhouettes; NO realistic humans</li>\n<li><strong>Title</strong>: Use exact title from user/source; never invent</li>\n</ul>\n<h3>Extension Support</h3>\n<p>Custom configurations via EXTEND.md. See <strong>Step 0</strong> for paths.</p>\n<p>Supports: Watermark | Preferred dimensions | Default aspect | Quick mode | Custom palettes | Language</p>\n<p>Schema: <a href=\"references/config/preferences-schema.md\">references/config/preferences-schema.md</a></p>\n<h3>References</h3>\n<p><strong>Dimensions</strong>: <a href=\"references/dimensions/text.md\">text.md</a> | <a href=\"references/dimensions/mood.md\">mood.md</a> | <a href=\"references/dimensions/font.md\">font.md</a>\n<strong>Palettes</strong>: <a href=\"references/palettes/\">references/palettes/</a>\n<strong>Renderings</strong>: <a href=\"references/renderings/\">references/renderings/</a>\n<strong>Types</strong>: <a href=\"references/types.md\">references/types.md</a>\n<strong>Auto-Selection</strong>: <a href=\"references/auto-selection.md\">references/auto-selection.md</a>\n<strong>Style Presets</strong>: <a href=\"references/style-presets.md\">references/style-presets.md</a>\n<strong>Compatibility</strong>: <a href=\"references/compatibility.md\">references/compatibility.md</a>\n<strong>Visual Elements</strong>: <a href=\"references/visual-elements.md\">references/visual-elements.md</a>\n<strong>Workflow</strong>: <a href=\"references/workflow/confirm-options.md\">confirm-options.md</a> | <a href=\"references/workflow/prompt-template.md\">prompt-template.md</a> | <a href=\"references/workflow/reference-images.md\">reference-images.md</a>\n<strong>Config</strong>: <a href=\"references/config/preferences-schema.md\">preferences-schema.md</a> | <a href=\"references/config/first-time-setup.md\">first-time-setup.md</a> | <a href=\"references/config/watermark-guide.md\">watermark-guide.md</a></p>\n",
  "decision-complete-plan": "<h2>决策完整的实施计划（Plan → Implement 交接）</h2>\n<p>计划文本是交给另一个会话/执行者的<strong>交接物</strong>，第一性目标是「决策完整（decision complete）」：<strong>实现者不需要再做任何决策</strong>。适用于先规划后执行的两段式工作，也适用于拿到计划后的执行侧。</p>\n<h3>1. 计划前的收敛（三阶段）</h3>\n<ol>\n<li><strong>落地环境</strong>：先探索、后提问。能从仓库查到的事实（某个结构在哪、用了什么组件）<strong>禁止</strong>问用户，自己 grep/读文件用证据确认。</li>\n<li><strong>意图对齐</strong>：问到能清楚陈述「目标+成功标准、范围内/外、约束、现状、关键偏好」为止。</li>\n<li><strong>实现收敛</strong>：把方案逼到决策完整——approach、接口(API/schema/IO)、数据流、边界/失败模式、测试与验收、迁移/兼容。</li>\n</ol>\n<p><strong>两类未知区别对待</strong>：</p>\n<ul>\n<li><strong>可发现的事实</strong>（仓库真相）→ 自己探索，用 <code>路径:行号</code> 证据确认；探索结果可以纠偏用户的初始假设（用户以为要大迁移，实际根因可能只是一层结构包装）。</li>\n<li><strong>偏好/取舍</strong>（不可发现）→ 尽早问，给 2-4 个互斥选项 + 推荐默认值；用户未答就按推荐走，并在计划里<strong>记为显式假设</strong>。</li>\n</ul>\n<p>收敛节奏：探索时每轮一句说明为何看下一个文件；探索完成后集中锁定 2-4 个高影响产品决策（「为避免实现偏差，我先锁定 N 个行为决策」），拍板后才出计划。</p>\n<h3>2. 计划文档模板（八段式）</h3>\n<pre><code class=\"language-markdown\"># &lt;一句话标题：动作 + 对象 + 关键约束&gt;\n\n## 摘要                        # 结论先行：能不能做 + 核心思路 + 影响面\n## 根因确认 / 现状基线          # bug/重构类必备；每条挂 绝对路径:行号 证据\n## 已确认的决策                 # 用户拍板项，来源可追溯\n## 实施方案（最小改动）          # 分步可执行；显式写&quot;不改 X&quot;\n## 具体改动点                   # 决策完整：细到文件清单、状态字段、持久化 key、常量值\n## 对外接口 / 类型变化          # 即使&quot;无变更&quot;也显式声明，界定影响面\n## 测试与验收                   # 回归命令 + 新增用例场景 + 手动验证（给预期数字）\n## 假设与默认                   # 未确认项固化为默认值，避免实现期再决策\n</code></pre>\n<p>写作要点：</p>\n<ul>\n<li><strong>结论/根因先行</strong>：摘要第一句就是判断（「可以，且适合用 X」「根因不是 A 而是 B」）。</li>\n<li><strong>「最小改动」是显式边界</strong>：单列「不做什么/范围外项」。</li>\n<li><strong>改动点不留「视情况而定」</strong>：新增/修改文件用绝对路径逐条列，键名、常量直接写死。</li>\n<li>结尾<strong>不要</strong>问「should I proceed?」——计划本身就是可直接执行的交付物。</li>\n</ul>\n<h3>3. 执行侧协议（拿到计划后）</h3>\n<ol>\n<li>先声明落地顺序（「先改 A，再改 B，最后跑测试回归」）。</li>\n<li><strong>先重读计划涉及的文件，核对计划与代码现状是否仍一致</strong>，有偏差先反馈再动手。</li>\n<li>按最小改动实现，不扩散到计划边界外。</li>\n<li>跑计划里的回归命令（lint + test 全量）；遇到新问题回到根因分析，不贴补丁。</li>\n<li>收尾用 <code>file:line</code> 分块汇报：主要改动 / 测试改动 / 验证结果（含通过数字）。</li>\n</ol>\n<h3>4. 交接格式</h3>\n<p>计划全文应可直接作为新会话的开场消息（<code>PLEASE IMPLEMENT THIS PLAN:</code> + 计划原文）被执行，不依赖原会话的对话上下文——这也是检验「决策完整」的标准：脱离上下文仍然可以无歧义执行。</p>\n",
  "design-system-as-input-ui": "<h2>用「设计系统作为输入」生成一致的 UI</h2>\n<h3>核心问题与原理</h3>\n<p>AI 逐屏生成 UI 时，每屏都从零重画，<strong>没有记忆上一屏的决策</strong>，于是每屏重新发明一个蓝色、一个圆角、一套间距。十屏就是十张恰好放在同一文件夹里的图，而不是一个产品。</p>\n<p>这不是模型能力问题，是<strong>缺少 source of truth</strong>。模型在像素级生成，是因为它没有别的可对齐的东西。解法：给它一份设计系统作为<strong>输入（input / contract）</strong>，而不是一次次的描述（request）。截图让模型猜，token 包让模型引用——同一个模型，完全不同的结果。</p>\n<p>设计系统作为输入时是一个可移植的包，三部分：</p>\n<ul>\n<li><strong>Tokens</strong> —— 实际的颜色、间距、字号 scale</li>\n<li><strong>Components</strong> —— 按钮及其 variants 和 states</li>\n<li><strong>Guidelines</strong> —— 这些如何组合的规则（如何时用 drawer vs modal、空态/加载态长什么样）</li>\n</ul>\n<h3>四步工作流</h3>\n<p>工作流的关键：把设计系统做成一次性的规则，改规则一次修所有屏；而不是逐屏改像素。工作从「restyle 像素」变成「review 引用」，是一个短得多的活。</p>\n<h4>Step 1 — 从「具体的 brief」而非「vibe」建系统</h4>\n<p>系统质量几乎完全由 brief 质量决定。四个词的模糊 brief（&quot;a dashboard for AI&quot;）只会把猜测上移一层，产出一个放到任何产品上都行、因而什么问题都没解决的通用系统。<strong>早期的具体性就是全部。</strong></p>\n<p>Brief 模板（把方括号换成你的真实内容）：</p>\n<pre><code class=\"language-text\">build a design system for [用一句具体的话描述你的产品]. surface and accent colors: [具体色值/风格，如 deep navy surface + one high-contrast accent]. type: [字体，如 Inter for everything]. density: [low/high]. components: [列出你真正会用到的，如 cards, data tables, tabs, status badges, KPI tiles]. include guidelines for drawer vs modal and for empty and loading states.\n</code></pre>\n<p>导入已有系统时：结构优于像素，<strong>HTML 和 Figma SVG 效果最好，PNG 最差</strong>（PNG 只是一张要被解读的图，给不出真实 token 和层级）。</p>\n<h4>Step 2 — 生成时把系统「附加」进去，而非事后补</h4>\n<p>系统 attach 到每次生成后，每屏都引用你的 token 而不是发明新的。</p>\n<pre><code class=\"language-text\">generate [N] screens using the attached design system: [列出所有屏，如 overview, cost-by-model, latency, token usage, agent status, alerts, single-agent detail, settings, billing, empty/first-run]. keep the nav, cards, and chart styles consistent across all of them.\n</code></pre>\n<p>此时你做的是 review（导航栏每屏一致、KPI 卡只有一种样式、图表共用调色板），而不是 restyle。</p>\n<h4>Step 3 — 下载系统，交给 Claude Code / Cursor</h4>\n<p>导出整份系统（tokens + component specs + guidelines + assets），作为输入交给编码 agent。关键在最后一句 <strong>stop-and-ask</strong>：它把模型「填补空白」的本能转成「标记空白」的本能，是防止发明色值的核心机制。</p>\n<pre><code class=\"language-text\">here is our design system (attached). build [screen/component] using only the tokens, components, and spacing it defines. do not introduce new colors, radii, or font sizes. if the system doesn&#39;t cover something, stop and ask instead of inventing it.\n</code></pre>\n<p>（Cursor 同理：把下载的系统丢进项目，告诉它 &quot;build using our design system&quot;，编辑器里的 AI 会把 token 和 spec 当 spec 读。）</p>\n<h4>Step 4 — 设为常驻规则，防止漂移</h4>\n<p>下载的系统只有在<strong>每个任务都引用它</strong>时才有用。某一屏跳过它，那屏就漂回猜测。把下面这条在项目级设一次：</p>\n<pre><code class=\"language-text\">for every new screen or component in this project, reference the attached design system first. before writing any color, spacing, or type value, check whether the system already defines it. flag any case where the design doesn&#39;t cover what you need.\n</code></pre>\n<h3>完整可复制流水线</h3>\n<pre><code class=\"language-text\"># 1. 建系统（如在 Moonchild 等 design-system-first 工具里，或直接让 agent 产出一份 token 文件）\nbuild a design system for [用一句具体的话描述你的产品]. surface and accent colors: [具体]. type: [字体]. density: [low/high]. components: [列出你真正会用到的]. include guidelines for drawer vs modal and for empty and loading states.\n\n# 2. 附加系统后生成多屏\ngenerate [N] screens using the attached design system: [列出所有屏]. keep nav, cards, and core components consistent across all of them.\n\n# 3. 下载系统后，在 Claude Code / Cursor 里\nhere is our design system (attached). build [screen/component] using only the tokens, components, and spacing it defines. do not introduce new colors, radii, or font sizes. if the system doesn&#39;t cover something, stop and ask instead of inventing it.\n\n# 4. 设为常驻规则防漂移\nfor every new screen or component, reference the attached design system first. before writing any color, spacing, or type value, check whether the system defines it. flag anything it doesn&#39;t cover.\n</code></pre>\n<h3>三个会浪费时间的坑</h3>\n<ol>\n<li><strong>用 PNG 导入系统</strong> —— 结构优于像素，PNG 只能得到粗略近似；用 HTML / Figma SVG。</li>\n<li><strong>brief 太泛</strong> —— 只是把猜测上移一层，没有真正消除。</li>\n<li><strong>某个任务跳过系统</strong> —— agent 从不引用的包等于没有；「写前先查系统」这条规则才让它真正生效。</li>\n</ol>\n<h3>三个诚实的边界</h3>\n<ol>\n<li><strong>产出仍需 review</strong> —— 更快更一致，但仍是生成代码；当强初稿对待，尤其逻辑部分要测试。</li>\n<li><strong>系统有多好，产出就有多好</strong> —— 系统薄而通用，导出就薄而通用，下游全部继承。省下的 restyle 时间转移到「一次性把系统建扎实」。</li>\n<li><strong>纪律不会自我执行</strong> —— 常驻指令有帮助，但团队里 code review 仍要检查是否用了 token 而非硬编码近似值。</li>\n</ol>\n<h3>何时用</h3>\n<p>生成 <strong>超过一屏</strong> 的 UI、dashboard、或任何需要跨屏视觉一致的产品时。单个一次性页面不需要这套。衡量收益的指标：首次生成即 on-brand 的屏数、restyle 总耗时、AI 发明的一次性色值数量、跨屏品牌漂移。</p>\n",
  "electron-desktop-release": "<h2>Electron 桌面应用打包、签名与安全</h2>\n<p>Electron 应用（electron-vite 构建 main/preload/renderer + electron-builder 出包）的发布与安全经验。</p>\n<h3>1. 打包结构（pnpm monorepo）</h3>\n<ul>\n<li><code>apps/desktop</code>（Electron）+ 其他 app（如浏览器插件）；构建输出（各 app 的 dist）与发布产物（根 <code>dist/app/mac-arm64</code>、<code>dist/ext/chrome-mv3</code>）分离，按 os/arch 组织。</li>\n<li>scripts：<code>make</code>（全部）/ <code>make:app</code> / <code>make:ext</code> 分层。</li>\n<li><strong>坑</strong>：pnpm 的 <code>onlyBuiltDependencies</code> 白名单会拦住 electron-builder 的原生构建脚本，需显式加入白名单。</li>\n</ul>\n<h3>2. macOS 签名 + 公证（站外分发三件事）</h3>\n<p><strong>Developer ID Application 签名 + hardened runtime + notarization staple</strong>，验证链：签名 → notarytool → <code>stapler staple</code> → <code>spctl</code> / <code>stapler validate</code>。</p>\n<ul>\n<li><strong>ad-hoc 陷阱</strong>：配置里 <code>mac.identity: &#39;-&#39;</code> 会绕开 Developer ID；应删掉该项，让 Keychain/CI secret 决定身份，证书名不写进仓库。</li>\n<li><strong>证书</strong>：CSR 必须在本机 Keychain 生成（私钥留本机）；导入 <code>.cer</code> 后配对。「证书没有私钥」通常就是 CSR 不是本机生成的。</li>\n<li><strong>identity 命名坑</strong>：electron-builder 不接受 <code>Developer ID Application:</code> 前缀，要传限定名如 <code>NAME (TEAMID)</code>。</li>\n<li><strong>签名慢是正常的</strong>：对每个 <code>.pak</code> 逐个 <code>codesign --timestamp</code>，首次要几分钟；不能随意 <code>signIgnore</code>（seal 不一致会被 Gatekeeper 拒）。</li>\n<li><strong>公证凭据</strong>：用 App Store Connect <strong>Team</strong> API Key（Individual key 不支持 notarytool）；放专用 env 文件（chmod 600），不进 shell rc。</li>\n<li><strong>公证耗时</strong>：<code>notarytool submit --wait</code> 是 Apple 服务器侧排队处理，可能等 10 分钟量级，不要中途停。</li>\n</ul>\n<h3>3. CI / 本地混合发布</h3>\n<ul>\n<li>CI 时间不够时：Mac 打包（含签名公证）放本地跑，CI 只打其他平台，release 后本地补传 Mac 产物。</li>\n<li>完整 timestamp + notarization + staple 只在 tag release 跑；普通 CI/开发用 ad-hoc，只验证「能打包、能跑、能找到身份」。</li>\n</ul>\n<h3>4. Secret 存储架构</h3>\n<p>第一性原则：<strong>SQLite 不是 secret store</strong>（含 WAL 文件都会留明文）。</p>\n<ul>\n<li>真实 key 交给 OS 凭据设施：Electron <code>safeStorage</code>（macOS Keychain / Windows Credential Manager + DPAPI）；SQLite 只存元数据 + <code>hasApiKey</code> 布尔引用。</li>\n<li><strong>主进程独占 secret</strong>：renderer 永不拿到明文 key；用户输入新 key 经 IPC 发 main 保存并立即清空前端态；需要 key 的调用由 main 读取后执行。</li>\n<li>边界要讲清楚：DPAPI 防「同机他人 / 拿到文件的人」，不防「同用户会话内已运行的恶意进程」。</li>\n<li>明文迁移：迁入 secure store → 清空原字段 → WAL checkpoint + <code>VACUUM</code> 清除残留。</li>\n</ul>\n",
  "en-zh-literary-translation": "<h2>英译中：译境</h2>\n<p>把英文译成读起来像中文原生写作的中文。核心判据不是&quot;字面对上了&quot;，而是&quot;中文读者读起来自然、准确、有质感&quot;。译文是原文的孪生：说着另一种语言，却是同一个灵魂。</p>\n<h3>三质（每句译文都要同时满足）</h3>\n<ul>\n<li><strong>信</strong> — 原意如根，深扎不移。不增不减原文的信息与态度，偏离原意即失败。歧义处优先忠实，不自作主张替作者表态。</li>\n<li><strong>达</strong> — 意流如水，寻最自然的中文路径。哪里读起来堵，就改道。以中文母语者的语感为准，不迁就英文结构。</li>\n<li><strong>雅</strong> — 形神合一，不造作也不粗陋，恰到好处。不堆砌辞藻，也不流于口水话。</li>\n</ul>\n<p>三者冲突时的优先级：<strong>信 &gt; 达 &gt; 雅</strong>。先保证不失真，再保证顺畅，最后才追求质感。绝不为&quot;雅&quot;牺牲&quot;信&quot;。</p>\n<h3>本性（要排斥的两种失败）</h3>\n<ul>\n<li><strong>排斥直译的僵硬</strong> — 逐词对应、保留英文语序和句式，导致&quot;翻译腔&quot;。这是最常见的失败。</li>\n<li><strong>排斥意译的飘忽</strong> — 借&quot;通顺&quot;之名擅自增删、脑补、改写原意。放飞自我不是翻译。</li>\n</ul>\n<p>目标是<strong>活的对应</strong>：结构可以变，信息不能变。</p>\n<h3>转化规则（逐条套用）</h3>\n<h4>词层面</h4>\n<ul>\n<li><strong>词选简朴，避繁就简</strong> — 同义项里选最朴素、最常用的中文词。不为显得&quot;有文化&quot;而挑生僻词。</li>\n<li><strong>专词化俗</strong> — 专业术语、行话，在不损失准确性的前提下用通俗表达。真正的技术术语（如已确立的 API、框架名）保留英文或通行译名，不硬翻。</li>\n<li><strong>俗词得体</strong> — 口语、俚语要译得得体，不生硬照搬也不过度书面化。</li>\n</ul>\n<h4>句层面</h4>\n<ul>\n<li><strong>句循母语，顺其自然</strong> — 按中文的句法习惯重组，而不是套英文的从句结构。英文的定语从句、被动语态、名词化表达，常需要拆开、转主动、动词化。</li>\n<li><strong>长句化短</strong> — 英文长句拆成若干中文短句。中文靠短句和意合推进，不靠层层嵌套的关系词。</li>\n<li><strong>短句存神</strong> — 但拆句不是碎尸，要保留原句的节奏和语气重心。</li>\n<li><strong>意随语境，深浅得宜</strong> — 同一个词在不同语境深浅不同，随上下文定分寸。</li>\n</ul>\n<h4>风格层面</h4>\n<ul>\n<li><strong>洋腔化土，土语不俗</strong> — 消除&quot;翻译腔&quot;（如滥用&quot;的&quot;、&quot;被&quot;、&quot;进行&quot;、&quot;关于……的&quot;、&quot;一个&quot;、&quot;们&quot;），换成地道中文；但地道不等于俚俗，保持文气。</li>\n<li><strong>显现之道</strong>：<ul>\n<li>如写文章，不如说话——书面但不端着。</li>\n<li>如江河，不如溪流——有分量但流畅。</li>\n<li>清澈见底，却有深度——易读，但不失原文的思想密度。</li>\n</ul>\n</li>\n</ul>\n<h3>常见翻译腔病灶与修法</h3>\n<table>\n<thead>\n<tr>\n<th>病灶</th>\n<th>症状</th>\n<th>修法</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>滥用&quot;的&quot;</td>\n<td>&quot;一个复杂的难以解决的问题&quot;</td>\n<td>减&quot;的&quot;，拆分：&quot;问题很复杂，难以解决&quot;</td>\n</tr>\n<tr>\n<td>滥用被动</td>\n<td>&quot;这个方案被广泛地采用了&quot;</td>\n<td>转主动/无主：&quot;这个方案得到广泛采用&quot;</td>\n</tr>\n<tr>\n<td>名词化堆叠</td>\n<td>&quot;对性能的优化的实现&quot;</td>\n<td>动词化：&quot;优化性能&quot;</td>\n</tr>\n<tr>\n<td>冗余复数&quot;们&quot;</td>\n<td>&quot;开发者们、用户们&quot;</td>\n<td>中文复数靠语境：&quot;开发者、用户&quot;</td>\n</tr>\n<tr>\n<td>&quot;进行/作出+名词&quot;</td>\n<td>&quot;进行一次分析&quot;</td>\n<td>直接用动词：&quot;分析一下&quot;</td>\n</tr>\n<tr>\n<td>关系词嵌套</td>\n<td>&quot;那个我昨天见过的写了这本书的人&quot;</td>\n<td>拆句：&quot;那个人我昨天见过，就是写这本书的&quot;</td>\n</tr>\n<tr>\n<td>直译连接词</td>\n<td>&quot;由于……因此……&quot;成对出现</td>\n<td>中文常省略一半，靠意合</td>\n</tr>\n</tbody></table>\n<h3>执行流程</h3>\n<ol>\n<li><strong>通读整段</strong>，抓住原文的意思、态度、语气重心，而不是从第一个词开始逐词译。</li>\n<li><strong>定位灵魂</strong> — 这句话真正要传达什么？以此为锚。</li>\n<li><strong>按中文重组</strong> — 拆长句、转被动、动词化、去冗余，用中文句法重新表达。</li>\n<li><strong>去翻译腔</strong> — 对照上表逐条检查病灶。</li>\n<li><strong>回读校对</strong> — 只读中文译文（遮住原文），问自己：像不像中文母语者写的？读起来堵不堵？</li>\n<li><strong>回查信</strong> — 再对照原文，确认没有增删、没有走样。</li>\n</ol>\n<h3>示例</h3>\n<p><strong>原文</strong>：</p>\n<blockquote>\n<p>Markdown has become the dominant file format used by agents to communicate with us. It&#39;s simple, portable, and easy for you to edit.</p>\n</blockquote>\n<p><strong>直译（反例，僵硬）</strong>：</p>\n<blockquote>\n<p>Markdown 已经成为了被 agents 用来与我们进行沟通的占主导地位的文件格式。它是简单的、可移植的、并且对你来说是易于编辑的。</p>\n</blockquote>\n<p><strong>译境（正例）</strong>：</p>\n<blockquote>\n<p>Markdown 已经成了 agent 跟我们沟通的主流文件格式。它简单、可移植，你改起来也方便。</p>\n</blockquote>\n<p>要点：去掉&quot;被……进行……的&quot;翻译腔，长句拆短，&quot;dominant&quot;化俗为&quot;主流&quot;，末句转成第二人称口吻贴合原文的 &quot;easy for you&quot;。</p>\n<hr>\n<p><strong>原文</strong>：</p>\n<blockquote>\n<p>The chance of someone actually reading your spec is much higher if it&#39;s in a format they enjoy.</p>\n</blockquote>\n<p><strong>译境</strong>：</p>\n<blockquote>\n<p>如果你的 spec 用的是别人乐意读的格式，真正有人去读它的概率就高得多。</p>\n</blockquote>\n<p>要点：条件从句提前（中文习惯先条件后结果），&quot;actually&quot; 译为&quot;真正&quot;点出对比语气，保留 spec 术语。</p>\n<h3>输出约定</h3>\n<ul>\n<li>默认只输出译文，不加解释；除非用户要求对照或说明取舍。</li>\n<li>遇到原文歧义、双关、或无法两全的取舍，简短标注一句，让用户定夺，不擅自替原文表态。</li>\n<li>保留原文的段落结构和 Markdown 格式（标题、列表、代码块、链接）。</li>\n<li>代码块内的代码、命令、标识符不译；代码块外的技术术语按&quot;专词化俗但保留确立译名&quot;处理。</li>\n</ul>\n",
  "error-message-writing": "<h2>Error Message Writing</h2>\n<p>写或审查错误消息时遵循本规范。核心：出错的一刻是「关怀」的时刻，不是「打扫」的时刻。</p>\n<h3>核心心智</h3>\n<p>出错时用户想知道三件事，好的错误消息尽量都回答：</p>\n<ol>\n<li><strong>What happened?</strong>（发生了什么）</li>\n<li><strong>Why did it happen?</strong>（为什么）</li>\n<li><strong>How can I fix it?</strong>（怎么解决）</li>\n</ol>\n<p>一句泛泛的 &quot;Something went wrong&quot; 三个都没答，用户被晾在原地——被晾住的用户 = 沮丧且不再信任的用户。像跟朋友说话一样写。</p>\n<h3>四个反模式（要避免）</h3>\n<ol>\n<li><strong>语气不当（inappropriate tone）</strong> — 在高风险失败时用俏皮、轻佻、过度随意的口吻。想象医生手术中说 &quot;Oops!&quot;。当用户的工作、金钱、数据受影响时，语气要匹配严肃程度。</li>\n<li><strong>技术黑话（technical jargon）</strong> — &quot;Couldn&#39;t fetch data&quot;、&quot;Credentials denied&quot;、&quot;Null reference exception&quot; 对普通用户毫无意义。用平实语言说清出了什么事、该做什么，别把 stack trace 甩给用户。</li>\n<li><strong>推卸责任（passing the blame）</strong> — 既不要羞辱用户（&quot;You entered the wrong…&quot;），也不要甩锅第三方（&quot;Stripe isn&#39;t responding&quot;）。聚焦<em>问题</em>本身，而非<em>谁的错</em>。<ul>\n<li>✅ 可接受：&quot;We&#39;re having trouble connecting to Stripe.&quot;</li>\n<li>❌ 不可接受：&quot;Stripe isn&#39;t responding right now.&quot;</li>\n</ul>\n</li>\n<li><strong>无谓的泛化（generic for no reason）</strong> — 有时原因确实未知，但很多时候系统<em>知道</em>发生了什么，消息却不说。隐瞒已知信息是对用户最大的失职。</li>\n</ol>\n<p>反面范本（一句话踩满四条）：</p>\n<blockquote>\n<p><strong>Whoops! Something went wrong</strong>（语气不当）\nThe third-party you&#39;re trying to connect to isn&#39;t responding,（推卸责任）so we can&#39;t fetch your data.（技术黑话）Try again later.（泛化）</p>\n</blockquote>\n<h3>五个原则（要遵循）</h3>\n<ol>\n<li><strong>说清 what 和 why</strong> — 明确说出发生/没发生什么。解释原因，哪怕唯一诚实的解释是「我们这边的技术问题」。空间允许时用 &quot;an issue on our end&quot; 强调不是用户的错。</li>\n<li><strong>提供安慰（reassurance）</strong> — 告诉用户什么<em>没</em>受影响：草稿还在吗？付款安全吗？上传部分成功了吗？安慰能防止恐慌，避免用户重做本不必重做的工作。</li>\n<li><strong>有同理心（empathetic）</strong> — 别过度道歉，也别冷冰冰。情况严重、或用户自己无能为力时，适当用 &quot;please&quot;。同理心是一种语气，不是一句套话。</li>\n<li><strong>帮他解决（help them fix it）</strong> — 有出路就说清楚，具体、可操作、即时：&quot;Check your connection and try again.&quot;、&quot;Upload a file under 10MB.&quot;。空间不够就给描述性链接（&quot;Learn how to resolve this&quot;），绝不用光秃秃的 &quot;Click here&quot;。</li>\n<li><strong>永远给出路（give a way out）</strong> — 用户自己修不了、或错误可能反复出现时，给一条通往客服或明确下一步的路。绝不把用户留在死胡同。</li>\n</ol>\n<p>正面范本（对应五原则）：</p>\n<blockquote>\n<p><strong>Unable to connect your account</strong>（说清 what）\nYour changes were saved,（安慰）but we could not connect your account due to a technical issue on our end.（说清 why）Please try connecting again.（帮他解决）If the issue keeps happening, <strong>contact Customer Care.</strong>（给出路）</p>\n</blockquote>\n<h3>Generic vs. Unclear：两种不同的失败</h3>\n<p>都是坏消息，但病因和疗法不同，别混为一谈：</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Generic（泛化）</th>\n<th>Unclear（含糊不清）</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>例子</td>\n<td>&quot;Something went wrong and this action could not be completed.&quot;</td>\n<td>&quot;Make sure you allow the requested permissions and try again.&quot;</td>\n</tr>\n<tr>\n<td>毛病</td>\n<td>什么都没说</td>\n<td>试图解释，但用了让人费解的语言</td>\n</tr>\n<tr>\n<td>用户感受</td>\n<td>被晾住</td>\n<td>困惑、被指责、被 gaslight</td>\n</tr>\n<tr>\n<td>疗法</td>\n<td><strong>不是</strong>把它写得更长，而是查清到底出了什么错、把<em>那个</em>说出来</td>\n<td>用<strong>用户的词汇</strong>重写，而非系统的词汇</td>\n</tr>\n</tbody></table>\n<h3>执行清单（九点评分）</h3>\n<p>写或审查一条错误消息时逐条走：</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 用平实语言说清了 <strong>what</strong> 发生了？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 说清了 <strong>why</strong>（或诚实承认是系统问题）？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 避开了非开发者看不懂的技术黑话？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 没有指责用户或第三方？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 安慰了用户什么还是安全的（草稿、数据、付款）？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 告诉了用户下一步该做什么？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 若用户修不了，给了出路（客服、重试、替代路径）？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 语气匹配风险——该严肃处严肃，该温暖处温暖？</li>\n<li><input disabled=\"\" type=\"checkbox\"> 是针对<em>这个</em>具体失败写的，还是图省事复用了泛化消息？</li>\n</ul>\n<p><strong>若三条或以上为「否」，这条消息还不能上线。</strong></p>\n<h3>关于复用</h3>\n<ul>\n<li>一条泛化消息作为某个<em>罕见</em> edge case 的 fallback，合理。</li>\n<li>同一条泛化消息被复用到几十个不同的失败场景，这是<em>流程</em>问题，不是文案问题。</li>\n<li>当发现同一条泛化消息被套用到多个具体场景时，正确反应是去问「底层到底发生了什么」，而不是把这条泛化消息润色得更好听。</li>\n</ul>\n<h3>更深一层：这是团队工程</h3>\n<p>泛化错误往往是开发和产品流程的症状，不只是写作问题。当有人要求「这里放个 generic error」时，反问：<em>用户为什么会看到这个？后台到底发生了什么？</em> 答案常常直接解锁一条更好的消息。新产品上线时用泛化 fallback 可以接受——前提是有流程在上线一个月后回头复审它们。</p>\n",
  "frontend-large-data-performance": "<h2>大数据量前端性能与流式渲染</h2>\n<p>处理大文件（50-300MB）与流式数据的前端性能方法论。原则：<strong>benchmark 驱动、增量化、先低成本优化后换技术</strong>。</p>\n<h3>1. Benchmark 驱动的优化流程</h3>\n<ul>\n<li>先建多规格基准（如 50/150/300MB 大文件 + 高记录数小文件），每个优化跑单次 + 3-run 对比。</li>\n<li>每个优化配等价性回归测试；「收益被剩余成本掩盖」时如实报告，不虚报。</li>\n<li>验收有量化门槛，达标即停。</li>\n</ul>\n<h3>2. 四类高频根因（Worker 流式解析场景）</h3>\n<ol>\n<li><strong>batch 合并 O(n²)</strong>：每 batch spread 复制历史记录。→ append-only backing array 累加（引用稳定）+ 版本号信号驱动 UI。</li>\n<li><strong>每 batch 全量重派生</strong>：聚合/概览/可见集全量重扫。本质是聚合层没把 append-only 当增量。→ 增量聚合状态（O(n)→O(delta)）；无过滤条件时统计直接复用 parser 内部 stats（O(1)）。</li>\n<li><strong>观察者与虚拟列表冲突</strong>：app 级 observer 与虚拟列表重复观察。→ 导出阈值，超过阈值时 app observer 提前返回。</li>\n<li><strong>搜索每次输入全量重读</strong>：只对文件路径搜索加 debounce，内存内搜索保持即时。</li>\n</ol>\n<h3>3. WASM/Rust 替换的第一性检验</h3>\n<p>想用 WASM 加速解析前先过四问：</p>\n<ol>\n<li>瓶颈真的在解析吗？（浏览器 <code>JSON.parse</code> 是引擎内建优化路径；实测 10MB parse p95 仅 ~34ms，瓶颈常在数据结构构造/内存分配/React 渲染）</li>\n<li>跨边界成本算了吗？（最终仍要给 React 一个 JS 对象图，WASM 解析完可能复制两次内存）</li>\n<li>工程成本算了吗？（async init、打包、多端加载差异、CSP、双语言维护 schema 契约）</li>\n<li>有量化验收门槛吗？</li>\n</ol>\n<p>结论模式：先做低成本优化 + 补基准，<strong>只在目标规格失败时才做限定范围的 WASM spike</strong>。技术选型是战略选项，不是默认路径。</p>\n<h3>4. 大文档渲染（万字级重排）</h3>\n<ul>\n<li>超长文档不直接滚动，用成熟分页引擎（EPUB 场景选 foliate-js：paginator reflow 分页 + CFI 做进度/批注定位）。</li>\n<li>改字号等全文重排操作慢时，可同时用交互设计弱化延迟可见性（如重排期间隐藏页码）。</li>\n</ul>\n<h3>5. 流式 UI 协议（LLM 输出场景）</h3>\n<ul>\n<li><strong>已渲染内容不得重动画</strong>：流式更新时保持 <code>components</code> 等引用稳定（memo/ref），否则已渲染节点被重建、逐词动画反复播放。CJK 分词用对应插件。</li>\n<li><strong>流式图表用 init/append 协议</strong>：不让前端 parse 半成品 JSON——init 先渲染空图框+骨架，append 增量加点；协议文档硬约束「长序列拆多次 append」。</li>\n<li><strong>流式部件按增量透传</strong>：消费 SDK 的 <code>partial_json</code> 单 chunk，按 toolUseId 累积 buffer + 按 id upsert，不整段替换。</li>\n<li><strong>布局稳定</strong>：流式期间只保留单一「进行中视图」，完成后一次性重组最终布局（思考区/回答区/工具区），避免内容在区域间搬家跳动。</li>\n<li>占位符链路：文本流出现 <code>[[chart:x]]</code> 占位 → 挂载/增量事件更新 store → 流式阶段立即渲染。</li>\n</ul>\n<h3>6. 运行时坑</h3>\n<ul>\n<li><strong>Bun 长流式</strong>：默认 <code>idleTimeout</code> 10s 会中断长流式并触发 abort，服务入口设 <code>idleTimeout: 0</code>。</li>\n</ul>\n",
  "git-commit-message": "<h2>Git Commit Message</h2>\n<p>写 commit message 时遵循本规范。一条好的 commit message 传达的是 diff 无法表达的 <strong>context</strong>——diff 说明改了「什么」，只有 message 能说清「为什么」。</p>\n<h3>核心心智</h3>\n<ul>\n<li>diff 回答 <em>what changed</em>；commit message 回答 <em>why</em>。把精力放在解释原因上。</li>\n<li>未来读这条 message 的人可能是几个月后的你自己。为「重建上下文」省下的时间就是它的价值。</li>\n<li>一条 commit 只做一件逻辑上内聚的事（atomic commit）。如果 subject 难以在 50 字符内概括，通常说明这次改动塞了太多东西，应拆分。</li>\n</ul>\n<h3>七条规则</h3>\n<ol>\n<li><strong>Subject 与 body 用空行分隔。</strong> 简单改动可只有 subject。一旦需要 body，subject 与 body 之间必须留一个空行，否则 <code>git log</code>、<code>shortlog</code>、<code>rebase</code> 等会解析错乱。</li>\n<li><strong>Subject 控制在 50 字符左右。</strong> 50 是经验值不是硬限，72 是硬上限（GitHub 超过会截断）。逼自己用最凝练的方式概括。</li>\n<li><strong>Subject 首字母大写。</strong>（英文 message；中文项目按团队既有惯例，保持一致即可。）</li>\n<li><strong>Subject 结尾不加句号。</strong> 标点浪费宝贵的字符空间。</li>\n<li><strong>Subject 用祈使语气（imperative mood）。</strong> 像下命令一样写：<code>Add</code>、<code>Fix</code>、<code>Remove</code>、<code>Refactor</code>，而非 <code>Added</code> / <code>Fixes</code> / <code>Changing</code>。</li>\n<li><strong>Body 每行折到 72 字符。</strong> git 不会自动换行，需手动折行，给缩进留出余量、整体控制在 80 以内。</li>\n<li><strong>Body 说明 what 和 why，而非 how。</strong> 代码本身已经说明了 how。重点讲：改动前是怎样的、哪里有问题、现在怎样、为什么这样解决。副作用和不直观的后果也在这里交代。</li>\n</ol>\n<h3>祈使语气自检</h3>\n<p>subject 必须能填入这个句子并读通：</p>\n<blockquote>\n<p>If applied, this commit will <strong>______</strong></p>\n</blockquote>\n<ul>\n<li>✅ <em>If applied, this commit will</em> <strong>refactor subsystem X for readability</strong></li>\n<li>✅ <em>If applied, this commit will</em> <strong>remove deprecated methods</strong></li>\n<li>❌ <em>If applied, this commit will</em> <del>fixed bug with Y</del></li>\n<li>❌ <em>If applied, this commit will</em> <del>changing behavior of X</del></li>\n<li>❌ <em>If applied, this commit will</em> <del>more fixes for broken stuff</del>（这是在描述内容，不是命令）</li>\n</ul>\n<h3>模板</h3>\n<pre><code>Summarize change in around 50 chars, imperative mood\n\nExplain the problem this commit solves. Focus on why, not how\n(the code shows how). Describe how things worked before and what\nwas wrong with that. Wrap body lines at 72 characters.\n\nAre there side effects or unintuitive consequences? Explain them\nhere.\n\n - Bullet points are okay\n - Use a hyphen or asterisk with a space, blank lines between\n\nResolves: #123\nSee also: #456, #789\n</code></pre>\n<p>单行足够时直接：</p>\n<pre><code>$ git commit -m &quot;Fix typo in introduction to user guide&quot;\n</code></pre>\n<h3>好坏对照</h3>\n<p>坏（长度形式各异、指向不清、堆细节）：</p>\n<pre><code>fixed two build-breaking issues: + reverted ClassMetadata... + eliminated ...\nTweaks to package-info.java files\npolishing\nMore fixes for broken stuff\n</code></pre>\n<p>好（简洁一致、祈使语气、可独立阅读）：</p>\n<pre><code>Fix failing CompositePropertySourceTests\nRework @PropertySource early parsing logic\nAdd tests for ImportSelector meta-data\nSimplify serialize.h&#39;s exception handling\n</code></pre>\n<p>带 body 的范例（讲清 why，不讲 how）：</p>\n<pre><code>Simplify serialize.h&#39;s exception handling\n\nRemove the &#39;state&#39; and &#39;exceptmask&#39; from serialize.h&#39;s stream\nimplementations, as well as related methods.\n\nAs exceptmask always included &#39;failbit&#39;, and setstate was always\ncalled with bits = failbit, all it did was immediately raise an\nexception. Get rid of those variables, and replace the setstate\nwith direct exception throwing (which also removes some dead code).\n</code></pre>\n<h3>执行清单</h3>\n<p>生成或审查 commit message 时逐条检查：</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> subject 与 body 之间有空行（若有 body）</li>\n<li><input disabled=\"\" type=\"checkbox\"> subject ≤ 50 字符（绝不超过 72）</li>\n<li><input disabled=\"\" type=\"checkbox\"> subject 首字母大写、结尾无句号</li>\n<li><input disabled=\"\" type=\"checkbox\"> subject 是祈使语气，能通过 &quot;If applied, this commit will…&quot; 测试</li>\n<li><input disabled=\"\" type=\"checkbox\"> body 每行 ≤ 72 字符</li>\n<li><input disabled=\"\" type=\"checkbox\"> body 解释了 why 与 what，没有复述代码已能表达的 how</li>\n<li><input disabled=\"\" type=\"checkbox\"> 这是一次 atomic 改动；若 subject 难以概括，考虑拆分 commit</li>\n<li><input disabled=\"\" type=\"checkbox\"> issue / PR 引用放在 body 末尾（<code>Resolves: #123</code>）</li>\n</ul>\n<h3>边界</h3>\n<ul>\n<li>Metadata（issue ID、PR 号）统一放 body 底部。</li>\n<li>项目已有既定惯例（如 Conventional Commits 的 <code>feat:</code> / <code>fix:</code> 前缀、语言选择）时，服从项目惯例；本规范约束的是那之下的结构与表达质量。</li>\n</ul>\n",
  "ios-on-device-vision-ml": "<h2>iOS 端侧视觉识别（相机 → Vision 分割 → 分类/颜色 → 评估迭代）</h2>\n<p>端侧识别 pipeline 的完整经验：<strong>取景框裁切 → Vision 前景分割 → 单次分割产出三份结果（预览抠图/识别图/落库图）→ 分类 + 主色 + 兜底</strong>。核心原则：同一份主体分割结果贯穿识别与落库，避免偏差源发散。</p>\n<h3>1. 相机取景框：所见即所得是几何问题</h3>\n<ul>\n<li>最常见根因：取景框只是「视觉提示」，拍照链路实际做固定比例中心裁切——<strong>取景框与真实裁切不是同一区域</strong>，背景污染由此而来。</li>\n<li>正确做法是显式几何映射：原始完整帧尺寸 + preview 实际 bounds + <code>aspectFill</code> 反推，保证「取景框看到什么，裁出来就是什么」。不要用 <code>metadataOutputRectConverted</code> 这类坐标语义黑盒。</li>\n<li>捕获链路拆两份：<strong>完整原始帧</strong>（存档，供回看/重裁）+ <strong>取景框裁剪工作帧</strong>（供识别）。不要把裁后图当原图保存。</li>\n<li>焦段切换按<strong>运行时探测真机能力</strong>实现：绑定后置虚拟多摄（triple→dualWide→dual→wide→ultraWide→telephoto 优先级），读 <code>constituentDevices</code> + <code>virtualDeviceSwitchOverVideoZoomFactors</code> 推导真实焦段；默认焦段按视角从广到窄排序取最广，不依赖系统数组顺序。</li>\n</ul>\n<h3>2. Vision 前景分割</h3>\n<ul>\n<li><code>VNGenerateForegroundInstanceMaskRequest</code>（iOS 16+，零体积）。<strong>不能用 <code>allInstances</code></strong>——床/地板等显著前景会一起保留；正确做法：逐实例生成 mask + 几何评分 + 分类评分，只选中心主实例。</li>\n<li>mask 外扩 1-2px 压边缘漏光。</li>\n<li>能力边界要诚实：该 API 只擅长主体与背景边界明显的场景；系统相册抠图更强是产品层后处理的结果，公开 API 离成品有工程距离。轻量分割优先于重型模型（SAM 类）。</li>\n</ul>\n<h3>3. 分类与颜色</h3>\n<ul>\n<li>主色识别在<strong>前景 mask 内</strong>做：高阈值筛选 + 中心加权 + 主色分布峰值——不用平均 RGB（对阴影/反光/脏背景太脆弱）。</li>\n<li>采集区收紧：分析帧取中心 ~86%，入库图取中心 ~72%。</li>\n<li>重复识别的根因常是「事件边界过早」：改状态机「变化开始 → 重新稳定 → 单次截帧」+ 短时视觉重复抑制。</li>\n<li>误差标准分层：<strong>exact label / coarse category / color 三种准确率分开统计</strong>；近义细类（短裤↔泳裤）可容错，颜色错误不可接受。提供用户手动修正闭环（细类候选可点选，修正入本地记录表）。</li>\n</ul>\n<h3>4. Mac 上做模型评估</h3>\n<ul>\n<li>准确性评估在 Mac 上即可，端侧性能/体验才需要真机。</li>\n<li>用<strong>接近 iOS 运行时的格式</strong>评估（CoreML <code>.mlpackage</code> / LiteRT <code>.litertlm</code>），不要用 Transformers/Safetensors——后者不代表 App 内表现。</li>\n<li>评估集命名编码标签（<code>syn_001_tshirt_red_good.png</code>）+ manifest.csv + 批量评估脚本 + contact sheet 人工抽检；多模型（原始/int8/p4 量化）跑同一回归。</li>\n<li>先 <code>du -sh</code> 核实模型真实体积，不假设压缩状态。</li>\n<li>CLI 单次进程加载不代表 App 常驻引擎的延迟表现。</li>\n</ul>\n<h3>5. 合成数据路线（独立开发者无真实数据）</h3>\n<ul>\n<li><strong>评估集</strong>：合成图只当「边界回归集」（样本少、标签清楚、覆盖弱点）；真实产品指标必须靠真机拍摄验收集。</li>\n<li><strong>训练集</strong>：可行且性价比最高，定位「补覆盖率」；核心风险是学到生成器自身纹理偏差。策略：<strong>生成图补覆盖率，真实图定方向，现有大模型做蒸馏约束</strong>，控制 synthetic 比例。</li>\n<li>多样性固定成<strong>标签矩阵</strong>（label × placement × background × lighting × pattern × quality），可复现可扩展。</li>\n<li>批量生成：先 24-48 张 pilot 验证风格与标签，再走 API 批量（按几百张分块，防单次失败难恢复），输出图片 + manifest.csv。</li>\n<li>公开数据集的核心矛盾是<strong>分布差异</strong>（电商图 vs 真实使用场景）；可用分割 mask 把前景贴到目标场景背景做域增强。</li>\n</ul>\n<h3>6. 模型迭代路线与隐私边界</h3>\n<ul>\n<li>能力拆成<strong>多个可独立评估的头</strong>（类别/颜色/图案/质量），不压进一个大模型。</li>\n<li>顺序：固定评估集 + 收集真实错误样本 → 模型压缩 → 蒸馏训练小模型 → 必要时转向可下载端侧 VLM（准确率优先于速度；把慢速识别绑定到低频动作上使其可接受）。</li>\n<li><strong>模型即可提取资产</strong>：端侧只放粗分类/白名单能力，高价值细粒度能力放云端或独立更新链路。</li>\n<li>用户数据训练闭环走本地优先：本地记录修正标签，导出前明示字段，图片默认由用户主动选择是否包含。</li>\n</ul>\n",
  "ios-recording-system-integration": "<h2>iOS 录音类系统集成（灵动岛 / Action Button / 后台录音 / 实时 ASR）</h2>\n<p>灵动岛、Action Button、后台录音是<strong>一个耦合系统</strong>。开发前先接受四条 iOS 硬边界，产品设计必须围绕它们收敛：</p>\n<ol>\n<li><strong>初次麦克风录音的 <code>AVAudioSession</code> 激活必须在前台发生</strong>——第三方 App 没有系统级「桌面直接后台起录」的私有通道。</li>\n<li><strong>Action Button 是否已分配给本 App 不可读</strong>——没有公开 API，也不能深链到其设置页。</li>\n<li><strong>Live Activity 的 update 是系统托管的内容更新，不是实时流通道</strong>——高频 update 会被限流，表现为「上岛如同没上」。</li>\n<li><strong>后台不能触发权限请求</strong>——后台调 <code>requestAuthorization</code> 会把 App 拉到前台。</li>\n</ol>\n<h3>1. Live Activity / 灵动岛</h3>\n<p>三层结构：共享 <code>ActivityAttributes</code>（ContentState 含 statusText/elapsedSeconds/<code>timerStartDate</code>/isRecording/waveformSamples）→ actor 控制器（start/update/end）→ Widget target 的 <code>ActivityConfiguration</code> + <code>DynamicIsland</code>（compact/expanded/minimal + 锁屏视图）。</p>\n<p><strong>「能显示但像没显示」三段式排查</strong>：</p>\n<ul>\n<li>更新频率：转写等实时流<strong>不触发 update</strong>，后台更新节流到 ≥2 秒。</li>\n<li>计时不要依赖推送：用 <code>Text(timerStartDate, style: .timer)</code> 系统自走时；防秒数抖动用稳定文本 + 关隐式动画 + <code>.monospacedDigit()</code>。</li>\n<li>update 前 <code>guard activityState == .active</code>，不对失效 activity 推送。</li>\n</ul>\n<p>其他要点：</p>\n<ul>\n<li>点岛路由：<code>.widgetURL(...)</code> 要挂在 <strong><code>DynamicIsland</code> 本体</strong>上，不只是锁屏视图；App 回 active 时兜底检查录音状态。</li>\n<li>桌面入口起的后台录音会话与应用内录音页要接<strong>同一套预览组件</strong>，否则两条路径体验分裂。</li>\n<li>Live Activity 可做真开关（是否创建是 App 自己的行为）；Action Button 状态不可读所以不能做开关。</li>\n<li>Widget target 用 <code>-D WIDGET_EXTENSION</code> 编译标记让共享源码在扩展里走 fallback，扩展进程不得依赖主 App 录音服务。</li>\n</ul>\n<h3>2. Action Button（App Intents）</h3>\n<ul>\n<li>主 App Intent：<code>AudioRecordingIntent + LiveActivityIntent</code>，<code>openAppWhenRun=false</code>；新系统用 <code>supportedModes=[.background, .foreground(.dynamic)]</code>，旧系统 <code>ForegroundContinuableIntent</code>，<code>@available</code> 双分支兼容。Widget/Control 扩展的 Intent 必然 <code>openAppWhenRun=true</code>，只写 pendingAction 再开 App。</li>\n<li><strong>「未分配」状态：不要假装能读</strong>。UI 改成中性「系统动作：已提供」+ 引导 sheet（系统设置 → 操作按钮 → 选择本 App 动作）。通用经验：系统级绑定状态大多不可读，UI 应改为「提供动作 + 配置引导」。</li>\n<li><code>perform()</code> 状态机（冷后台起录不稳定的正解）：<pre><code>后台会话正在录音          → 停止并处理（再按=停止，永远可靠）\n已在录音 或 App 在前台     → 发通知让前台录音页接管\n冷后台首次               → 先试后台录音；失败 → 清 primed 标记 + continueInForeground 开 App\n</code></pre>\n用持久化 <code>audioSessionPrimed</code> 标记「历史上成功建过会话」。坑：把「当前进程必须 primed」当前置条件太保守，会退化成「一按就开 App」；正解是<strong>永远先试后台、失败才前台</strong>。</li>\n<li>错误码解读：<code>AUIOClient_StartIO failed (2003329396)</code> = FourCC <code>&#39;what&#39;</code>，即冷后台起麦克风被拒。FourCC 解码是音频错误排查的第一步。</li>\n</ul>\n<h3>3. 实时 ASR WebSocket 接入</h3>\n<p>统一抽象：<code>protocol SpeechTranscribing</code> + 按设置分发的包装器（Apple Speech / 提供商 A / 提供商 B），错误收敛到单一 error 类型。</p>\n<p><strong>两种协议范式</strong>：</p>\n<ul>\n<li><strong>OpenAI-Realtime 风格 JSON 事件流</strong>（如通义千问）：Bearer 鉴权 + <code>OpenAI-Beta: realtime=v1</code>；握手 <code>session.created → session.update</code>（pcm/16k、server_vad、language）→ <code>session.updated</code> 后才进收循环；上行 <code>input_audio_buffer.append</code> + base64；下行增量 text → completed 段落定稿 → <code>session.finish</code> 拿最终稿。拿到最终稿直接保存，<strong>不做二次转写</strong>。</li>\n<li><strong>自定义二进制帧</strong>（如火山豆包）：多 header 鉴权（App-Key/Access-Key/Resource-Id）；4 字节头（version/msgType/flags/serial/compression）+ 大端长度 + <strong>Gzip 压缩 payload</strong>；时序 fullClientRequest → audioOnly 帧 → 空 payload + lastPacket 收尾。</li>\n<li>方言/语言能力按接口型号确认（流式与非流式支持面常不同），产品文案不要把「自动识别」包装成「多语种选择」。</li>\n</ul>\n<p><strong>通用音频管线</strong>（所有提供商复用）：<code>AVAudioConverter</code> 重采样到 PCM 16kHz/Int16/mono，累积 200ms 再分块发送。</p>\n<h3>4. 后台录音与权限</h3>\n<ul>\n<li>会话配置：<code>.playAndRecord</code> + <code>[.mixWithOthers, .allowBluetoothHFP]</code>；停止时 <code>setActive(false)</code>。</li>\n<li><code>Info.plist</code>：<code>NSSupportsLiveActivities</code>、<code>UIBackgroundModes=[audio]</code>、麦克风/语音识别 usage 描述。</li>\n<li>后台只用 <code>authorizationStatus()</code> 判断权限，绝不 request；未授权降级为只录音。</li>\n<li><strong>启动顺序</strong>：先起 <code>AVAudioEngine</code> 录音硬件，成功后再挂实时转写；转写不可用不能让录音失败。</li>\n</ul>\n<h3>5. 工程惯例</h3>\n<ul>\n<li>XcodeGen 管工程：改 <code>project.yml</code> → <code>xcodegen generate</code>，绝不手改 pbxproj。</li>\n<li>跨进程状态（App / Widget 扩展 / Intent）用 UserDefaults 传递（isRecording / pendingAction / audioSessionPrimed）。</li>\n<li>并发：控制器与 ASR core 用 actor；音频回调 <code>@unchecked Sendable</code> + 锁。</li>\n<li>调试方法论：先拿真机 Console 日志 → FourCC 解码错误码 → <code>rg</code> SDK swiftinterface 确认协议边界，不猜；反复翻车后先列 3-5 条修复路径再选最小可靠方案。</li>\n</ul>\n",
  "ios-widget-appstore-shipping": "<h2>iOS Widget 开发与 App Store 上架</h2>\n<p>WidgetKit 扩展的数据共享/刷新经验 + 完整上架 checklist（含真实被拒案例的根因）。</p>\n<h3>1. Widget 与主 App 的数据共享</h3>\n<p><strong>根本约束</strong>：Widget Extension 是独立 bundle，<strong>不能把主 App bundle 当自己的资源目录读</strong>——典型症状是「App 内预览能找到数据、真实桌面 widget 找不到」（资源 target 归属不一致）。</p>\n<p>标准方案（<strong>App Group 共享单份</strong>，不要两个 target 各打包一份）：</p>\n<ol>\n<li>App 与 Widget 配同一个 App Group entitlement（group id 完全一致）。</li>\n<li>App 首启/onboarding 时把数据从 bundle 复制到 App Group container；Widget 只从 container 读，读不到时显示「请打开 App 完成初始化」占位（与 onboarding 动线契合）。</li>\n<li>App Group container 是<strong>持久化磁盘存储</strong>：首次复制成功后重启手机不需重新打开 App；只有卸载、数据版本更新、复制中断三种情况需要重新复制。</li>\n<li>大数据不要整包压缩（牺牲随机访问）；用面向访问模式的紧凑二进制格式（如半字节打包），并把「共享方案」与「数据格式」拆成两个独立目标分别验证。</li>\n</ol>\n<h3>2. 尺寸、刷新与亮暗</h3>\n<ul>\n<li>small/medium/large 分开渲染；App 内预览必须固定成真实 small 比例，不能撑满页宽。</li>\n<li>固定列数不能只调 columns，要给足横向内容宽度，否则 SwiftUI 压缩截断。卡片背景用 <code>.containerBackground(.fill.tertiary, for: .widget)</code>。</li>\n<li><strong>「改设置 widget 不变」两层根因</strong>：(a) 配置项若是 AppIntent widget 配置（每个已放置 widget 独立持有），App 内设置改了不影响桌面既有 widget；(b) 桌面 widget 有 timeline 缓存，需长按编辑或移除重加强制刷新。</li>\n<li>Widget 的 Info.plist 太极简会在 <strong>AppIntents 训练阶段</strong>报 <code>Unable to parse Info.plist</code>——补齐 <code>CFBundleIdentifier/CFBundleExecutable/CFBundlePackageType</code> 等标准键。</li>\n<li>亮暗分两个独立策略：App 界面跟随系统；Widget 视觉主题固定，不随亮暗大面积重排。</li>\n</ul>\n<h3>3. 「模拟器好、真机坏」排查</h3>\n<ul>\n<li>立刻切真机 destination 构建，检查 <code>Debug-iphoneos</code> 产物；<strong>模拟器产物不能推断真机</strong>（thinning 结果不同，如模拟器 @2x、真机 @3x）。</li>\n<li>常用命令：<code>xcrun assetutil --info Assets.car</code>、<code>plutil -p</code>、<code>sips -g hasAlpha</code>、<code>PlistBuddy -c &#39;Print :CFBundleIcons&#39;</code>。</li>\n<li>真实案例结论：桌面图标只在开/退 App 闪现 1 秒——一路「合理假设」（缺 @3x、Dark/Tinted 变体、Liquid Glass .icon）逐个被最小实验证伪，<strong>真正根因是 SpringBoard 图标缓存卡死，重启手机即恢复</strong>。教训：靠读产物 + 最小实验逐层证伪，不靠假设堆叠。</li>\n<li>上传后 ASC 页面图标占位：若归档包内图标正常，通常是 Apple 侧缓存未刷新，不阻塞 TestFlight。</li>\n</ul>\n<h3>4. 上架 checklist</h3>\n<ul>\n<li><strong>版本号</strong>：UI 显示从 bundle 读 <code>CFBundleShortVersionString</code>，不硬编码。</li>\n<li><strong>加密豁免</strong>：<code>ITSAppUsesNonExemptEncryption = false</code>，<strong>主 App 和每个 Extension 的 Info.plist 都要加</strong>（每个 bundle 独立扫描）。HTTPS/WSS 属可豁免的标准加密；但 ASC 手动回答时不要选「完全不使用加密」。</li>\n<li><strong>TestFlight</strong>：内部（≤100 ASC 用户）→ 外部（≤10000，首个 build 需过 Beta App Review）。Archive 时 destination 必须是 Any iOS Device/真机；CI 构建成功 ≠ 自动上 TF，首次建议手动 Archive 跑通证书/合规。地区控制在 Pricing and Availability。</li>\n<li><strong>截图</strong>：1242×2688 或 1284×2778 PNG，按语言分目录；可包装但不画不存在的功能；用户真实内容必须脱敏替换为虚构示例；不暴露第三方服务商品牌；<strong>绝不放非 iOS 状态栏（假状态栏）——真实被拒条款 2.3.10</strong>。</li>\n<li><strong>Review Notes（BYOK 类应用范式）</strong>：讲清「默认审核路径零配置可跑通」（无需账号/key）与「BYOK 边界」（第三方服务默认关闭、仅用户手动启用+自有凭证、凭证本地存储）。</li>\n</ul>\n<h3>5. 审核被拒的根因分析方法</h3>\n<p>先把拒绝原因拆三类：<strong>改代码 / 改 ASC 元数据 / 改隐私政策</strong>，并精读 Apple 原文措辞，不凭印象误判。真实案例：</p>\n<ul>\n<li><strong>1.3 Kids Category</strong>：真因是勾选了 Kids Category。4+ 年龄分级 ≠ Kids Category，取消勾选即可（分级问卷结果可保留）。</li>\n<li><strong>2.3.10</strong>：真因是截图里的假状态栏，不是第三方品牌。</li>\n<li><strong>5.1.1</strong>：Apple 原文 &quot;revise the <strong>app</strong>&quot; 意味着<strong>仅改隐私政策不够</strong>——必须在 App 内、把数据发给第三方 AI 之前做清晰披露+授权弹窗（说明发送什么、发给谁，允许后才启用；系统能力不弹）。</li>\n</ul>\n<h3>6. 本地化与其他</h3>\n<ul>\n<li>用 String Catalog（<code>.xcstrings</code>）；<strong>App 内切换语言 = 跳系统设置</strong>（<code>openSettingsURLString</code>），不做 App 内语言覆盖——避免 App / Widget / AppIntent 三处语言规则分裂。</li>\n<li>Widget 配置文案用 <code>LocalizedStringResource</code> / <code>DisplayRepresentation</code> 本地化。</li>\n<li>反复不生效的低价值功能（如 App 内亮暗切换）果断砍入口改为跟随系统，不无限投入。</li>\n<li>CarPlay：Audio category 一般不允许录音；录音例外仅 navigation 与 voice-based conversational（且限 voice control template）。「已过 CarPlay Audio 审批 ≠ 允许录音」；拿 Case-ID 向 Apple 追问明确 category。</li>\n<li>嵌套 git 仓库：子目录自带 <code>.git</code> 会成为嵌套边界，<code>git add</code> 无效；需移除嵌套 .git 元数据才能纳入父仓库。</li>\n</ul>\n",
  "issue-driven-dev-loop": "<h2>Issue 驱动开发循环</h2>\n<p>围绕本地 <code>.issues/</code> 目录（或 Linear 等外部 tracker）的完整开发循环：领取 issue → 细化设计并确认 → 实现 → 提交 PR → merge 后更新状态 → 继续下一个。</p>\n<h3>1. Issue 的组织方式（本地 <code>.issues/</code>）</h3>\n<pre><code>.issues/\n├── .config.yaml              # prefix、init_number\n├── backlog/                  # 全局未排期\n├── v0.5.0/                   # 里程碑目录\n│   ├── milestone.yaml\n│   ├── backlog/ todo/ in-progress/ in-review/ done/ cancelled/\n└── _archive/                 # 已关闭里程碑整体移入\n</code></pre>\n<ul>\n<li>单个 issue 是一个<strong>目录</strong>（如 <code>ISS-401/</code>，前缀由 <code>.config.yaml</code> 定义），内含 <code>meta.yaml</code>（必需，唯一 source of truth）+ 按需的 <code>design.md</code>（既定方案）、<code>discussion.md</code>（时间倒序讨论日志）、<code>notes.md</code>（实施期偏差决策，不改写原始定义）。</li>\n<li><strong>状态流转 = 物理移动目录</strong>，同时必须同步 <code>meta.yaml</code> 的 <code>status</code> 字段（双冗余，两处都要改）。</li>\n<li><code>meta.yaml</code> 关键字段：<code>id</code>、<code>title</code>、<code>type</code>(feature|bug|chore|refactor|docs)、<code>priority</code>(P0-P3)、<code>status</code>、<code>milestone</code>、<code>pull_requests[]</code>(url/status/branch)、<code>depends_on</code>/<code>blocks</code>、<code>completed_at</code>、<code>summary</code>。</li>\n<li>定位 issue 永远先 <code>find .issues -type d -name &quot;ISS-401&quot;</code>，不要假设它在哪个状态目录。</li>\n</ul>\n<h3>2. 开工序列（不要拿到 issue 就写代码）</h3>\n<ol>\n<li>读约束文件：issue-tracker 的 SKILL/格式说明、commit 规范、项目 <code>AGENTS.md</code>。</li>\n<li><code>git status --short --branch</code> 确认在干净的 main。</li>\n<li>盘点 backlog：读全部 <code>meta.yaml</code> + <code>design.md</code>。</li>\n<li><strong>按「谁为谁建立契约边界」推导依赖顺序</strong>，而不是按编号或优先级机械排序，写出处理计划。</li>\n<li>已有详细设计的 issue 直接实现（用户可预授权「待确认项采用默认方案」）；需细化的走：扩写 design.md 并把不确定项标「待讨论决策」→ 用户拍板 → <strong>先去代码核实再写回文档</strong>（避免写虚）→ 把「待讨论」改为「明确决策」→ 用户确认后才动代码。待确认事项必须固化进 design.md，不能只留在对话里。</li>\n</ol>\n<h3>3. 分支与 commit</h3>\n<ul>\n<li><strong>模式由用户指令决定</strong>：<ul>\n<li>一 issue 一 PR：独立分支 <code>feat/{issue-id}-{slug}</code>（如 <code>feat/iss-42-windowed-data-load</code>）。</li>\n<li>直接 main：不开分支，靠合理拆分 commit + issue 状态推进；commit body 加 <code>Refs: ISS-xxx</code> 追溯，可带 <code>Verification: &lt;验证命令&gt;</code>。</li>\n</ul>\n</li>\n<li>commit 遵循约定式提交：类型前缀 + 可选 scope（<code>fix(web):</code>），subject 祈使语气、首字母大写、≤50 字符、无尾标点。<strong>一个逻辑边界一个 commit</strong>，绝不混两个 issue。</li>\n<li><strong>修 bug 铁律：先加日志读运行时输出，再改代码</strong>——插临时标记日志（如 <code>[ISS-42]</code>），跑测试读真实执行顺序确认根因，不静态猜测。</li>\n</ul>\n<h3>4. 提交 PR</h3>\n<ol>\n<li><code>git push -u origin &lt;branch&gt;</code>。</li>\n<li>创建 <strong>draft PR</strong>（默认 draft，不直接 open）；有 <code>.github/pull_request_template.md</code> 则套模板。</li>\n<li>PR 标题：约定式提交格式、英文小写；PR 正文英文，结构：<code>## Summary</code> / <code>## Root cause</code>(bug 类) / <code>## Impact</code> / <code>## Validation</code>(验证命令) + 结尾 <code>Issue: ISS-42</code>。</li>\n<li><code>gh pr view &lt;branch&gt; --json number,url,state,isDraft</code> 回读确认。</li>\n<li><strong>建 PR 后立即回写 issue</strong>：status <code>in-progress→in-review</code>，追加 <code>pull_requests</code> 条目，<code>mv</code> 目录到 <code>in-review/</code>。</li>\n<li>跟进 CI：失败则读日志定位、本地修复补 commit push；<strong>根因具有普遍性时把预防规则写进 AGENTS.md</strong> 防复发。</li>\n</ol>\n<h3>5. Merge 后收尾与下一个</h3>\n<ol>\n<li><code>gh pr view &lt;n&gt; --json state,mergedAt</code> 核实确已 <code>MERGED</code>。</li>\n<li>更新 meta.yaml：<code>in-review→done</code>、PR status→<code>merged</code>、填 <code>completed_at</code>；<code>mv</code> 到 <code>done/</code>。</li>\n<li><code>git switch main &amp;&amp; git pull</code>，从最新 main 建下一分支。</li>\n<li>按依赖顺序取下一个 issue，<code>mv</code> 到 <code>in-progress/</code>，循环。</li>\n<li>里程碑发布后经确认将整个里程碑目录 <code>mv</code> 到 <code>_archive/</code>。</li>\n</ol>\n<h3>6. 并行处理（大批量 backlog）</h3>\n<p>主 agent 先画依赖图，把<strong>写入文件范围互不重叠</strong>的 issue 分给 worker 子代理并行；每个子代理指令强制：不创建分支、不提交 commit、不改 <code>.issues/</code>、不 revert 他人改动、只改指定文件。返回后主 agent review diff 统一提交。会改同一文件群的 issue 必须串行。</p>\n<h3>7. 关键坑位清单</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> <strong>先探测 <code>.issues/</code> 是否在 .gitignore</strong>：有的项目忽略（状态只留本机），有的纳入 git（issue 移动要随代码一起 commit）——这是最易错的点。</li>\n<li><input disabled=\"\" type=\"checkbox\"> meta.yaml <code>status</code> 与所在目录双向一致。</li>\n<li><input disabled=\"\" type=\"checkbox\"> 语言分工固定：issue 文档/讨论/commit body/汇报用中文；commit subject/PR 标题/PR 正文用英文；代码、类型、字段、路径永不翻译。</li>\n<li><input disabled=\"\" type=\"checkbox\"> 尊重项目 AGENTS.md 的特殊要求（如 shell 命令前缀），不要写死通用命令。</li>\n</ul>\n",
  "issue-tracker": "<h2>Issue Tracker Skill</h2>\n<p>Local-first, Markdown-based issue tracking that lives inside the project repository. Replaces Linear / GitHub Issues for lightweight project management with full offline access.</p>\n<h3>When to Read References</h3>\n<p>Before any operation, read the relevant reference:</p>\n<ul>\n<li><strong>First time or init</strong>: <code>references/directory-spec.md</code> (directory layout, naming, init flow)</li>\n<li><strong>Creating or editing issues</strong>: <code>references/issue-format.md</code> (meta.yaml schema, design.md template, discussion.md conventions)</li>\n<li><strong>Generating HTML</strong>: <code>DESIGN.md</code> (visual system) and <code>references/html-template.md</code> (generation workflow)</li>\n</ul>\n<h3>Core Concepts</h3>\n<h4>Directory Layout</h4>\n<pre><code>.issues/\n├── .config.yaml              # project-level config (prefix, init_number, etc.)\n├── backlog/                   # unscheduled issues\n│   └── RD-401/\n├── v0.5.0/                    # milestone directory\n│   ├── milestone.yaml         # milestone metadata\n│   ├── backlog/               # planned but not started\n│   ├── todo/                  # ready to work on\n│   ├── in-progress/           # actively being worked\n│   ├── in-review/             # PR submitted, awaiting review\n│   ├── done/                  # completed\n│   └── cancelled/             # dropped from this milestone\n├── v0.6.0/\n│   └── ...\n└── _archive/                  # closed milestones move here\n</code></pre>\n<h4>Issue Directory</h4>\n<p>Each issue is a directory named by its ID (e.g., <code>RD-401/</code>):</p>\n<pre><code>RD-401/\n├── meta.yaml        # structured metadata (required)\n├── design.md        # detailed design document (created when needed)\n├── discussion.md    # chronological discussion log (created when needed)\n├── notes.md         # implementation notes and decision drift (created when needed)\n└── design.html      # rendered HTML version (generated on demand)\n</code></pre>\n<h4>Status Flow</h4>\n<pre><code>backlog → todo → in-progress → in-review → done\n                                          → cancelled\n</code></pre>\n<p>Moving an issue between statuses means physically moving its directory from one status folder to another within the same milestone. Moving to a different milestone means moving it to that milestone&#39;s appropriate status folder.</p>\n<h4>Issue ID</h4>\n<p>Format: <code>{PREFIX}-{NUMBER}</code>, e.g., <code>RD-401</code>.</p>\n<ul>\n<li>PREFIX is defined per project in <code>.config.yaml</code></li>\n<li>NUMBER auto-increments from <code>init_number</code> (to avoid collision with existing Linear issues)</li>\n</ul>\n<h3>Operations</h3>\n<h4>1. Initialize Tracking</h4>\n<p>When user says &quot;初始化 issue 跟踪&quot; or equivalent:</p>\n<ol>\n<li>Read <code>references/directory-spec.md</code></li>\n<li>Ask for: project prefix (default from repo name), init_number (starting issue number)</li>\n<li>Create <code>.issues/</code> directory with <code>.config.yaml</code> and <code>backlog/</code></li>\n<li>Suggest adding <code>.issues/**/*.html</code> to <code>.gitignore</code> if HTML files should not be committed</li>\n</ol>\n<h4>2. Create Issue</h4>\n<p>When user says &quot;创建 issue&quot; / &quot;新建需求&quot; / &quot;开个 bug&quot;:</p>\n<ol>\n<li>Read <code>.issues/.config.yaml</code> to get prefix and next number</li>\n<li>Determine placement: global <code>backlog/</code> or a specific milestone&#39;s <code>backlog/</code></li>\n<li>Create the issue directory with <code>meta.yaml</code></li>\n<li>If the user provides design details in the same message, also create <code>design.md</code></li>\n<li>Increment the counter in <code>.config.yaml</code></li>\n</ol>\n<h4>3. Update Issue</h4>\n<p>When user says &quot;更新 RD-401&quot; or provides new design/discussion content:</p>\n<ol>\n<li>Find the issue by ID (search across all milestone and status directories)</li>\n<li>Update the relevant file (meta.yaml for status/PR changes, design.md for design, discussion.md for discussion)</li>\n</ol>\n<h4>4. Move Issue</h4>\n<p>When user says &quot;把 RD-401 移到 in-progress&quot; or &quot;RD-401 移入 v0.5.0&quot;:</p>\n<ol>\n<li>Locate the issue directory</li>\n<li>Move it to the target milestone/status directory</li>\n<li>Update meta.yaml timestamps</li>\n</ol>\n<h4>5. Query / List</h4>\n<p>When user says &quot;当前进度&quot; / &quot;看板&quot; / &quot;v0.5.0 的状态&quot;:</p>\n<ol>\n<li>Scan the relevant directories</li>\n<li>Present a summary grouped by status</li>\n<li>Include PR status if available in meta.yaml</li>\n</ol>\n<h4>6. Generate HTML</h4>\n<p>When user says &quot;生成 RD-401 的 HTML&quot; / &quot;导出 HTML&quot;:</p>\n<ol>\n<li>Read <code>DESIGN.md</code> and <code>references/html-template.md</code></li>\n<li>Read the issue&#39;s <code>meta.yaml</code>, <code>design.md</code>, and any relevant <code>notes.md</code> / <code>discussion.md</code></li>\n<li>Design a content-specific static HTML page from the markdown structure; do not use a generic Markdown-to-HTML script as the final output</li>\n<li>Save the handcrafted page as <code>design.html</code> inside the issue directory</li>\n</ol>\n<h4>7. Link PR</h4>\n<p>When user says &quot;关联 PR #117 到 RD-401&quot;:</p>\n<ol>\n<li>Update meta.yaml with PR URL and current status (draft/open/merged)</li>\n<li>If AI has GitHub access, fetch current PR state and update</li>\n<li>If a PR is newly created for the issue, move the issue to <code>in-review</code> only after the PR exists and <code>meta.yaml</code> has been updated with the PR URL, branch, status, and <code>updated_at</code></li>\n</ol>\n<h4>7a. Update Issue After PR Merge</h4>\n<p>When user says &quot;PR merged, 更新 issue&quot; or equivalent:</p>\n<ol>\n<li>Identify the issue from the explicit issue ID, the PR number/URL, the current branch&#39;s PR, or the branch recorded in <code>meta.yaml</code>. If more than one issue matches, ask before editing.</li>\n<li>Verify the PR state with GitHub CLI or a GitHub connector when available. If no GitHub access is available, state that the update is based on the user&#39;s report.</li>\n<li>Only move an issue to <code>done</code> when the relevant PR is confirmed or explicitly reported as merged, or when the user says the non-PR change is otherwise complete.</li>\n<li>Update <code>pull_requests[*].status</code>, <code>pull_requests[*].updated_at</code>, <code>status</code>, <code>updated_at</code>, and <code>completed_at</code>, then physically move the issue directory so its status folder matches <code>meta.yaml</code>.</li>\n<li>Leave <code>.issues/</code> changes uncommitted when the repository treats issue files as local-only.</li>\n</ol>\n<h4>8. Archive Milestone</h4>\n<p>When user says &quot;归档 v0.5.0&quot;:</p>\n<ol>\n<li>Move the entire milestone directory to <code>_archive/</code></li>\n<li>Keep all content intact</li>\n</ol>\n<h4>9. Record Implementation Notes</h4>\n<p>During implementation, if reality differs materially from the issue definition and forces a new decision, append the decision to the issue directory&#39;s <code>notes.md</code>. Record only meaningful deviations that change scope, design, validation, or tradeoffs; do not log routine progress.</p>\n<h4>10. Develop an Issue</h4>\n<p>When implementing an issue, before editing source code:</p>\n<ol>\n<li>Locate the issue directory and confirm its current status</li>\n<li>Inspect the current branch, upstream, and <code>git status --short</code>. Do not reuse a branch that already contains unrelated work.</li>\n<li>Create or switch to a dedicated git branch. Branch names must use Conventional Commit type prefixes and must not include the issue ID: <code>{type}/{slug}</code>, e.g., <code>feat/ipc-contract-map</code>, <code>fix/provider-keyring</code>, <code>refactor/settings-store</code>. Choose the type from the primary change being implemented, not mechanically from the issue type.</li>\n<li>Decide and state the commit plan before editing. Name the planned review units and the verification for each unit. Revise the plan if runtime findings change the real boundaries.</li>\n<li>Use one commit only when the issue is one coherent review unit. Otherwise split commits by API/contract change, data migration, user-visible behavior, visual/theme system, integration glue, or independent follow-up fix. Do not split tests away from the code they validate unless the test-only change is itself the review unit.</li>\n</ol>\n<p>During implementation:</p>\n<ol>\n<li>After each coherent review unit is implemented and verified, stage only the files for that unit and create a commit before moving to unrelated work.</li>\n<li>Each commit should carry one coherent behavior or design change. Avoid one giant &quot;implement issue&quot; commit when multiple review boundaries exist, and avoid one commit per file or mechanical &quot;fix lint&quot; commits unless that mechanical change is independently reviewable.</li>\n<li>Before committing, inspect <code>git diff --cached --name-only</code> and follow project instructions for local issue files. If <code>.issues/</code> is local-only for the repository, do not stage it.</li>\n<li>Use the repository&#39;s commit message rules. For issue implementation commits, write a useful commit body/description; do not make the body only <code>Refs: RD-123</code>.</li>\n<li>In the commit body, include the non-obvious context for that slice: why this slice exists, what boundary it changes, and how it was verified. Keep it short and wrap lines. Put the issue ID in a footer such as <code>Refs: RD-123</code> or <code>Issue: RD-123</code> after the explanatory body.</li>\n<li>Before giving a final implementation summary or opening a PR, run <code>git status --short</code>. If source, test, or documentation files remain modified or untracked, either commit them as a focused unit or explicitly state why they are intentionally uncommitted.</li>\n<li>Do not report an issue implementation as complete while source changes are still uncommitted, unless the user explicitly requested no commit or the work is blocked before a safe commit can be made.</li>\n<li>Treat the issue context as scoped to the current issue ID, branch, or linked PR. When the user starts an unrelated task in the same thread without referencing that issue, stop updating the old issue and start from normal git/worktree discovery.</li>\n</ol>\n<p>Commit body template for non-trivial issue commits:</p>\n<pre><code class=\"language-text\">{type}({scope}): {subject}\n\nExplain why this review unit exists and what boundary it changes.\nMention the verification that was run for this unit, or why it was not run.\n\nRefs: RD-123\n</code></pre>\n<p>Good issue commit split examples:</p>\n<ul>\n<li>Theme contract tokens first, then component rendering and animation behavior, then an independent runtime bug fix discovered during validation.</li>\n<li>Database/schema migration first, then application reads/writes, then UI integration if each layer is reviewable separately.</li>\n<li>Asset-only replacement as one commit when the work is only generated binaries and verification.</li>\n</ul>\n<p>Poor split examples:</p>\n<ul>\n<li>One commit named <code>feat: implement RD-123</code> that mixes contract, UI, tests, assets, and follow-up bug fixes.</li>\n<li>Separate commits for <code>update code</code>, <code>update tests</code>, and <code>fix format</code> when they validate the same behavior.</li>\n<li>Commit body containing only <code>Refs: RD-123</code>.</li>\n</ul>\n<h3>Locating an Issue</h3>\n<p>Issues can be in any milestone&#39;s any status directory, or in the global backlog. To find an issue by ID:</p>\n<pre><code class=\"language-bash\">find .issues -type d -name &quot;RD-401&quot; | head -1\n</code></pre>\n<p>Always search before assuming location. If the user references a PR instead of an issue ID, search <code>meta.yaml</code> files for that PR URL/number or branch name and use the issue only when there is exactly one match. Report the current location to the user when operating on an issue.</p>\n<h3>Writing Conventions</h3>\n<ul>\n<li>meta.yaml: YAML, structured fields, no prose</li>\n<li>design.md: 中文为主，技术术语保留英文，代码/类型/字段名不翻译</li>\n<li>discussion.md: 按时间倒序追加，每条记录带日期和参与者标识</li>\n<li>notes.md: 记录实现过程中与 issue 定义明显不一致而产生的新决策，按时间倒序追加</li>\n<li>HTML output: light theme only, self-contained single file, content-specific visual page based on <code>DESIGN.md</code></li>\n</ul>\n<h3>Integration with Existing Workflows</h3>\n<p>This system is designed to coexist with:</p>\n<ul>\n<li><strong>AGENTS.md</strong>: Issue IDs can be referenced in commit messages and AGENTS.md constraints</li>\n<li><strong>KLIP documents</strong>: Complex issues may reference or embed KLIP documents in their design.md</li>\n<li><strong>Git branches</strong>: Create a dedicated branch before issue implementation; use a Conventional Commit type prefix without the issue ID, e.g., <code>feat/ipc-contract-map</code></li>\n<li><strong>Commits</strong>: Split issue work into focused, reviewable commits that map to coherent behavior or design changes; include useful commit bodies for non-trivial issue commits, with the issue ID as a footer rather than the only description</li>\n<li><strong>PR descriptions</strong>: Include issue ID for traceability</li>\n</ul>\n",
  "klip-writing": "<h2>KLIP Writing Skill</h2>\n<p>Write structured technical design documents following the KLIP format conventions established in the Fin-Agent project.</p>\n<h3>What is a KLIP</h3>\n<p>KLIP (Kevin&#39;s Lightweight Improvement Proposal) is a technical document format used for feature design, architecture evaluation, migration assessment, codebase review, and protocol specification. Each KLIP is a self-contained Markdown file that captures the full lifecycle of a technical decision: from background and motivation through design details to acceptance criteria.</p>\n<h3>When to Read Reference</h3>\n<p>Before writing any KLIP, read the reference document for structural patterns and field definitions:</p>\n<pre><code>Read: references/klip-conventions.md\n</code></pre>\n<p>This reference contains the canonical section catalog, KLIP category taxonomy, naming rules, frontmatter fields, and writing constraints extracted from real KLIPs.</p>\n<h3>Core Workflow</h3>\n<ol>\n<li><strong>Identify KLIP category</strong> from user intent (see reference for taxonomy).</li>\n<li><strong>Select applicable sections</strong> from the section catalog based on category.</li>\n<li><strong>Gather inputs</strong> from user: background context, codebase facts, constraints, decisions already made.</li>\n<li><strong>Write the KLIP</strong> following structural and writing rules.</li>\n<li><strong>Output</strong> as a Markdown file named <code>klip-{number}-{slug}.md</code>.</li>\n</ol>\n<h3>Writing Principles</h3>\n<p>These principles govern all KLIP writing. They are non-negotiable.</p>\n<h4>Grounded in code, not imagination</h4>\n<p>Every claim about current system behavior must cite a real file path, function name, or data structure. If the author has not verified a fact against the codebase, the KLIP must say &quot;待确认&quot; or leave a placeholder, never fabricate.</p>\n<h4>Honest about what is decided vs. what is not</h4>\n<p>Sections that record conclusions use definitive language. Sections that record open questions use &quot;待讨论&quot;, &quot;待确认&quot;, or &quot;建议&quot;. The two must never be mixed within the same paragraph.</p>\n<h4>Minimal viable scope per document</h4>\n<p>Each KLIP covers one coherent decision boundary. If scope creep is detected during writing, the overflow should be called out as a future KLIP reference (e.g., &quot;该策略在后续 klip 中补充&quot;).</p>\n<h4>No motivational padding</h4>\n<p>No sentences whose only purpose is to reassure the reader that the proposal is good. Every sentence must carry information or a decision.</p>\n<h4>Frontmatter is mandatory</h4>\n<pre><code class=\"language-yaml\">---\nAuthor: &quot;@handle&quot; or &quot;AgentName&quot;\nUpdated: YYYY-MM-DD\nStatus: Draft | Almost Complete | Complete | Completed\n---\n</code></pre>\n<p>Optional frontmatter fields for completed KLIPs:</p>\n<ul>\n<li><code>Reviewer</code>, <code>ReviewDate</code>, <code>ClosedDate</code></li>\n<li><code>Origin</code> (if derived from another KLIP)</li>\n</ul>\n<h3>Section Selection Guide</h3>\n<p>Not every KLIP needs every section. Choose based on category:</p>\n<table>\n<thead>\n<tr>\n<th>Category</th>\n<th>Required Sections</th>\n<th>Common Optional Sections</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Feature Design</td>\n<td>背景, 目标, 非目标, 设计概览, 验收标准</td>\n<td>前置依赖, 术语表, 组件设计, 数据存储, 实施阶段, 待讨论</td>\n</tr>\n<tr>\n<td>Architecture Evaluation</td>\n<td>现状结论, 背景, 目标, 非目标, 评估维度, 评估结果, 最终建议</td>\n<td>成本与风险, 目标态设计, 迁移计划, 回滚策略</td>\n</tr>\n<tr>\n<td>Codebase Review</td>\n<td>背景, 现状, 目标, 非目标, 发现与方案(P0/P1/P2), 建议落地顺序</td>\n<td>关键参考位置</td>\n</tr>\n<tr>\n<td>Protocol/Streaming Design</td>\n<td>现状结论, 背景, 目标, 非目标, 协议设计, 服务端设计, 前端设计, 兼容性要求</td>\n<td>测试矩阵, 实施阶段, 决策摘要</td>\n</tr>\n<tr>\n<td>State Migration</td>\n<td>现状结论, 目标, 非目标, 状态边界, 阶段进展, 验收标准</td>\n<td>关键决策与约定, 测试矩阵, 实施后检查清单</td>\n</tr>\n<tr>\n<td>Runtime Optimization</td>\n<td>背景, 目标, 非目标, 设计方案, 测试矩阵, 验收标准</td>\n<td>实施后检查清单</td>\n</tr>\n</tbody></table>\n<h3>Output Format</h3>\n<ul>\n<li>File: <code>klip-{N}-{english-slug}.md</code></li>\n<li>Language: 中文为主体，技术术语保留英文原文，代码/类型/字段名不翻译</li>\n<li>Heading style: <code>#</code> for title (must match filename slug), <code>##</code> for top-level sections, <code>###</code> for subsections</li>\n<li>Code blocks: use fenced code with language tag (<code>json, </code>typescript, ```mermaid, etc.)</li>\n<li>Tables: Markdown pipe tables, keep columns aligned</li>\n<li>Checklists: use <code>- [ ]</code> for pending, <code>- [x]</code> for done, <code>- [~]</code> for moved/deferred</li>\n</ul>\n<h3>Quick Start Example</h3>\n<p>User: &quot;我想写一个 klip，关于把 fin-echarts 的 kline 图表从 replace 模式升级为支持 append 流式追加&quot;</p>\n<p>This maps to <strong>Protocol/Streaming Design</strong> category. The KLIP would include:</p>\n<ul>\n<li>现状结论（code references to current kline rendering）</li>\n<li>背景（why replace-only is limiting）</li>\n<li>目标 / 非目标</li>\n<li>协议补充（new append command shape with candles array）</li>\n<li>对外契约补充（type definitions, skill constraints, completion payload）</li>\n<li>实现落点（server parser, frontend runtime, render layer）</li>\n<li>测试计划</li>\n<li>Phase 建议</li>\n</ul>\n",
  "monorepo-architecture-principles": "<h2>Monorepo 架构组织与决策原则</h2>\n<p>跨多个真实项目收敛出的组织范式与决策原则。词汇遵循 deep-module 设计语言（module / interface / depth / seam / adapter / leverage / locality）。</p>\n<h3>1. Monorepo 分层范式</h3>\n<pre><code>apps/\n  web        # 应用（SPA / 客户端）\n  api|worker # 服务端（编排与事务边界，不放业务规则）\n  www|docs   # 内容站（SSG，与应用完全解耦）\npackages/\n  contracts|shared-types  # ★契约层：唯一被多方共享的最底层，只放数据形状不放逻辑\n  core|domain             # 纯函数业务规则，环境无关，每条配单测\n  db                      # 持久化，只被服务端依赖\n  tsconfig                # 共享配置\n</code></pre>\n<ul>\n<li><strong>依赖是单向 DAG</strong>：应用 → 领域包 → 契约层；内容站独立不进依赖图。契约层是前后端唯一共享面。</li>\n<li><strong>形态分离</strong>：营销/文档站 = 内容站（SSG，核心价值是正文进初始 HTML、可被搜索引擎与 AI 读取）；应用 = SPA。不要做「React App 套一层 SSG 壳」的假 SSG。</li>\n<li>工具链：pnpm workspace + Turbo；每包 AGENTS.md 记职责边界 + 依赖方向 + 运行时陷阱。</li>\n</ul>\n<h3>2. 单仓 → monorepo 的拆分顺序（依赖倒置）</h3>\n<p><strong>先抽最底层共享包（纯逻辑/类型/契约 + 单测）→ 再拆应用 → 最后配流水线（Turbo + CI）</strong>。每一步可即时修复、可回滚。不要从应用侧开拆。</p>\n<h3>3. 十条决策原则</h3>\n<ol>\n<li><strong>深模块优先</strong>：小接口藏大行为；接口即测试面；用删除测试判模块价值（删掉后复杂度消失 = pass-through 浅模块）；「一个 adapter 是假想 seam，两个才是真实 seam」——不为可能的变化预留抽象。</li>\n<li><strong>纯逻辑与副作用分离</strong>：业务规则下沉为环境无关纯函数（跨端复用 + 直接单测）；副作用推到边缘层。</li>\n<li><strong>契约层单一化</strong>：只放形状，不放逻辑；被多方依赖的包越底层越薄。</li>\n<li><strong>单一数据源 + 读写路径不分叉</strong>：如「前台只读本地缓存库、唯一 worker 负责写入」——坚决不开「源数据更新就绕过缓存直读」的兜底分叉，宁可接受低频场景的数据滞后；根因要在刷新链路本身修。</li>\n<li><strong>执行位置正确性</strong>：重 CPU / 长任务必须离开主执行线程（Electron main → worker、event loop → worker thread）。「导入无法取消」这类症状的本质往往是执行位置错了。</li>\n<li><strong>状态分层不套模式</strong>：store 只承载需驱动渲染的共享状态；useRef 管瞬时可变引用；拒绝教条式全面迁移。</li>\n<li><strong>成本 vs 现有痛点收口</strong>：即使新架构优点列得再多，短期无发布/性能/维护痛点就不重写；把新架构的收益以低成本方式内化到现有栈（如先把稳定契约硬边界留在 monorepo 内）。</li>\n<li><strong>语义正确性</strong>：先问「这个机制要解决什么问题」——本地持久索引不该套 HTTP cache 的 TTL 失效语义；DB 批处理必须保留租户/权限/软删/分页约束。</li>\n<li><strong>P0/P1/P2 分批整改</strong>：评估先出文档（每条 Go/Hold/Drop），不直接进实施；每批小步快跑、最小改动、可回归、可回滚、可度量，显式声明「不做什么」。</li>\n<li><strong>债务一次性收口</strong>：确认是结构性债务时做「边界治理」一次性收掉，不 ad-hoc 一处处打补丁。</li>\n</ol>\n<h3>4. 反模式清单</h3>\n<ul>\n<li>读取路径兜底分叉（缓存 + 直读双路径）</li>\n<li>假 SSG（客户端应用套 SSG 壳）</li>\n<li>上帝组件（千行级 + 几十个 useState）；整改三手法：强交互域局部 store 状态自治、纯展示组件二次分离、拒绝一刀切迁移</li>\n<li>「深度 = 实现行数 / 接口行数」的机械度量（奖励灌水）</li>\n<li>为迁移而迁移、为统一而统一</li>\n<li>native 模块被主 workspace 按 Node ABI 构建或被 bundle 进主进程</li>\n</ul>\n<h3>5. Native 模块与打包坑位</h3>\n<ul>\n<li>native addon（如 better-sqlite3）：Node 大版本 ABI 不匹配时固定 Node 版本；Electron 场景用<strong>独立子 workspace</strong> 隔离并针对 Electron ABI rebuild。</li>\n<li>pnpm 新版：native 包必须在 <code>pnpm-workspace.yaml</code> 显式放行构建，否则 install 失败。</li>\n<li>安装期编译慢：「安装期问题用发布预编译包解决，不在代码里处理」。</li>\n<li>Electron ESM main：生态包假设 <code>__dirname</code>，需构建插件替换 <code>import.meta</code> 并特判 Windows 路径。</li>\n<li>wasm/native 资源：用 electron-builder <code>extraResources</code> 显式 copy + <code>asarUnpack</code>，不手动拼路径。</li>\n<li>Cloudflare Worker 异步副作用必须挂 <code>waitUntil</code>（<code>void asyncTask()</code> 本地正常、线上丢失）。</li>\n<li>开发环境与打包应用要隔离 userData 存储路径，否则 onboarding 等首次逻辑不触发。</li>\n</ul>\n",
  "react-state-management-decisions": "<h2>React 状态管理决策</h2>\n<p>不是「要不要上 Zustand」，而是<strong>按语义给每份状态找对归属</strong>。核心决策树：</p>\n<ul>\n<li><strong>服务端数据</strong> → React Query（获取、缓存、刷新、mutation 后失效）。</li>\n<li><strong>DOM 引用 / 定时器句柄 / AbortController / 瞬时交互标记</strong> → useRef，保持不动。</li>\n<li><strong>单组件视图派生计算</strong> → useMemo，留在视图层。</li>\n<li><strong>跨组件共享 + 生命周期长于单组件实例（+ 需持久化）</strong> → Zustand。</li>\n<li><strong>导航状态</strong> → 路由（菜单点击应该体现为 URL 变更）。</li>\n</ul>\n<h3>1. useMemo 不要盲目迁 Zustand</h3>\n<ul>\n<li>Zustand 解决的是「状态共享与订阅」，<strong>不是</strong>自动优化「重计算」。把渲染派生结果塞进 store，流式更新时反而更频繁触发 store 更新 → 更多订阅重渲染。</li>\n<li>适合入 store 的派生：跨组件共享、多处复用、生命周期长于单组件的索引类数据（如会话级引用索引）。</li>\n<li>强依赖单条消息 props 的派生（内容转换、markdown components）本质是视图层局部计算，进全局 store 只增加失效与同步复杂度。</li>\n<li>优先级排序（按成本收益）：组件拆分 + 纯函数抽离 ＞ 部分引入 store 做会话级派生缓存 ＞ 大规模 useMemo 迁 store（收益不确定，最大风险是缓存失效不完整）。</li>\n</ul>\n<h3>2. useRef 按语义分类（实测 34 处只有 1 处值得迁）</h3>\n<table>\n<thead>\n<tr>\n<th>语义</th>\n<th>处置</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>DOM 引用</td>\n<td>保持 useRef</td>\n</tr>\n<tr>\n<td>定时器/动画句柄</td>\n<td>保持 useRef</td>\n</tr>\n<tr>\n<td>请求控制（AbortController）/并发标记</td>\n<td>保持 useRef</td>\n</tr>\n<tr>\n<td>交互瞬时态（拖拽中间值等）</td>\n<td>保持 useRef</td>\n</tr>\n<tr>\n<td>初始化哨兵位（initializedRef 等）</td>\n<td>通常保持；若 store 已有等价标记，改读 store</td>\n</tr>\n<tr>\n<td><strong>共享业务状态型 ref</strong></td>\n<td>唯一的迁移候选</td>\n</tr>\n</tbody></table>\n<p>不要为了「统一用 store」而替换——那是把实现手段当目标。</p>\n<h3>3. Zustand 与 React Query 边界</h3>\n<ul>\n<li>React Query 管服务端数据全生命周期；<strong>不要把服务端列表数据再塞进 Zustand</strong>（与 RQ 缓存职责重叠，双份真源必然漂移）。</li>\n<li>Zustand 只放客户端 UI 状态：导航折叠、侧栏、抽屉、modal、筛选条件、轻量用户偏好。</li>\n</ul>\n<h3>4. 渐进式迁移策略</h3>\n<ul>\n<li>以「某个状态收敛为单一来源」为目标推进（如 <code>activeConversationId</code> 收敛进 runtime store），按模块分批，不做大爆炸迁移。</li>\n<li>迁移进度用文档跟踪，定期用子代理分模块核对「文档声称 vs 代码现状」，防止脱节。</li>\n<li>复杂强交互（拖拽排序、多组件通信）用极轻量 store 取代深层 prop drilling / Context，配合纯展示组件二次分离。</li>\n</ul>\n<h3>5. persist 草稿态模式</h3>\n<p>「新建即草稿」场景（点新建不立即建库，发送时才创建正式实体）：</p>\n<ul>\n<li>草稿用 <code>zustand + persist</code>（localStorage），全局单例；重复触发新建回到同一草稿并恢复内容。</li>\n<li>只持久化必要字段（如输入文本）；草稿不进主数据库；多标签页冲突可先按「最后写入生效」不做处理。</li>\n</ul>\n",
  "save-the-cat-writing": "<h2>Save the Cat 写作自检</h2>\n<p>这是凯哥（Kevin）写 AI 辅助开发类公众号文章用的自检方法论。改编自 Blake Snyder 的 Save the Cat，去掉了讨好逻辑，保留结构技巧。</p>\n<h3>触发场景</h3>\n<p>两类场景触发，需要先判断：</p>\n<p><strong>写作前（选题/动笔阶段）</strong>：用户描述了一个想写的主题、分享了一个事件想写快评、或者在犹豫要不要写某个话题。关键词：想写、要不要写、选题、切入角度、有个想法。</p>\n<p>→ 走 §1 Logline 测试和 §2 标题测试，不要跳到后面的结构检查。</p>\n<p><strong>写作后（审稿/改稿阶段）</strong>：用户贴出一段草稿、一篇完整文章、或者说&quot;你看看这篇怎么样&quot;、&quot;哪里不够好&quot;、&quot;帮我看看开头&quot;。</p>\n<p>→ 走 §3-§7 完整清单。如果是快评（500-800 字），优先走 §7 而不是 §4 Beat Sheet。</p>\n<p>不确定是哪种场景时，优先问用户处在哪个阶段，不要两个都做。</p>\n<h3>默认交付物</h3>\n<p><strong>按清单逐项给出诊断意见，报告式输出</strong>。不要只说&quot;还不错&quot;或只给一两点建议。对每个适用的清单项，明确给出：</p>\n<ul>\n<li>这一项是否通过</li>\n<li>不通过的话，具体问题在哪（引用原文片段）</li>\n<li>怎么改（给出方向而不是代写）</li>\n</ul>\n<p>诊断要诚实，不要给万金油评语。如果某段确实写得好，说&quot;这段通过&quot;，不要硬挑毛病；如果某段平淡，直接指出来，不要用&quot;可以考虑进一步丰富&quot;这种废话。</p>\n<h3>§1 Logline 测试（动笔前）</h3>\n<p>一句话写出文章的&quot;讽刺性前提&quot;——读者看到这句应该产生认知冲突（&quot;我以为是 A，原来是 B&quot;）。</p>\n<p>检查点：</p>\n<ul>\n<li>这句话写不写得出来？写不出来说明切入角度不够锋利，劝退或让用户重想</li>\n<li>是否包含：谁会关心（受众）+ 反直觉的核心判断 + 具体的动作或场景</li>\n<li>非 AI 开发圈的人能不能听懂大意并且觉得有意思</li>\n</ul>\n<p><strong>通过示例</strong>：&quot;你以为在写 Spec，其实在绕路写代码。&quot;\n<strong>不通过示例</strong>：&quot;聊聊 Spec 驱动开发的几点思考。&quot;（没有反直觉判断，没有画面感）</p>\n<h3>§2 标题测试（动笔前或定稿前）</h3>\n<p>标题本身就是 pitch，不读正文光看标题就知道讲什么、为什么点。</p>\n<p>检查点：</p>\n<ul>\n<li>标题是否带态度或判断，不是中性主题描述</li>\n<li>是否有开发者能搜到的关键词（Spec、Agent、Code Review 等），兼顾搜一搜和推荐流</li>\n<li>如果是快评，光看标题能不能让人一眼判断&quot;我同不同意&quot;从而想点进来</li>\n</ul>\n<p><strong>通过示例</strong>：&quot;你以为在写 Spec，其实在绕路写代码&quot;\n<strong>不通过示例</strong>：&quot;聊聊 Spec 驱动开发的几点思考&quot;</p>\n<h3>§3 开场前两段：Save the Cat + 旧世界</h3>\n<p>检查点：</p>\n<ul>\n<li>前两段是否给了一个具体的、读者能代入的场景——不是泛泛背景介绍，而是有画面感的瞬间</li>\n<li>前三段内是否出现至少一个具体细节（报错信息、迭代次数、某个参数、某次失败），证明&quot;我真的自己干过这事&quot;</li>\n<li>开场是否展示了&quot;旧世界&quot;——读者正在经历的痛点或困境</li>\n</ul>\n<p><strong>通过示例</strong>：&quot;第 17 次让 Codex 改同一个函数的时候，我开始怀疑到底谁在给谁打工。&quot;\n<strong>不通过示例</strong>：&quot;最近 AI 编程工具越来越多了，今天来聊聊我的使用心得。&quot;</p>\n<h3>§4 Beat Sheet 结构检查（长文）</h3>\n<p>用五节点检查文章是否有推进感，而非平铺直叙：</p>\n<table>\n<thead>\n<tr>\n<th>节点</th>\n<th>对应内容</th>\n<th>自检问题</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>旧世界</td>\n<td>现有做法 / 主流认知 / 痛点</td>\n<td>是否足够具体，不是泛泛而谈</td>\n</tr>\n<tr>\n<td>触发事件</td>\n<td>我为什么要动手做 / 想明白这件事的契机</td>\n<td>是否有明确的时间点或事件</td>\n</tr>\n<tr>\n<td>尝试与挫败</td>\n<td>中间踩的坑、走过的弯路</td>\n<td>是否诚实，不是事后诸葛亮</td>\n</tr>\n<tr>\n<td>顿悟时刻</td>\n<td>真正管用的认知转变</td>\n<td>这个认知是不是只有亲手做过才能得出</td>\n</tr>\n<tr>\n<td>新世界</td>\n<td>现在我怎么做的 / 我的判断是什么</td>\n<td>是否给出明确的行动指引或立场</td>\n</tr>\n</tbody></table>\n<p>额外检查：</p>\n<ul>\n<li>中段是否有&quot;升高赌注&quot;的时刻——文章写到一半时，读者是否会觉得&quot;这事比我想的严重/有意思&quot;。只罗列要点说明缺少 midpoint 转折</li>\n<li>节点过渡是否自然，不是靠&quot;接下来我们聊聊……&quot;这种机械连接</li>\n</ul>\n<h3>§5 论点锋利度检查</h3>\n<p>检查点：</p>\n<ul>\n<li>是否有明确的、可以被反驳的判断。所有人都会同意的说法，说明观点不够锋利</li>\n<li>这个判断是一手经验还是转述别人观点</li>\n<li>是否避免了&quot;两边都有道理&quot;的万金油结尾——读者想听判断，不是平衡报道</li>\n</ul>\n<p>这一节是核心。如果文章在这一项上不通过，前面几项写得再好也救不回来，要直接告诉用户论点本身需要重构。</p>\n<h3>§6 结尾检查</h3>\n<p>检查点：</p>\n<ul>\n<li>是否回扣了开头的场景或问题，形成闭环</li>\n<li>是否有一句可以被单独摘出来转发的收束语（快评尤其重要）</li>\n<li>是在向前看（读者可以带走什么），而不是在总结全文（把前面说过的再说一遍）</li>\n</ul>\n<h3>§7 快评专项（500-800 字，替代 §4）</h3>\n<p>快评是&quot;一张电影海报&quot;：一眼看完，决定要不要转发。</p>\n<p>三要素必须同时满足：</p>\n<ul>\n<li>一个具体事件</li>\n<li>一个非显而易见的判断</li>\n<li>一句可传播的收束语</li>\n</ul>\n<p>检查点：</p>\n<ul>\n<li>事件描述是否克制，只给读者理解你判断所需的最少背景</li>\n<li>判断是否足够&quot;非共识&quot;——大多数人看到这个事件的第一反应和你一样，这篇快评就不值得写</li>\n<li>§3 开场、§5 锋利度、§6 结尾三项同样适用，但 §4 Beat Sheet 不适用</li>\n</ul>\n<h3>§8 发布前最后一遍</h3>\n<p>检查点：</p>\n<ul>\n<li>标记所有&quot;正确但无聊&quot;的段落——能删就删，能压缩就压缩</li>\n<li>是否有过度解释（读者是一线开发者，不需要解释什么是 CI/CD）</li>\n<li>和之前文章有主题重叠的部分，是否用&quot;一句带过 + 引导阅读&quot;代替了重复解释</li>\n<li>标题、首图、摘要三件套是否一致地传达了同一个信息</li>\n</ul>\n<h3>输出格式</h3>\n<p>报告式输出，建议结构：</p>\n<pre><code>## Logline / 标题（如适用）\n[通过 / 不通过 + 具体问题 + 改进方向]\n\n## 开场\n[引用原文开头 2-3 句] → [诊断] → [怎么改]\n\n## 结构（Beat Sheet 或快评三要素）\n[按节点逐项过]\n\n## 论点锋利度\n[关键节点，单独写一段]\n\n## 结尾\n[同样引用 + 诊断]\n\n## 发布前建议\n[具体哪些段落可以删/压缩]\n</code></pre>\n<p>不适用的节直接跳过，不要硬凑内容。比如用户只贴了开头，就只过 §3；只让看标题，就只过 §2。</p>\n<h3>几个写作时要坚守的原则</h3>\n<ul>\n<li>不要用&quot;可以考虑&quot;&quot;也许&quot;&quot;或许&quot;这类软化判断的词。要么通过要么不通过，含糊等于没说</li>\n<li>不要给两个以上并列建议让用户自己选——给一个你认为最该改的</li>\n<li>引用原文时用原话，不要转述后再点评，读者（凯哥）要自己对照</li>\n<li>如果整篇文章的问题在论点本身（§5 不通过），直接告诉用户&quot;建议重写而不是修改&quot;，不要在烂论点上堆改进意见</li>\n</ul>\n<hr>\n<p><strong>底层逻辑提醒</strong>：这套方法借鉴 Snyder 的结构技巧，不是他的讨好逻辑。文章值钱的地方在于敢下判断、基于一手经验、不怕得罪人。诊断时如果&quot;让文章更好看&quot;和&quot;说出真正的判断&quot;冲突了，支持后者。</p>\n",
  "stock-report": "<h2>Stock Report</h2>\n<p>这个 skill 的目标很窄：<strong>给定一个 A 股标的名称或代码，用本仓库固定的 DS 数据采集链路拉数，并生成统一版式的 HTML 报告</strong>。<br>它不是泛化的股票分析 skill，也不是 MCP skill。这里的真源是：</p>\n<ul>\n<li><code>./cli-data-fetch-guide.md</code></li>\n<li><code>./scripts/fetch_and_generate_stock_report.py</code></li>\n<li><code>./scripts/generate_ds_report.py</code></li>\n</ul>\n<h3>何时使用</h3>\n<p>出现以下任一情况时，直接使用本 skill：</p>\n<ul>\n<li>用户说“生成某只股票的报告”</li>\n<li>用户给出股票名称或股票代码，希望直接出本仓库这套 HTML 报告</li>\n<li>用户要复跑现有报告、验证生成链路是否稳定</li>\n<li>用户要在批量化之前先单标的验证 DS 拉数 + 报告生成</li>\n</ul>\n<p>如果用户要的是：</p>\n<ul>\n<li>改报告样式/章节/图表逻辑：先用本 skill 找到真源脚本，再修改脚本</li>\n<li>改 DS CLI 本身：回到代码仓库，不要把 skill 当主场</li>\n<li>用 MCP 或其他数据源：不要用这个 skill</li>\n</ul>\n<h3>输入约定</h3>\n<p>支持两种输入：</p>\n<ul>\n<li>股票代码：如 <code>000728.SZ</code></li>\n<li>股票名称：如 <code>国元证券</code></li>\n</ul>\n<p>如果用户给的是中文名称，脚本会先执行：</p>\n<pre><code class=\"language-bash\">ds iid Stk search &quot;&lt;名称&gt;&quot; --output json\n</code></pre>\n<p>并先按 A 股主证券做精确匹配。<br>如果无法唯一确认标的，应该明确报错并要求用户提供 <code>000728.SZ</code> 这类准确 IID，不要猜测代码或直接取第一条。</p>\n<h3>环境前提</h3>\n<p>执行前默认检查：</p>\n<pre><code class=\"language-bash\">ds status\n</code></pre>\n<p>只有在以下条件满足时才继续：</p>\n<ul>\n<li><code>ds</code> 命令存在</li>\n<li>用户已登录，token 可用</li>\n<li><code>python3</code> 可执行</li>\n</ul>\n<p>如果 <code>ds status</code> 失败，先告诉用户是环境/登录问题，不要继续生成半成品。</p>\n<h3>标准执行步骤</h3>\n<h4>1. 识别标的</h4>\n<p>用 <code>ds iid Stk search &lt;标的名称&gt;</code> 确认标的的IID代码</p>\n<p>运行：</p>\n<pre><code class=\"language-bash\">python3 &lt;skill_path&gt;/scripts/fetch_and_generate_stock_report.py &quot;&lt;标的IID代码&gt;&quot;\n</code></pre>\n<p>这个脚本会自动做以下事情：</p>\n<ul>\n<li>创建仓库根目录下的 <code>data/&lt;股票代码&gt;/raw/</code></li>\n<li>按 skill 内 <code>cli-data-fetch-guide.md</code> 的固定专题清单抓取原始 JSON</li>\n<li>对“允许为空”的专题写空文件，而不是报错退出</li>\n<li>调用 skill 内 <code>scripts/generate_ds_report.py</code> 生成结构化 JSON 和 HTML 报告</li>\n<li>写出仓库根目录下的 <code>data/&lt;股票代码&gt;/run_summary.json</code></li>\n</ul>\n<h4>2. 固定查询范围</h4>\n<p>不要临场删改这套查询清单。当前固定覆盖：</p>\n<ul>\n<li>基本信息 <code>1000933</code></li>\n<li>实控人 <code>1000998</code></li>\n<li>高管薪酬 <code>1001034</code></li>\n<li>股本结构 <code>1000987</code></li>\n<li>解禁 <code>1001006</code></li>\n<li>十大股东 <code>1000991</code></li>\n<li>十大流通股东 <code>1000996</code></li>\n<li>股东户数 <code>1000183</code></li>\n<li>大股东持股比例 <code>1000989</code></li>\n<li>分红 <code>1000978</code></li>\n<li>年度资产负债表 <code>1000964</code></li>\n<li>年度利润表 <code>1000965</code></li>\n<li>年度现金流量表 <code>1000966</code></li>\n<li>季度利润表 <code>1000970</code></li>\n<li>季度现金流量表 <code>1000971</code></li>\n<li>主营业务构成 <code>1000961</code></li>\n<li>一致评级 <code>1001348/1001352/1001351/1001353</code></li>\n</ul>\n<p>其中以下专题允许无数据：</p>\n<ul>\n<li>实控人 <code>1000998</code></li>\n<li>解禁 <code>1001006</code></li>\n<li>分红 <code>1000978</code></li>\n<li>一致评级四个专题</li>\n</ul>\n<p>无数据时应保留空结果，并让最终报告明确显示“该标的暂无此类数据”，不要表述成抓取异常。</p>\n<h4>3. 生成结果</h4>\n<p>成功后，目标目录应至少包含：</p>\n<pre><code class=\"language-text\">data/&lt;股票代码&gt;/\n├── raw/\n├── basic_info.json\n├── financial_data.json\n├── shareholders.json\n├── data_sources.md\n├── run_summary.json\n└── report/\n    └── report.html\n</code></pre>\n<p>其中 <code>report.html</code> 是用户主要关心的产物。</p>\n<h3>报告契约</h3>\n<p>当前报告是固定 5 章结构：</p>\n<ol>\n<li>公司简介</li>\n<li>股本和股东</li>\n<li>财务数据</li>\n<li>主营业务行业数据</li>\n<li>一致评级</li>\n</ol>\n<p>关键约束：</p>\n<ul>\n<li>样式对齐 <code>data/601377/report/report.html</code> 和 <code>stock-analysis-report/template/report_template.html</code></li>\n<li>页面中不显示数据来源区块</li>\n<li>利润表、现金流量表支持年度/季度页内 Tab</li>\n<li>资产负债表只展示年度口径</li>\n<li>解禁/一致评级无数据时显示明确空状态</li>\n<li>分红有数据则展示图表，无数据则显示空状态</li>\n<li>“主营业务行业数据”里的业务观察与论证逻辑，不通过代码模板生成；应在报告文件生成完成后，由执行该 skill 的 agent 基于当次标的数据单独输出。</li>\n</ul>\n<h3>输出给用户时怎么说</h3>\n<p>默认简洁交付：</p>\n<ul>\n<li>报告路径</li>\n<li>关键生成文件路径</li>\n<li>是否存在“该标的暂无此类数据”的专题（如实控人/解禁/分红/一致评级）</li>\n<li>是否真的完成了脚本执行验证</li>\n</ul>\n<p>如果你没有实际运行生成，不要暗示已经生成成功。</p>\n<h3>故障处理</h3>\n<h4>1. <code>ds status</code> 失败</h4>\n<p>优先判断为环境或登录问题，提示用户先确认：</p>\n<ul>\n<li><code>ds</code> 是否安装</li>\n<li>是否已登录</li>\n<li>token 是否有效</li>\n</ul>\n<h4>2. <code>iid search</code> 没结果</h4>\n<p>直接报“未识别到标的”，让用户提供更准确名称或代码。</p>\n<h4>3. 单个专题返回“缺少数据集”</h4>\n<p>如果该专题属于允许为空的范围：</p>\n<ul>\n<li>写空文件</li>\n<li>继续后续流程</li>\n<li>最终报告显示空状态</li>\n</ul>\n<p>如果不属于允许为空范围：</p>\n<ul>\n<li>中止流程</li>\n<li>明确告诉用户是哪条专题失败</li>\n</ul>\n<h4>4. 生成脚本失败</h4>\n<p>先检查：</p>\n<ul>\n<li>raw 文件是否存在空文件以外的损坏内容</li>\n<li>标的目录是否创建成功</li>\n<li>Python 是否可执行</li>\n</ul>\n<p>不要手工改 HTML 兜底，优先修脚本真源。</p>\n<h3>真源文件</h3>\n<p>需要修改或排查时，优先看：</p>\n<ul>\n<li><code>./scripts/fetch_and_generate_stock_report.py</code>：一键采集入口</li>\n<li><code>./scripts/generate_ds_report.py</code>：HTML/JSON 生成逻辑</li>\n<li><code>./cli-data-fetch-guide.md</code>：DS 查询专题清单</li>\n</ul>\n<h3>示例</h3>\n<p><strong>示例 1：直接生成报告</strong></p>\n<p>输入：</p>\n<pre><code class=\"language-text\">给我生成国元证券的报告\n</code></pre>\n<p>执行：</p>\n<pre><code class=\"language-bash\">python3 &lt;skill_path&gt;/scripts/fetch_and_generate_stock_report.py &quot;国元证券&quot;\n</code></pre>\n<p><strong>示例 2：按代码重跑</strong></p>\n<p>输入：</p>\n<pre><code class=\"language-text\">重跑 000728.SZ 的报告\n</code></pre>\n<p>执行：</p>\n<pre><code class=\"language-bash\">python3 &lt;skill_path&gt;/scripts/fetch_and_generate_stock_report.py &quot;000728.SZ&quot;\n</code></pre>\n<h3>边界</h3>\n<ul>\n<li>不要把这个 skill 扩展成批量任务编排器；批量化另做</li>\n<li>不要自动切换到 MCP</li>\n<li>不要擅自改章节结构或报告样式</li>\n<li>不要跳过 DS 原始数据落盘直接生成报告</li>\n</ul>\n",
  "tauri-menubar-agent-app": "<h2>Tauri 菜单栏应用（macOS）与多 Agent 事件桥接</h2>\n<p>Tauri v2 + Rust (<code>src-tauri</code>) + bun/vite 前端（固定端口 + strictPort）的菜单栏应用经验，含监听外部 coding agent 的 hook 桥接架构。</p>\n<h3>1. macOS 顶层窗口：纯 Tauri API 不够，要下到 objc2</h3>\n<ul>\n<li><strong>透明窗口崩溃</strong>：<code>transparent: true</code> 需要开 <code>app.macOSPrivateApi = true</code>。</li>\n<li><strong>IPC 崩溃（no reactor running）</strong>：IPC 监听不能在无 Tokio reactor 的上下文初始化——改用标准线程 + <code>UnixStream</code> 监听。</li>\n<li><strong>窗口被菜单栏压住</strong>：用 <code>objc2-app-kit</code> 拿 <code>ns_window()</code>，把层级提到 <code>NSStatusWindowLevel</code>(25)，Floating(3) 不够。</li>\n<li><strong>贴不到屏幕顶部</strong>有两个叠加原因：① <code>setFrameTopLeftPoint</code> 用 flipped 坐标，会贴在菜单栏下方；② macOS constraint 保护会自动修正坐标。要同时设置 <code>collectionBehavior</code> 允许覆盖菜单栏 + 原生精确定位。</li>\n<li>原生窗口操作必须在主线程执行。</li>\n<li><strong>不要猜 magic offset</strong>：坐标不对时从坐标系语义根源解决，不要试 <code>-28</code> 之类的魔法值。</li>\n</ul>\n<h3>2. 多 Agent hook 桥接架构</h3>\n<ul>\n<li>注入点：各 agent 的 hook 配置（<code>~/.claude</code> settings、<code>~/.codex/hooks.json</code>、<code>~/.cursor/hooks.json</code>），覆盖 SessionStart / Stop / SubagentStop / Notification / PermissionRequest / UserPromptSubmit。</li>\n<li>bridge 是独立小二进制（<code>--source &lt;claude|codex|cursor&gt;</code>），打进 <code>.app</code> 的 <code>Contents/Helpers</code>。</li>\n<li>IPC：bridge 写入 <code>~/.&lt;app&gt;/run/&lt;app&gt;.sock</code>（UnixStream），协议为单行 JSON <code>{ event: ... }</code>。</li>\n<li><strong>核心难点是事件归一化</strong>：各 agent 原始字段结构不同，覆盖面不足的症状是「日志进来了但 UI action 没联动」——归一化层要按 agent 逐个校准状态机映射（如 Codex：UserPromptSubmit 即 running，Stop 即 idle，SessionStart 后可能什么都不发生）。</li>\n<li>常见状态机 bug：permission 请求与 ask_user_question 未区分；permission 未处理被误判会话结束；会话结束后仍显示运行中；第二个 agent 进程不可见。<strong>排查一律先加日志读运行时输出</strong>。</li>\n</ul>\n<h3>3. 交互设计原则</h3>\n<ul>\n<li>菜单栏应用的介入要克制：需要用户介入时改变图标状态（如「招手」动画），<strong>不主动弹 popover 打断</strong>；用户的回应仍回到 agent 自身完成。</li>\n<li>涉及 permission 的事件优先级最高，菜单栏计数与动画图标要同步更新。</li>\n<li>消息预览区不做占位：0 条显示 0 条，最多显示最近 N 条，不为对齐而填充。</li>\n</ul>\n",
  "tech-stack-scaffolding": "<h2>技术栈脚手架与项目初始化惯例</h2>\n<p>四条技术栈主线的标准初始化。选型基线：新项目<strong>直接 oxlint/oxfmt（跳过 biome）</strong>、插件<strong>直接 WXT（跳过 crxjs）</strong>。</p>\n<h3>1. 前端 / 全栈 Web（bun + vite + react 19 + tailwind v4 + shadcn）</h3>\n<p><strong><code>.oxlintrc.json</code></strong>（几乎逐字通用）：</p>\n<pre><code class=\"language-json\">{\n  &quot;$schema&quot;: &quot;./node_modules/oxlint/configuration_schema.json&quot;,\n  &quot;plugins&quot;: [&quot;typescript&quot;, &quot;react&quot;, &quot;oxc&quot;, &quot;unicorn&quot;, &quot;import&quot;, &quot;vitest&quot;, &quot;jsx-a11y&quot;],\n  &quot;rules&quot;: {\n    &quot;@typescript-eslint/consistent-type-imports&quot;: [&quot;error&quot;, { &quot;fixStyle&quot;: &quot;inline-type-imports&quot;, &quot;prefer&quot;: &quot;type-imports&quot; }],\n    &quot;@typescript-eslint/no-non-null-assertion&quot;: &quot;error&quot;,\n    &quot;@typescript-eslint/no-unused-vars&quot;: [&quot;error&quot;, { &quot;args&quot;: &quot;after-used&quot;, &quot;argsIgnorePattern&quot;: &quot;^_&quot;, &quot;caughtErrors&quot;: &quot;none&quot;, &quot;varsIgnorePattern&quot;: &quot;^_&quot; }],\n    &quot;react-hooks/exhaustive-deps&quot;: &quot;error&quot;,\n    &quot;react/no-array-index-key&quot;: &quot;warn&quot;,\n    &quot;unicorn/prefer-node-protocol&quot;: &quot;error&quot;\n  },\n  &quot;settings&quot;: { &quot;react&quot;: { &quot;version&quot;: &quot;19.2&quot; } }\n}\n</code></pre>\n<p>简化变体：<code>&quot;categories&quot;: {&quot;correctness&quot;:&quot;error&quot;,&quot;perf&quot;:&quot;error&quot;,&quot;suspicious&quot;:&quot;error&quot;}</code>。</p>\n<p><strong><code>.oxfmtrc.json</code></strong>：<code>printWidth 100 / tabWidth 2 / singleQuote / trailingComma &quot;all&quot;</code> + ignorePatterns（node_modules、build、coverage 等）。</p>\n<p><strong>scripts 命名（强惯例）</strong>：</p>\n<pre><code class=\"language-json\">{\n  &quot;lint&quot;: &quot;oxlint src server &amp;&amp; oxfmt --check src server &amp;&amp; tsc --noEmit&quot;,\n  &quot;lint:fix&quot;: &quot;oxlint src server --fix &amp;&amp; oxfmt src server --write&quot;,\n  &quot;lint:format&quot;: &quot;oxfmt src server --write&quot;,\n  &quot;test&quot;: &quot;vitest run&quot;, &quot;test:coverage&quot;: &quot;vitest run --coverage&quot;,\n  &quot;check&quot;: &quot;bun run --parallel lint test&quot;\n}\n</code></pre>\n<p><code>lint</code> = 检查门禁（含 format --check 和 tsc --noEmit）；<code>check</code> = 提交前一键收口。</p>\n<ul>\n<li>monorepo（pnpm workspace + turbo）：每个 package 自定义 <code>lint</code>，根用 <code>turbo run lint</code> 调度；type-aware 时 <code>oxlint --type-aware --tsconfig tsconfig.json</code>，需要独立 <code>tsconfig.oxlint.json</code>（<code>moduleResolution: &quot;bundler&quot;</code>、<code>verbatimModuleSyntax</code>、<code>noEmit</code>、strict）。<strong>坑：type-aware 不接受 <code>baseUrl</code>，用 <code>paths</code> 代替。</strong></li>\n<li>shadcn <code>components.json</code>：style <code>new-york</code> + baseColor <code>neutral</code> + cssVariables；alias <code>@/components</code>、<code>@/lib/utils</code>。</li>\n</ul>\n<h3>2. 浏览器插件（WXT / MV3）</h3>\n<p><code>wxt.config.ts</code> 是唯一构建入口：</p>\n<pre><code class=\"language-ts\">export default defineConfig({\n  srcDir: &quot;src&quot;, entrypointsDir: &quot;entrypoints&quot;, outDir: &quot;dist&quot;,\n  modules: [&quot;@wxt-dev/module-react&quot;],\n  manifest: {\n    name: &quot;...&quot;, version: packageJson.version,   // 版本单一真源 = package.json\n    icons: { &quot;16&quot;: &quot;/icons/icon-16.png&quot;, &quot;32&quot;: &quot;...&quot;, &quot;48&quot;: &quot;...&quot;, &quot;128&quot;: &quot;...&quot; }\n  },\n  alias: { &quot;@&quot;: resolve(&quot;src&quot;) },\n  vite: () =&gt; ({ plugins: [tailwindcss()] })     // tailwind v4 走 @tailwindcss/vite\n});\n</code></pre>\n<ul>\n<li>scripts：<code>dev: wxt</code> / <code>build: wxt build</code> / <code>zip: wxt zip</code> / <code>postinstall: wxt prepare</code>（必须先于 typecheck，生成 <code>.wxt</code> 类型）。</li>\n<li>图标固定 [16,32,48,128]：轻量用 <code>sips -z 16 16 logo.png --out ...</code>；工程化用 <code>@resvg/resvg-js</code> 从 SVG 渲染（先补正方形 viewBox）。</li>\n<li><code>.gitignore</code>：<code>entrypoints/</code> 源码<strong>提交</strong>；<code>.wxt</code> / <code>.output</code> / <code>dist</code> <strong>忽略</strong>。</li>\n<li>从 crxjs 迁移：移除 <code>@crxjs/vite-plugin</code> 与 manifest.config.ts，删自定义打包脚本，出包统一 <code>wxt zip</code>。</li>\n</ul>\n<h3>3. Python CLI（uv + hatchling）</h3>\n<ul>\n<li><strong>src layout</strong> 固定；<code>[project.scripts]</code> 定义 CLI 入口；依赖基线 Typer + httpx + Rich + Pydantic。</li>\n<li>目录按职责 ≤6 组：<code>src/&lt;pkg&gt;/{commands,services,formatters,core,models,http}</code> + <code>tests/</code>。</li>\n<li>质量三件套：<code>ruff</code>（<code>select = [&quot;E&quot;,&quot;F&quot;,&quot;I&quot;,&quot;UP&quot;,&quot;B&quot;]</code>，line-length 100）+ <code>pyright</code>（basic）+ <code>ty</code>。</li>\n<li>动态版本：<code>dynamic = [&quot;version&quot;]</code> + <code>[tool.hatch.version] path = &quot;src/&lt;pkg&gt;/__about__.py&quot;</code>。</li>\n<li><code>justfile</code> 编排：<code>lint / lint-fix / lint-format / check(pyright+ty) / test / build(uv build --wheel) / publish-check(dry-run) → publish-test(TestPyPI) → publish</code>；<strong><code>just isok</code> = lint-fix + lint-format + check + test，提交前固定收口入口</strong>。</li>\n</ul>\n<h3>4. Go CLI</h3>\n<ul>\n<li>工具链版本用 <code>mise</code> 钉死（<code>mise.toml</code> 声明 go 版本，命令走 <code>mise x go@x.y.z -- go ...</code>）。</li>\n<li><code>justfile</code>（<code>set shell := [&quot;bash&quot;,&quot;-euo&quot;,&quot;pipefail&quot;,&quot;-c&quot;]</code>）：<code>fmt / fmt-check / vet / lint(golangci-lint) / test / build / check</code>（check 聚合 fmt-check+vet+test）。</li>\n<li><code>.golangci.yml</code> 只开稳妥规则，避免噪音。</li>\n<li>CI 加 <code>quality</code> job（gofmt + vet + test）；<code>v*</code> tag 触发矩阵构建 <code>&lt;name&gt;-&lt;version&gt;-&lt;platform&gt;.tgz</code>。</li>\n</ul>\n<h3>5. 新项目周边标配</h3>\n<ul>\n<li><code>LICENSE</code>（MIT）+ <code>CHANGELOG.md</code>（起始 0.1.0，顶部 <code>## Unreleased</code>）+ README；对外项目 CHANGELOG/README 中英双份，并在 AGENTS.md 写明「需维护中英文两份」。</li>\n<li><strong>AGENTS.md 精简写法（1 分钟能读完）</strong>，固定四段：<ol>\n<li>文档定位：工程协作入口，不重复 README、不展开业务参数；</li>\n<li>技术栈：对齐 package.json/pyproject 真实依赖逐条列；</li>\n<li>开发流程：固定顺序（装依赖 → 定向测试 → 全量校验 → <code>just isok</code>/<code>check</code> 收口 → 提交）；</li>\n<li>代码目录说明：按职责分层 ≤6 组，点明读代码入口顺序。\n可选：跨模块同步清单（「新增 X 必须同步 N 处」）、安全红线、常见坑。风格：中文、短句、强约束、全部核对真实实现，不写泛化模板。</li>\n</ol>\n</li>\n</ul>\n<h3>6. script 语义对照表</h3>\n<table>\n<thead>\n<tr>\n<th>语义</th>\n<th>前端</th>\n<th>Python (just)</th>\n<th>Go (just)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>检查</td>\n<td><code>lint</code></td>\n<td><code>lint</code> + <code>check</code></td>\n<td><code>fmt-check</code> + <code>vet</code> + <code>lint</code></td>\n</tr>\n<tr>\n<td>自动修</td>\n<td><code>lint:fix</code></td>\n<td><code>lint-fix</code></td>\n<td><code>fmt</code></td>\n</tr>\n<tr>\n<td>格式化</td>\n<td><code>lint:format</code></td>\n<td><code>lint-format</code></td>\n<td><code>fmt</code></td>\n</tr>\n<tr>\n<td>测试</td>\n<td><code>test</code> / <code>test:coverage</code></td>\n<td><code>test</code></td>\n<td><code>test</code></td>\n</tr>\n<tr>\n<td>一键收口</td>\n<td><code>check</code></td>\n<td><code>isok</code></td>\n<td><code>check</code></td>\n</tr>\n<tr>\n<td>出包</td>\n<td><code>build</code> / <code>zip</code>(wxt)</td>\n<td><code>build</code> / <code>publish</code></td>\n<td><code>build</code></td>\n</tr>\n</tbody></table>\n",
  "triple-mind-deep-reading": "<h2>三重心智模型：深读</h2>\n<p>对一篇文章/播客实录/演讲/论文，或一个复杂决策，不做概括式的平铺总结，而是同时用三个视角深入加工：既当正方，又当反方，还要导出对行动的启发。</p>\n<p>核心不是&quot;总结这篇讲了什么&quot;，而是&quot;这篇的观点站得住吗、能延伸到哪、对我意味着什么&quot;。</p>\n<h3>适用范围</h3>\n<ul>\n<li>硅谷技术播客 / 圆桌论坛实录</li>\n<li>长文（技术、思想、行业分析）</li>\n<li>带框架和方法论性质的演讲、论文</li>\n<li>审视一个复杂决策或方案</li>\n</ul>\n<p>不适用：查事实、短文本、用户只想要一句话摘要。</p>\n<h3>三个视角（缺一不可）</h3>\n<p>同一份材料，依次戴上三副眼镜。不是三段互不相干的评论，而是对同一批观点的三重加工。</p>\n<h4>1. 挑战者（Challenger / 批判性思想家）</h4>\n<p>找弱点。以最严格的反方立场审视：</p>\n<ul>\n<li>核心论点的前提是否成立？有没有未说明的假设？</li>\n<li>证据是否充分？是逸事、个例，还是系统证据？样本有没有偏差？</li>\n<li>逻辑有没有跳步、以偏概全、把相关当因果？</li>\n<li>边界条件是什么？在什么情况下这个观点会失效？</li>\n<li>作者的立场/利益是否影响了结论（卖课、卖产品、立人设）？</li>\n<li>有没有更简单的解释被忽略了？</li>\n</ul>\n<h4>2. 建构者（Builder / 框架构建者）</h4>\n<p>扩展观点。假设核心洞见成立，往前推：</p>\n<ul>\n<li>把零散的观点抽象成一个框架/模型。</li>\n<li>这个想法能推广到哪些原文没提的场景？</li>\n<li>和哪些已有的概念、理论、其他文章能连接、印证或互补？</li>\n<li>顺着作者的逻辑再往前走一步，会得到什么新结论？</li>\n<li>缺失的拼图是什么？补上哪一块，整个论述会更完整？</li>\n</ul>\n<h4>3. 实践者（Practitioner）</h4>\n<p>导出行动。落到&quot;对我/对读者有什么用&quot;：</p>\n<ul>\n<li>从中能提炼出哪些可操作的做法、清单、原则？</li>\n<li>我现在的做法和它有什么出入？该改什么？</li>\n<li>最小可执行的第一步是什么？</li>\n<li>有哪些坑、前置条件、代价需要注意？</li>\n<li>什么情况下我不该照搬？</li>\n</ul>\n<h3>执行流程</h3>\n<ol>\n<li><strong>通读全文</strong>，先抓住作者的核心主张和论证骨架（而不是逐段复述）。</li>\n<li><strong>一句话锚定</strong> 原文的核心命题（用来贯穿三个视角）。</li>\n<li><strong>依次跑三个视角</strong>：挑战者 → 建构者 → 实践者。每个视角独立成段，观点鲜明，不和稀泥。</li>\n<li><strong>交叉</strong>：挑战者挑出的弱点，建构者可以尝试补强或绕开；实践者要考虑弱点带来的实操风险。</li>\n<li><strong>收束</strong>：给出你自己（作为综合三视角的独立判断者）对这篇的净评价——值不值得信、值得信到什么程度、最该记住的一点。</li>\n</ol>\n<h3>输出结构</h3>\n<pre><code>## 核心命题\n（一句话概括原文真正在主张什么）\n\n## 挑战者视角\n（找弱点：前提、证据、逻辑、边界、立场偏差。观点鲜明，逐条列。）\n\n## 建构者视角\n（扩展：抽象成框架、连接已有概念、往前推一步、补缺失拼图。）\n\n## 实践者视角\n（行动：可操作做法/清单、与现状的差距、最小第一步、注意的坑。）\n\n## 净评价\n（综合三视角的独立判断：可信度、最该记住的一点、什么情况下不适用。）\n</code></pre>\n<h3>原则</h3>\n<ul>\n<li><strong>不平铺、不复述</strong>。如果某一段只是把原文换个说法重说一遍，删掉。三个视角都要产出原文里没有的东西。</li>\n<li><strong>观点要鲜明</strong>。挑战者就狠狠挑，建构者就大胆推，实践者就落到具体动作。不要每句都&quot;一方面……另一方面……&quot;和稀泥。</li>\n<li><strong>保持诚实</strong>。挑战者不为挑而挑，建构者不脑补作者没有的意思，实践者不给空泛口号。</li>\n<li><strong>独立思考</strong>。三视角跑完，最后要有你自己的判断，而不是把三段拼在一起就完事。</li>\n</ul>\n<h3>可选：直接对话式 prompt（迁移用）</h3>\n<p>当用户想用这套框架分析他自己贴进来的材料时，可用如下开场，然后让用户粘贴全文：</p>\n<blockquote>\n<p>我们将一起深入分析一份材料。请你不做概括式总结，而是同时以三重心智模型来加工：<strong>挑战者</strong>（找弱点）、<strong>建构者</strong>（扩展并抽象成框架）、<strong>实践者</strong>（导出可操作的启发）。请动态调整视角、层次丰富地展开，我们的讨论应当平等、深入、自主。准备好后，我把材料全文贴给你。</p>\n</blockquote>\n<p>用途可从&quot;分析文档&quot;迁移到&quot;讨论复杂问题&quot;&quot;学习一个概念&quot;&quot;审视一个决策&quot;——把三视角相应改为对应问题的正方、反方、行动方即可。</p>\n",
  "use-modern-go": "<h2>Modern Go Guidelines</h2>\n<h3>Detected Go Version</h3>\n<p>!<code>grep -rh &quot;^go &quot; --include=&quot;go.mod&quot; . 2&gt;/dev/null | cut -d&#39; &#39; -f2 | sort | uniq -c | sort -nr | head -1 | xargs | cut -d&#39; &#39; -f2 | grep . || echo unknown</code></p>\n<h3>How to Use This Skill</h3>\n<p>DO NOT search for go.mod files or try to detect the version yourself. Use ONLY the version shown above.</p>\n<p><strong>If version detected (not &quot;unknown&quot;):</strong></p>\n<ul>\n<li>Say: &quot;This project is using Go X.XX, so I’ll stick to modern Go best practices and freely use language features up to and including this version. If you’d prefer a different target version, just let me know.&quot;</li>\n<li>Do NOT list features, do NOT ask for confirmation</li>\n</ul>\n<p><strong>If version is &quot;unknown&quot;:</strong></p>\n<ul>\n<li>Say: &quot;Could not detect Go version in this repository&quot;</li>\n<li>Use AskUserQuestion: &quot;Which Go version should I target?&quot; → [1.23] / [1.24] / [1.25] / [1.26]</li>\n</ul>\n<p><strong>When writing Go code</strong>, use ALL features from this document up to the target version:</p>\n<ul>\n<li>Prefer modern built-ins and packages (<code>slices</code>, <code>maps</code>, <code>cmp</code>) over legacy patterns</li>\n<li>Never use features from newer Go versions than the target</li>\n<li>Never use outdated patterns when a modern alternative is available</li>\n</ul>\n<hr>\n<h3>Features by Go Version</h3>\n<h4>Go 1.0+</h4>\n<ul>\n<li><code>time.Since</code>: <code>time.Since(start)</code> instead of <code>time.Now().Sub(start)</code></li>\n</ul>\n<h4>Go 1.8+</h4>\n<ul>\n<li><code>time.Until</code>: <code>time.Until(deadline)</code> instead of <code>deadline.Sub(time.Now())</code></li>\n</ul>\n<h4>Go 1.13+</h4>\n<ul>\n<li><code>errors.Is</code>: <code>errors.Is(err, target)</code> instead of <code>err == target</code> (works with wrapped errors)</li>\n</ul>\n<h4>Go 1.18+</h4>\n<ul>\n<li><code>any</code>: Use <code>any</code> instead of <code>interface{}</code></li>\n<li><code>bytes.Cut</code>: <code>before, after, found := bytes.Cut(b, sep)</code> instead of Index+slice</li>\n<li><code>strings.Cut</code>: <code>before, after, found := strings.Cut(s, sep)</code></li>\n</ul>\n<h4>Go 1.19+</h4>\n<ul>\n<li><code>fmt.Appendf</code>: <code>buf = fmt.Appendf(buf, &quot;x=%d&quot;, x)</code> instead of <code>[]byte(fmt.Sprintf(...))</code></li>\n<li><code>atomic.Bool</code>/<code>atomic.Int64</code>/<code>atomic.Pointer[T]</code>: Type-safe atomics instead of <code>atomic.StoreInt32</code></li>\n</ul>\n<pre><code class=\"language-go\">var flag atomic.Bool\nflag.Store(true)\nif flag.Load() { ... }\n\nvar ptr atomic.Pointer[Config]\nptr.Store(cfg)\n</code></pre>\n<h4>Go 1.20+</h4>\n<ul>\n<li><code>strings.Clone</code>: <code>strings.Clone(s)</code> to copy string without sharing memory</li>\n<li><code>bytes.Clone</code>: <code>bytes.Clone(b)</code> to copy byte slice</li>\n<li><code>strings.CutPrefix/CutSuffix</code>: <code>if rest, ok := strings.CutPrefix(s, &quot;pre:&quot;); ok { ... }</code></li>\n<li><code>errors.Join</code>: <code>errors.Join(err1, err2)</code> to combine multiple errors</li>\n<li><code>context.WithCancelCause</code>: <code>ctx, cancel := context.WithCancelCause(parent)</code> then <code>cancel(err)</code></li>\n<li><code>context.Cause</code>: <code>context.Cause(ctx)</code> to get the error that caused cancellation</li>\n</ul>\n<h4>Go 1.21+</h4>\n<p><strong>Built-ins:</strong></p>\n<ul>\n<li><code>min</code>/<code>max</code>: <code>max(a, b)</code> instead of if/else comparisons</li>\n<li><code>clear</code>: <code>clear(m)</code> to delete all map entries, <code>clear(s)</code> to zero slice elements</li>\n</ul>\n<p><strong>slices package:</strong></p>\n<ul>\n<li><code>slices.Contains</code>: <code>slices.Contains(items, x)</code> instead of manual loops</li>\n<li><code>slices.Index</code>: <code>slices.Index(items, x)</code> returns index (-1 if not found)</li>\n<li><code>slices.IndexFunc</code>: <code>slices.IndexFunc(items, func(item T) bool { return item.ID == id })</code></li>\n<li><code>slices.SortFunc</code>: <code>slices.SortFunc(items, func(a, b T) int { return cmp.Compare(a.X, b.X) })</code></li>\n<li><code>slices.Sort</code>: <code>slices.Sort(items)</code> for ordered types</li>\n<li><code>slices.Max</code>/<code>slices.Min</code>: <code>slices.Max(items)</code> instead of manual loop</li>\n<li><code>slices.Reverse</code>: <code>slices.Reverse(items)</code> instead of manual swap loop</li>\n<li><code>slices.Compact</code>: <code>slices.Compact(items)</code> removes consecutive duplicates in-place</li>\n<li><code>slices.Clip</code>: <code>slices.Clip(s)</code> removes unused capacity</li>\n<li><code>slices.Clone</code>: <code>slices.Clone(s)</code> creates a copy</li>\n</ul>\n<p><strong>maps package:</strong></p>\n<ul>\n<li><code>maps.Clone</code>: <code>maps.Clone(m)</code> instead of manual map iteration</li>\n<li><code>maps.Copy</code>: <code>maps.Copy(dst, src)</code> copies entries from src to dst</li>\n<li><code>maps.DeleteFunc</code>: <code>maps.DeleteFunc(m, func(k K, v V) bool { return condition })</code></li>\n</ul>\n<p><strong>sync package:</strong></p>\n<ul>\n<li><code>sync.OnceFunc</code>: <code>f := sync.OnceFunc(func() { ... })</code> instead of <code>sync.Once</code> + wrapper</li>\n<li><code>sync.OnceValue</code>: <code>getter := sync.OnceValue(func() T { return computeValue() })</code></li>\n</ul>\n<p><strong>context package:</strong></p>\n<ul>\n<li><code>context.AfterFunc</code>: <code>stop := context.AfterFunc(ctx, cleanup)</code> runs cleanup on cancellation</li>\n<li><code>context.WithTimeoutCause</code>: <code>ctx, cancel := context.WithTimeoutCause(parent, d, err)</code></li>\n<li><code>context.WithDeadlineCause</code>: Similar with deadline instead of duration</li>\n</ul>\n<h4>Go 1.22+</h4>\n<p><strong>Loops:</strong></p>\n<ul>\n<li><code>for i := range n</code>: <code>for i := range len(items)</code> instead of <code>for i := 0; i &lt; len(items); i++</code></li>\n<li>Loop variables are now safe to capture in goroutines (each iteration has its own copy)</li>\n</ul>\n<p><strong>cmp package:</strong></p>\n<ul>\n<li><code>cmp.Or</code>: <code>cmp.Or(flag, env, config, &quot;default&quot;)</code> returns first non-zero value</li>\n</ul>\n<pre><code class=\"language-go\">// Instead of:\nname := os.Getenv(&quot;NAME&quot;)\nif name == &quot;&quot; {\n    name = &quot;default&quot;\n}\n// Use:\nname := cmp.Or(os.Getenv(&quot;NAME&quot;), &quot;default&quot;)\n</code></pre>\n<p><strong>reflect package:</strong></p>\n<ul>\n<li><code>reflect.TypeFor</code>: <code>reflect.TypeFor[T]()</code> instead of <code>reflect.TypeOf((*T)(nil)).Elem()</code></li>\n</ul>\n<p><strong>net/http:</strong></p>\n<ul>\n<li>Enhanced <code>http.ServeMux</code> patterns: <code>mux.HandleFunc(&quot;GET /api/{id}&quot;, handler)</code> with method and path params</li>\n<li><code>r.PathValue(&quot;id&quot;)</code> to get path parameters</li>\n</ul>\n<h4>Go 1.23+</h4>\n<ul>\n<li><code>maps.Keys(m)</code> / <code>maps.Values(m)</code> return iterators</li>\n<li><code>slices.Collect(iter)</code> not manual loop to build slice from iterator</li>\n<li><code>slices.Sorted(iter)</code> to collect and sort in one step</li>\n</ul>\n<pre><code class=\"language-go\">keys := slices.Collect(maps.Keys(m))       // not: for k := range m { keys = append(keys, k) }\nsortedKeys := slices.Sorted(maps.Keys(m))  // collect + sort\nfor k := range maps.Keys(m) { process(k) } // iterate directly\n</code></pre>\n<p><strong>time package</strong></p>\n<ul>\n<li><code>time.Tick</code>: Use <code>time.Tick</code> freely — as of Go 1.23, the garbage collector can recover unreferenced tickers, even if they haven&#39;t been stopped. The Stop method is no longer necessary to help the garbage collector. There is no longer any reason to prefer NewTicker when Tick will do.</li>\n</ul>\n<h4>Go 1.24+</h4>\n<ul>\n<li><code>t.Context()</code> not <code>context.WithCancel(context.Background())</code> in tests.\nALWAYS use t.Context() when a test function needs a context.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">func TestFoo(t *testing.T) {\n    ctx, cancel := context.WithCancel(context.Background())\n    defer cancel()\n    result := doSomething(ctx)\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">func TestFoo(t *testing.T) {\n    ctx := t.Context()\n    result := doSomething(ctx)\n}\n</code></pre>\n<ul>\n<li><code>omitzero</code> not <code>omitempty</code> in JSON struct tags.\nALWAYS use omitzero for time.Duration, time.Time, structs, slices, maps.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">type Config struct {\n    Timeout time.Duration `json:&quot;timeout,omitempty&quot;` // doesn&#39;t work for Duration!\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">type Config struct {\n    Timeout time.Duration `json:&quot;timeout,omitzero&quot;`\n}\n</code></pre>\n<ul>\n<li><code>b.Loop()</code> not <code>for i := 0; i &lt; b.N; i++</code> in benchmarks.\nALWAYS use b.Loop() for the main loop in benchmark functions.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">func BenchmarkFoo(b *testing.B) {\n    for i := 0; i &lt; b.N; i++ {\n        doWork()\n    }\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">func BenchmarkFoo(b *testing.B) {\n    for b.Loop() {\n        doWork()\n    }\n}\n</code></pre>\n<ul>\n<li><code>strings.SplitSeq</code> not <code>strings.Split</code> when iterating.\nALWAYS use SplitSeq/FieldsSeq when iterating over split results in a for-range loop.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">for _, part := range strings.Split(s, &quot;,&quot;) {\n    process(part)\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">for part := range strings.SplitSeq(s, &quot;,&quot;) {\n    process(part)\n}\n</code></pre>\n<p>Also: <code>strings.FieldsSeq</code>, <code>bytes.SplitSeq</code>, <code>bytes.FieldsSeq</code>.</p>\n<h4>Go 1.25+</h4>\n<ul>\n<li><code>wg.Go(fn)</code> not <code>wg.Add(1)</code> + <code>go func() { defer wg.Done(); ... }()</code>.\nALWAYS use wg.Go() when spawning goroutines with sync.WaitGroup.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">var wg sync.WaitGroup\nfor _, item := range items {\n    wg.Add(1)\n    go func() {\n        defer wg.Done()\n        process(item)\n    }()\n}\nwg.Wait()\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">var wg sync.WaitGroup\nfor _, item := range items {\n    wg.Go(func() {\n        process(item)\n    })\n}\nwg.Wait()\n</code></pre>\n<h4>Go 1.26+</h4>\n<ul>\n<li><code>new(val)</code> not <code>x := val; &amp;x</code> — returns pointer to any value.\nGo 1.26 extends new() to accept expressions, not just types.\nType is inferred: new(0) → *int, new(&quot;s&quot;) → *string, new(T{}) → *T.\nDO NOT use <code>x := val; &amp;x</code> pattern — always use new(val) directly.\nDO NOT use redundant casts like new(int(0)) — just write new(0).\nCommon use case: struct fields with pointer types.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">timeout := 30\ndebug := true\ncfg := Config{\n    Timeout: &amp;timeout,\n    Debug:   &amp;debug,\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">cfg := Config{\n    Timeout: new(30),   // *int\n    Debug:   new(true), // *bool\n}\n</code></pre>\n<ul>\n<li><code>errors.AsType[T](err)</code> not <code>errors.As(err, &amp;target)</code>.\nALWAYS use errors.AsType when checking if error matches a specific type.</li>\n</ul>\n<p>Before:</p>\n<pre><code class=\"language-go\">var pathErr *os.PathError\nif errors.As(err, &amp;pathErr) {\n    handle(pathErr)\n}\n</code></pre>\n<p>After:</p>\n<pre><code class=\"language-go\">if pathErr, ok := errors.AsType[*os.PathError](err); ok {\n    handle(pathErr)\n}\n</code></pre>\n",
  "writing-agent-skills": "<h2>编写与测试 agent skill</h2>\n<p>一份把「写好一个 skill」拆成五步的可执行方法论：分类 → 编写 → 触发设计 → 脚本分工 → 测试评估。适用于为任何 agent（Claude Code、Codex、Gemini CLI 等）编写、重写、审查或评估 skill。</p>\n<h3>核心心智模型</h3>\n<ul>\n<li>Skill 不是一个 markdown 文件，是一个<strong>文件夹</strong>：<code>SKILL.md</code> 清单 + 可选的 <code>scripts/</code>、<code>references/</code>、<code>assets/</code>。把整个文件系统当作**渐进式披露（progressive disclosure）**工具。</li>\n<li>加载顺序是分层的：启动时只加载 <code>name</code> + <code>description</code>；被选中后才加载 <code>SKILL.md</code> 正文；正文里指向的引用/脚本只在需要时才读或跑。写作时要顺着这个顺序分配信息。</li>\n<li>一个好 skill 有三个特征：<strong>窄契约、明确触发、具体产出</strong>。让人困惑的 skill 往往横跨多个类别、或触发条件含糊。</li>\n</ul>\n<h3>第一步：分类（决定要不要做、做哪一类）</h3>\n<p>先判断这个 skill 属于哪一类，横跨多类是重构信号。常见类别：</p>\n<ol>\n<li>库/API 参考 — 帮 agent 正确使用某个库/CLI/SDK，核心是「踩坑点列表」+ 参考片段。</li>\n<li>产品验证 — 描述如何测试/验证代码是否工作，常配 Playwright、tmux、无头浏览器。</li>\n<li>数据获取与分析 — 连接数据与监控，含凭证、仪表盘 ID、查询工作流。</li>\n<li>业务流程自动化 — 把重复工作流收敛成一条命令（站会汇报、周报、建工单）。</li>\n<li>代码脚手架/模板 — 生成符合本仓库注解与约定的样板代码。</li>\n<li>代码质量/审查 — 执行代码风格、adversarial review，可挂在 hook 或 CI 里。</li>\n<li>CI/CD 与部署 — 拉取、推送、部署、监控 PR、回滚。</li>\n<li>运维手册 — 从一个现象（告警/错误）走完排查流程，输出结构化报告。</li>\n<li>基础设施运维 — 日常维护与破坏性操作，需要安全护栏。</li>\n</ol>\n<p>同时分清两种性质（影响测试策略）：</p>\n<ul>\n<li><strong>能力型（capability）</strong>：补模型稳定做不到的事。模型变强后可能过时，靠 eval 检测退役。</li>\n<li><strong>偏好型（preference）</strong>：记录你的特定工作流。持久，但价值取决于对真实工作流的忠实度，靠 eval 验证。</li>\n</ul>\n<h3>第二步：编写正文</h3>\n<ul>\n<li><strong>别说显而易见的事。</strong> Claude 本身很会编程、也了解代码库。正文重点放在能<strong>打破模型默认思路</strong>的信息上（例如某设计系统避免 Inter 字体和紫色渐变），而不是复述常识。</li>\n<li><strong>用指令，不用信息。</strong> 模型执行指令好于推断含义。写「Always use <code>interactions.create()</code>」而不是「Interactions API 是推荐做法」；写「运行完 X 才能标记完成」而不是「建议验证」。</li>\n<li><strong>建一个踩坑点（gotchas）章节。</strong> 这是信息量最大的部分。它应随着 agent 使用中暴露的失败点<strong>持续增量积累</strong>——每修一次真实失败就补一条。</li>\n<li><strong>别把 Claude 限制得太死。</strong> Skill 复用性强，指令过于具体会削弱泛化。给它需要的信息，但留出适应具体情况的灵活性。</li>\n<li><strong>渐进式披露正文。</strong> 详细函数签名、用法示例拆到 <code>references/api.md</code>；最终产物模板放 <code>assets/</code> 供复制。在正文里告诉 Claude 有哪些文件、何时去读。</li>\n<li><strong>初始设置。</strong> 若 skill 需要用户上下文（如发到哪个 Slack 频道），把配置存进 skill 目录下的 <code>config.json</code>，未配置时让 agent 主动询问；需要结构化多选时用 AskUserQuestion。</li>\n<li><strong>记忆与数据。</strong> 需要跨次记忆时，用 append-only 日志或 JSON/SQLite。数据存稳定目录（如 <code>${CLAUDE_PLUGIN_DATA}</code>），别放会随升级被清空的 skill 目录内。</li>\n</ul>\n<h3>第三步：触发设计（description 是路由契约）</h3>\n<p><code>description</code> 不是摘要，是<strong>路由元数据</strong>——启动时它是模型判断「这个请求要不要用这个 skill」的主要依据。这是结构性问题，不是文风问题。</p>\n<ul>\n<li>写成 <strong>when / if-then 条件</strong>，而非功能说明。要同时讲清三件事：<strong>何时适用、哪类变更触发、是否可选</strong>。</li>\n<li>反例（太模糊）：<code>Run the mandatory verification stack in the monorepo.</code></li>\n<li>正例：<code>Run the mandatory verification stack when changes affect runtime code, tests, or build/test behavior in the monorepo.</code></li>\n<li>描述里要嵌入触发时机与产出形态。例：<code>Create a PR title and draft description after substantive code changes are finished. Trigger when wrapping up a moderate-or-larger change ... you need the PR-ready summary block.</code> —— 它告诉模型：这是任务结束型、仅用于实质变更、产出是 PR 块。</li>\n<li><strong>匹配用户意图，不用 API 术语。</strong> 用用户真正会说的话描述场景。实测中，仅重写 description 就能修掉大部分「该触发却没触发」的失败。</li>\n<li>经验法则：<strong>如果路由不可靠，先修元数据，再加正文/代码。</strong></li>\n</ul>\n<h3>第四步：脚本分工（确定性下沉，判断留给模型）</h3>\n<p>可靠的拆分原则：</p>\n<ul>\n<li><strong>交给模型：</strong> 解释、比较、报告、权衡取舍——依赖上下文和判断力的部分。例如读源码推断预期行为、对比日志与预期、判断 release diff 是否有真实兼容性风险。</li>\n<li><strong>下沉到 <code>scripts/</code>：</strong> 确定性、可重复的 shell 工作。例如按固定顺序跑验证命令、收集日志、取上一个 release tag。<strong>判据：如果模型每次都要重新发现同一套 shell 配方，它就该变成脚本。</strong></li>\n<li>把脚本设计成<strong>小型 CLI</strong>：命令行可跑、打印确定性 stdout、失败时大声报错（附 usage）、需要时把产物写到已知路径；暴露 <code>start</code>/<code>stop</code>/<code>status</code>/<code>logs</code>/<code>rerun</code> 之类的辅助子命令便于反复运行。</li>\n</ul>\n<p><strong>脚本能挡掉 LLM 的固有缺陷（重要踩坑）：</strong></p>\n<ul>\n<li><strong>无语义文本（hash、随机串）LLM 会抄错。</strong> 别让模型手抄 SHA256 之类字符串——用脚本生成好带该字段的文件，再让模型填内容。</li>\n<li><strong>LLM 对时间流逝无感知</strong>，会「刻舟求剑」记住旧时间戳。别让它自行输出当前时间，让它调脚本取，例如 <code>node -e &#39;console.log(new Date().toISOString())&#39;</code>。</li>\n<li><strong>质量门槛用代码级判断，别靠 prompt 恳求。</strong> 「不要输出空 insight」写进 prompt 弱模型会忽略；<code>if zero_insights: return None</code> 永不退化。凡是「必须成立」的约束，尽量落到确定性代码里。</li>\n<li>约束不足时模型会自相矛盾/自作聪明；加更多约束能压制，但代价是灵活性下降——优先想能否用脚本或结构消除歧义，而不是无限堆 prompt。</li>\n</ul>\n<h3>第五步：测试评估（没有 eval 就别发 skill）</h3>\n<p>写代码不会不写测试，发 skill 也不该不做 eval。流程：</p>\n<ol>\n<li><strong>先定义成功标准（写 skill 之前就写）。</strong> 三个维度，评<strong>结果不评路径</strong>（agent 会找创造性解法，别惩罚意外但正确的路线）：<ul>\n<li>Outcome：是否产出可用结果（能编译/能渲染/API 返回有效）。这是基线。</li>\n<li>Style &amp; Instructions：是否遵循约定与 skill 指令（对的 SDK、正确的 model ID、命名规范、指定格式）。</li>\n<li>Efficiency：耗费的时间/token/重试。最被低估的维度，回归是真实成本。</li>\n</ul>\n</li>\n<li><strong>先手动触发几次。</strong> 用显式调用（<code>Use the {skill name} to do X</code> 或 <code>/skill</code>、<code>$skill-name</code>）跑几遍，观察在哪出错——暴露隐藏假设（假设了不存在的依赖？跳过了该做的步骤？）。手动阶段修的每一个问题都变成一条可自动化的检查。</li>\n<li><strong>建提示集（prompt set）。</strong> 单个 skill 起步 <strong>10–20 条</strong>，取自真实用法。每条声明自己的 <code>expected_checks</code>。覆盖核心能力、护栏（如禁用已弃用 API）、扩展功能，以及<strong>负面测试</strong>。</li>\n<li><strong>务必包含负面测试。</strong> 加上 skill <strong>不该触发</strong>的提示——描述过宽的 skill 会在每个请求上乱触发。</li>\n<li><strong>跑 agent 并捕获输出。</strong> 用 agent 真实体验它的方式测（通常经 CLI），捕获 response、stats、exit code。</li>\n<li><strong>写确定性检查。</strong> 每个检查是一个返回 bool 的小函数（多用 regex 针对抽取出的代码），注册进 registry，按 <code>check_id</code> 分发，遍历每个用例。</li>\n<li><strong>对定性维度用 LLM-as-judge。</strong> 代码结构、命名、设计品味等 regex 抓不住的，用第二个模型辅助评分，并用**结构化输出（typed schema）**约束结果使其可解析可追踪。选择性使用——确定性检查快，LLM 评分有成本和延迟。</li>\n<li><strong>多次试验 + 隔离环境。</strong> agent 非确定，每个用例跑 3–5 次看分布，不看单次；每个用例用干净环境，防止上下文串味掩盖真实失败。</li>\n<li><strong>跨 harness 测试。</strong> 同一 skill 在不同 agent 框架下行为可能不同；若跨工具使用，每个都要 eval。</li>\n<li><strong>升级与退役。</strong> 能力评估从低通过率爬到 ~100% 后，转为<strong>回归评估</strong>防倒退。定期在<strong>卸载该 skill</strong>的情况下跑评估——若仍全过，说明模型已内化其价值，可以退役。</li>\n</ol>\n<h3>分发与强制</h3>\n<ul>\n<li>小团队协作少数仓库：直接把 skill 提交到仓库 <code>./.claude/skills/</code>（或 <code>.agents/skills/</code>）即可。注意每个提交进去的 skill 都会给上下文增加负担。</li>\n<li>规模变大：做成插件 + 内部插件市场，让成员自选安装；让最有用的 skill 自然涌现，发布前要有审核机制，避免低质/重复 skill。</li>\n<li><strong>让工作流在正确时机成为强制项。</strong> 在仓库级指令文件（<code>AGENTS.md</code> / <code>CLAUDE.md</code>）里用简短 <strong>if-then 规则</strong>声明强制 skill，高价值的放最前：例如「改动影响运行时代码/测试/构建行为时，调用 <code>$code-change-verification</code>，通过前不得标记完成」「准备移交时调用 <code>$pr-draft-summary</code>」。skill 编码「什么叫已验证/已就绪」，指令文件让这个定义可强制执行。</li>\n<li><strong>门控输出要可操作。</strong> release/审查类 skill 从「安全」出发，只有 diff 有具体证据才切到「阻塞」，且每个阻塞必须附带明确的解除清单。</li>\n<li>本地稳定后，再用 GitHub Action 等把同一工作流搬进 CI（手动使用阶段才是调指令、修脚本、发现边缘情况的地方）。</li>\n</ul>\n<h3>快速清单</h3>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 归入单一类别，契约窄、产出具体</li>\n<li><input disabled=\"\" type=\"checkbox\"> description 写成 when/if-then，讲清何时适用+触发条件+是否可选，用用户意图措辞</li>\n<li><input disabled=\"\" type=\"checkbox\"> 正文用指令不用信息，不说显而易见的事，有踩坑点章节，不过度限制</li>\n<li><input disabled=\"\" type=\"checkbox\"> 确定性/重复的活下沉到 scripts（小型 CLI）；hash/时间戳/硬质量门用代码而非 prompt</li>\n<li><input disabled=\"\" type=\"checkbox\"> 详细引用与模板拆到 references/、assets/，渐进式披露</li>\n<li><input disabled=\"\" type=\"checkbox\"> 有 10–20 条 prompt 的 eval harness，含负面测试，评结果不评路径，多次试验</li>\n<li><input disabled=\"\" type=\"checkbox\"> 需要时用 AGENTS.md/CLAUDE.md 的 if-then 规则强制触发</li>\n</ul>\n"
};
